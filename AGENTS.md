<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# 公立応援団

公立高校野球応援サイト。全国の公立・国立高校の野球を「見る・知る・応援する」ためのWebメディア。

**現在の進捗・未完了事項・次にやることは `README.md` にまとめてある。作業を始める前に読むこと。**
要約：MVP全12フェーズ完了。未公開。運営者情報とドメイン取得が未了。

## 開発環境の注意点

**Node.js はポータブル版をシステムにインストールせず使っている。** PATH が通っていないため、
npm / npx を実行する前に必ず PATH を通すこと。

```powershell
$env:Path = "C:\Users\81809\tools\node-v24.19.0-win-x64;" + $env:Path
```

手動で起動する場合はプロジェクト直下の `開発サーバーを起動.bat` を使う。

## コマンド

| コマンド | 内容 |
|---|---|
| `npm run dev` | 開発サーバー |
| `npm run lint` | ESLint |
| `npm run typecheck` | tsc --noEmit |
| `npm run check` | lint → typecheck → build を通しで実行 |

コミット前は `npm run check` を通すこと。

## 設計上の決めごと

- **収録対象は「私立以外」**。県立・市立・町村立・組合立に加え、**国立と高専も含む**。
  `is_public` のような真偽値は持たず、`establishment` と `school_kind` の2軸で表現する。
- **東京都立国立高校（くにたち）は「都立」であって「国立（こくりつ）」ではない。**
  設置区分を機械判定すると必ず誤分類するので、この種の学校は手動確認する。
- **URLのslugはローマ字**。日本語URLはエンコードで読めなくなるため使わない。
- **画像は `image_url` / `image_credit` / `image_source_url` の3点セット**で持つ。
  Wikimedia Commons の CC BY-SA 画像は帰属表示が法的義務のため。
- **画像を外部URLから直接参照しない。** 自分のドメインから配信する。
  `next.config.ts` の `remotePatterns` は Supabase Storage のホストだけを許可している。
  **学校の校舎写真3,059枚は Supabase Storage ではなく `public/schools/` に置いてある**
  （2026-08-13）。この規則の目的は「外部URLを直接参照しない」ことで、
  `public/` でも満たせるうえ、バケットの作成も service_role キーも要らない
  （このリポジトリは service_role キーを持たない運用）。ロゴとヒーローも `public/` にある。
  **`public/schools/` は必ずコミットすること。** gitに入れないとVercelのビルドに
  画像が含まれず全部リンク切れになる。生成は `scripts/build-school-images.mjs`。
- **ニュースの全文転載はしない。** 見出し＋自作の要約＋出典名＋元記事リンクまで。
- **選手個人のページ・個人成績は作らない**（未成年の個人情報配慮）。
  **写真に選手が写ることはこれとは別扱い**（2026-08-12 方針変更）。
  運営者自身が観客席から撮った試合写真はヒーローで使っている。
  報道写真や第三者が撮った試合写真は著作権の面から引き続き不可。
- **データ取得は `src/lib/queries/` を経由する。** ページから直接 supabase-js を呼ばない。
- **`src/types/database.ts` は手書き。** Supabase CLI の型自動生成は使っていない
  （ローカルにツールを増やさないため）。`supabase/migrations/` を変更したら
  **必ずこのファイルも合わせること。** ずれても TypeScript は気づけない。
  `node --env-file=.env.local scripts/check-supabase.mjs` で実データを見て確認する。
- **クエリで `status = 'published'` を書かない。** RLS が公開済みの行しか返さない。
  アプリ側で二重に条件を書くと、RLSが壊れたときに気づけなくなる。
- **広告は `AdSlot` コンポーネント経由のみ。** ページに広告ネットワーク固有のコードを書かない。
- **未公開コンテンツの除外は RLS で強制する。** アプリ側のクエリ条件だけに頼らない。
- ~~**利用者の投稿は都道府県単位まで。学校ページに自由記述欄を置かない。**~~
  → **2026-08-20 に運営者の判断で撤回。応援メッセージは学校単位になった**
  （`supabase/migrations/0008_school_cheer_messages.sql`）。**元に戻さないこと。**
  ★**「選手個人のページ・個人成績は作らない」という上の方針は変わっていない。**
  置き場所が学校ページに移ったぶん個人名が書かれやすいので、歯止めは全部残してある
  ── 投稿は必ず draft で入り承認したものだけ公開、投稿欄に個人名不可の注意書き、
  利用規約 5-2 の「選手・生徒個人を名指しした内容は掲載しない」。
  **承認する人がこの基準で見ることが最後の砦。**
  ★**投稿欄は学校ページにしか無い。** 都道府県ページはその県の学校あての投稿を
  宛先つきで並べる集約表示で、投稿欄は置かない（2か所に置くと利用者が迷い、
  承認作業も2系統になるため）。
  ★**`prefecture_id` はクライアントから受け取らない。** DBのトリガが学校から引く。
  受け取ると、ある県の学校あての投稿を別の県のページに混ぜ込める。
- **anon の書き込みはRLSとDBトリガだけが守っている。** anonキーは公開鍵なので、
  アプリを経由しない書き込みが常に可能。API ルートを挟んでも裏口は塞げないため、
  検査をフロント側やクエリ層に書いても防御にならない。必ずDB側に置くこと。
- **集計トリガは `security definer` にする。** そうしないと呼び出した anon の権限で
  動き、RLSポリシーが無いテーブルへの update が**エラーにならず0行更新**になる。
  数が増えない不具合になり、画面上は成功に見えるので気づきにくい
  （`supabase/migrations/0006_community_fixes.sql` の経緯）。
- **オレンジ（accent）は小面積のアクセントのみ。** 面で使わない。
- **ロゴ画像は `npm run logo` で生成する。** `public/logo*.png` を直接編集しない。
  原本は `assets/logo-source.png`。詳細は README の「ロゴ」。
- **ヒーローの写真も同じで `npm run hero`。** 原本は `assets/hero/`、
  出力は `public/hero/`、表示順とクレジットは `src/lib/hero.ts`。
  **写真の上には無彩色の黒の膜を重ねる**（`globals.css` の `.hero-veil`）。
  一様に濃くすると写真が見えないので、**見出しのある左側だけ濃くする**。
  **ネイビーの膜に戻さない**（2026-08-13 に変更。写真が青くカブって見づらかった）。
  濃さは「いちばん明るい青空の上でもオレンジの見出しが3:1」から決めてある
  （白文字の4.5:1より厳しいのはオレンジのほう）。
  **区切りは % ではなく px**（460px / 660px）。`<br>` で折り返しを決めているので
  いちばん長い行の幅は一定で、% だと狭い画面のときだけ文字がはみ出す
  **`prefers-reduced-motion` では切り替えない。**
  **出典が確認できない写真を足さない**（広告を載せる＝営利なので個人利用限定の素材は不可）。
  出典は `HeroSlide.source` に必ず書く（画面には出さない記録用）。画面に出す
  `credit` は表示義務があるときだけ。
- **Wikimedia Commons の写真は使ってよい**（2026-08-13 決定）。
  ~~2026-08-12 に「使わない」と決めていたが撤回した。~~ ヒーローでも学校の写真でも使える。
  **CC BY / CC BY-SA は帰属表示が法的義務**なので、撮影者・ライセンス名・
  出典へのリンク・改変の告知（縮小や切り抜きをしたなら）を必ず画面に出すこと。
  ヒーローで使うなら `HeroSlide.credit` に入れる（`source` だけでは足りない）。
- **学校の校舎写真は ja.wikipedia から取り込んでいる**（2026-08-13。3,059校）。
  **この画像を「方針に反する」と判断して消さないこと。**
  取り込みの規則は `scripts/build-school-images.mjs` と README にある。要点だけ:
  - **ライセンスが判別できないものは使わない。** ja.wikipedia にある以上フリーの
    可能性は高いが、可能性で他人の著作物は使わない（3件除外）
  - **GFDL単独も使わない。** 複製にライセンス全文を添えることを求めるため（2件除外）
  - **校舎写真でない代表画像がある。** 県の位置図・高専のロゴなど。名前と拡張子で
    落とし、さらに**複数校が同じ画像を使っていたら除外**する（48件除外）
- **学校ごとの記章（`SchoolEmblem`）の色は、実際のチームカラーではない。**
  slugから決まるサイトの配色。機械的に当てられないし、当て推量は誤情報になる。
  色数を増やすほど「本物の色だ」と誤解されるので5色に絞ってある。
- **一覧で使う短い校名は `src/lib/school-name.ts`。** 末尾の「高校」だけを落とす。
  **見出し（学校ページのh1）と検索結果では使わない**（一意に伝える必要があるため）。
  「中等教育学校」「高専」「分校」は落とさない（何の学校か分からなくなる）。
  同じ県で衝突する3校（県岐阜商・市岐阜商・市和歌山）だけslugで上書きしている。
  ★**これは「画面に出す校名」の規則。照合用の別名とは別**（2026-08-13 追記）。
  出典は「城ノ内」「阿南高専」「科学技術」のように書くので、
  `build-regional-results.mjs` は照合のためにそれらの形も候補に持つ。
- **敗戦数を画面に出さない。** DBには `losses` が入っているが、表示するのは勝利数だけ。
  ランキングでも同じ（`wins`勝 とだけ書く）。
- **`prefectures` は都道府県ではなく「甲子園の大会区分」49件。**
  北海道は北北海道・南北海道、東京は東東京・西東京に分かれる。
  分割していない45件の id はJISコードのまま、分割した4件は48〜51。
  学校の住所としての「北海道」「東京都」は `schools.city` 側にある。
- **学校名・所在地・設置区分を手で書かない。** 文部科学省の学校コード一覧から
  `scripts/build-school-seed.mjs` で生成する。生成AIに校名を書かせると
  実在しない学校が混ざる。補ってよいのは読み・表示名・地区だけ（README参照）。
- **甲子園出場歴・戦績も生成AIに書かせない。** 校名と同じ扱いで、出典のある事実だけを入れる。
  統廃合・校名変更で現存しない学校の出場歴は現存校に引き継ぎ、`note` に旧校名を残す。
  **出典サイトは規約を読んでから選ぶ。** 見るのは①自動取得の禁止 ②**営利目的での利用の禁止**
  の2点。②があると目視での転記も塞がる（このサイトは広告を載せるので営利）。
  一球速報.com・バーチャル高校野球はどちらも確認済みで除外（README参照）。
- **硬式野球部の有無は各県高野連の加盟校名簿でしか判断しない。**
  校名や設置区分から推測しない。名簿は `data/baseball-clubs.json`。
  部が無い学校は削除せず `status = 'draft'` にする。
- **ランキング（`/rankings`）の集計はアプリ側でやる。DBのビューを足さない。**
  このプロジェクトは Supabase CLI を使わず人がSQL Editorで適用する運用なので、
  ビューを増やすと「適用し忘れてページが落ちる」が起きる。対象は出場歴3千件・
  学校700件で、1回あたり4リクエスト・1.5秒。ページの `revalidate` は1日。
  **PostgREST は1回に1,000行しか返さない。** 出場歴はページングで取る必要があり、
  そのとき**並び順を一意に決めてから**取ること（`school_id, year, season`）。
  並びが不定だとページの境目で行が重複したり抜けたりする。
- **ランキングには「私立を収録していない」と必ず書く。** 学校マスタが公立だけなので
  ここでの1位は全国1位ではない。`DataNote` コンポーネントを各ページに置く。
- ★★**自動で出す文章は、持っているデータの並べ替えだけにする**（2026-08-29）。
  学校ページのリード文（`src/lib/school-lead.ts`）・年別アーカイブ・記録2ページは
  **どれも生成AIに1文字も書かせていない。** 校名や戦績と同じ扱いで、
  **データに無いことは書かない。**
  ★**「甲子園に出ていない」と書かない** —— DBに無いことは世の中に無いことではない。
  **あるときだけ書く。**
  ★★**「0勝」「0試合に勝っています」は言い換えただけの敗戦数。**
  勝った数が0のときは**その文ごと出さない**（「敗戦数を画面に出さない」に触れる）。
  ★★**同じ文が2,300枚並ぶと逆効果**（自動生成の薄いページと見なされ、
  `school-index.ts` が避けている「サイト全体が薄い」に逆戻りする）。
  **文を差し込むのではなく、持っているデータで段落の構成そのものが変わる**作りにしてある。
- ★★**リード文と記録ページは生成物にしない。描画時に組む**（2026-08-29）。
  学校ページは**すでに**自分の県の試合・甲子園の試合・直接対決を読んでおり、
  そこから組めば**データが増えた瞬間に文も変わる。**
  ★`src/lib/data/` に生成物を増やすと「`TARGETS` に書き足し忘れて凍る」
  （`regional-progress.ts` で実際に起きた）を繰り返すことになる。
  ★★**年別アーカイブ（`src/lib/archive.ts`）も同じ理由で生成物を持たない** ——
  **大会スラッグの連番の規則が `listTournaments`（TS）の中にあり、
  スクリプト（.mjs）からは import できない。** 生成物にすると規則をもう1つ写すことになる。
  ★**全県を読むが、sitemap がすでにやっていること**で、
  `getRegionalDistrict` も `listTournaments` も県ごとに1回しか走らない。
  ★**このモジュール自身も1回だけ組む**（`indexPromise` / `records()`）。
- ★★**年別アーカイブ（`/archive/<年>`）は地方大会がある年だけ作る**（26枚）。
  甲子園は1915年から収録しているので、全部の年を作ると
  **中身の薄いページが80枚以上**できる。`/koshien/<年-季節>` が既にあるので作る意味も無い。
  ★**入口はフッター**（`FOOTER_EXTRA_LINKS`）。**`NAV` に足さない** ——
  1024px でナビの右端639px・ボタンの左端847px しかなく、6つ目であふれる。
- ★★**トップの結果カードは横スライド**（2026-08-31。運営者の提案）。
  `src/components/results/ResultsCarousel.tsx`。**既定で自動送り＋止める切り替え付き。**
  ★**きっかけは「抜粋に20試合あるのに4試合しか出していなかった」** ——
  **データを増やさずに見せる量が5倍**になる。
  ★★★**スライドの中身は必ずサーバーで全部描いてDOMに置くこと。**
  「見えている1枚だけ描く」作りにすると**検索エンジンには1枚ぶんしか見えず、
  インデックスされる中身はむしろ減る。**（実測：サーバーHTMLに20試合ぶんが入っている）
  ★**めくるのはCSSのスクロール（`snap`）。JSは位置合わせと自動送りだけ。**
  JSが動かなくても横スクロールで全部読める。
  ★★**自動で動くものには必ず止める手段を付ける**（WCAG 2.2.2）。
  ヒーローの写真は**装飾**なので `prefers-reduced-motion` だけで足りていたが、
  **試合結果は情報**なので、①目に見える切り替え（端末に保存）②触れているあいだ止める
  ③タブが裏なら止める ④手でめくったら数え直す、の4つを入れてある。
  ★**`prefers-reduced-motion` では既定で止める。ただし本人が「めくる」を選んだらそちらを優先**
  （`readAutoplay()` が **未設定(null) と オフ(false) を区別**しているのはこのため）。
  ★**1枚あたりの件数を枚ごとに変えないこと**（高さが揃わずめくるたびにガタつく。実測で5枚とも343px）。
  ★★★**その件数は抜粋の試合数から決める**（2026-09-01。`fitPerSlide`）——
  **決め打ちの12件は「24件がたまたま12で割り切れていた」だけ**で、
  **27件や30件の日は最後の1枚だけ短くなる**（上の決めごとに反する）。
  ★**2枚以上めくれる、いちばん大きい偶数**を採る（偶数なのは2列に均等に割るため）。
  **上限は18件＝2列×9行**。★**割り切れない余りは出さない**
  （抜粋はもともと乱数で選んだ標本なので数件落ちても性格は変わらない。
  **「最後の1枚だけ短い」を出さないほうが大事**）。
  ★**スマホは1列なので、18件の日は1枚が長くなる**（実測 12件で1,012px）。**承知のうえ。**
- ★★★**トップの結果カードと右カラムは、低いほうを伸ばさない**（2026-09-01。運営者の「目いっぱい使って」）。
  左（結果）と右（勝ち上がっている公立校）は**中身の量が別々に決まる**ので、
  **どちらが高いかは日によって入れ替わる。** `grid` の既定（`stretch`）だと
  **低いほうのカードに大きな空白**ができていた
  （実測：右が3校の日は**右に246px**、右が8校の日は**左に230px**）。
  ★**`items-start` にして右は中身なりの高さで終わらせる。**
  ★★**左だけ `h-full` で行の高さいっぱいに伸ばし、余りは行が分け合う**
  （`auto-rows-fr`）。**横スライドなので伸びしろを中身に配れる。**
  ★**`h-full` は `items-start` でも効く**（%の高さはグリッド領域＝行の高さに対して解く。実測済み）。
  ★**カルーセル側は `flex flex-col` ＋ 帯に `min-h-0 flex-1`**。
  **`min-h-0` が無いと `flex-1` を渡しても枠からはみ出す。**
- ★★**電光掲示板（リボンボード）はトップのヒーローと結果カードのあいだ**（2026-08-31。運営者の提案）。
  `src/components/results/ResultsTicker.tsx` ＋ `globals.css` の `.ticker`。
  ★★**JavaScriptを1行も使っていない**（サーバーコンポーネント＋CSSアニメーションだけ）。
  **`scroll` や `IntersectionObserver` が効かない環境でも壊れない**（カルーセルで踏んだ罠）。
  ★★**止めるボタンは付けない**（運営者の判断:「常に情報が流れるイメージ」）。
  ★**そのかわり文字をリンクにしない** —— **動いているものは押せない。**
  リンクは下の結果カードが持っているので、ここは眺めるものに徹する。
  ★**押せない飾りにリンクの見た目を与えないこと。**
  ★★**「動きを減らす」設定のときだけ流さない**（横スクロールできる帯にする）。
  **これは止めるボタンとは別物**で、ヒーローの写真と同じ規則。**サイト全体でそろえる。**
  ★★**読み上げからは外す**（`aria-hidden`）—— **同じ試合がすぐ下にリンク付きで並んでいる。**
  2度読ませると、動いているほうを先に読まされて邪魔になる。
  ★★**配色はネイビーの地＋navy-100の文字＋スコアだけ accent-500。**
  **オレンジを面で使わない**という決めごとをここでも守る。
  ★**地は navy-900 より暗くしてある** —— すぐ上の「公立旋風」が navy-800 の面なので、
  同じ明るさだと**並べたときに段差が出ない。**
  ★★**継ぎ目を無くす作りは「同じ並びを2回描いて `translateX(-50%)`」。**
  **1周の移動量＝前半の幅**になるので必ずつながる（実測5,991px で一致）。
  ★**流す秒数は長さに比例させること**（決め打ちにすると**試合が増えたときだけ速くなる**）。
- ★★★**ゆっくり流すものは合成レイヤーに載せる**（2026-08-31。運営者から「カクカクする」）。
  毎秒65pxは60fpsで**1フレーム約1.1px**。**レイヤーに載っていないと整数ピクセルに丸められ、
  1px進んでは止まる段々の動きになる。** ★**速度を落とすほど目立つ。**
  ★**`will-change: transform` と `translate3d` は組で入れる**
  （2Dの `translateX` だと載らない環境がある）。
  ★**`will-change` を面の広い要素に付けないこと**（レイヤーぶんのメモリを食う。
  電光掲示板は高さ40pxなので幅12,000pxでも約2MB）。
- ★★★**校名を切ってはいけない**（2026-08-31。結果カードを2列にしたとき）。
  2列にした最初の版は、県・日付を左の細い列に置いたままだったので
  **校名に残る幅が50px（3文字ほど）**になり、**実測で22校が切れていた**
  （`岡山吉備白陵` → `岡山吉…`）。**校名はこのサイトの主役。**
  ★**県・日付・回戦を上の行へ逃がして、校名とスコアに列幅を丸ごと渡す**形にした（22件 → 1件）。
  ★**残る1件は6校の連合チーム**（幅432px必要）。**`title` で全名が出るようにしてある。**
  ★★**列を増やすときは「切れている校名の数」を実測すること**
  （`scrollWidth > clientWidth` で数えられる）。見た目だけで判断しない。
- ★★★**カルーセルで「動かない」に見える壊れ方が2つある**（2026-08-31 に実測。どちらも警告が出ない）。
  ★**`scrollTo({behavior:"smooth"})` が無反応な環境がある。**
  `behavior:"auto"` は動くのに `"smooth"` だけ何も起きず、
  **CSS の `scroll-behavior: smooth` を指定すると `scrollLeft` への代入まで塞がれる。**
  ★**対処：なめらかに頼んだあと250msで「動き出したか」を確かめ、
  1ピクセルも動いていなければ瞬間移動させる。** ★**CSSに `scroll-behavior` を書かないこと**
  （書くと逃げ道の `behavior:"auto"` まで効かなくなる）。
  ★**`scroll` イベントも `IntersectionObserver` も発火しない環境がある**
  （`scrollLeft` は変わるのにイベント0件、観測開始直後の1回すら来ない）。
  ★★**だから「いま何枚目か」を位置の監視だけに頼らない。**
  **ボタン・点・自動送りのぶんは `goTo` がその場で state を進め、
  スクロールの監視は「指で払われたとき」の補正に徹する。**
  ★**症状は「点が1枚目のまま」「2枚目から先へ進まない」。** 原因の切り分けは
  ①`scrollTo({behavior:"auto"})` が効くか ②`scroll` が発火するか、の順で見る。
- ★★**リード文は5種類のページにある。器は1つ、組み立てはページごと**（2026-08-29 その3）。
  器＝`src/components/common/LeadText.tsx`（**文の組み立てをここに置かないこと**）。
  組み立て＝`school-lead.ts` / `prefecture-lead.ts` / `tournament-lead.ts` /
  `national-lead.ts` / `versus-lead.ts`。**規則は上の項（自動で出す文章）が全部に効く。**
  ★★**優勝校は `summarizeTournament` からしか取らない**（決勝がちょうど1試合のときだけ）。
  ★★**`publicEntrants` の「成績不明」を勝ち上がりとして書かない** ——
  並びの先頭に来ることがあり、そのまま書くと**「もっとも勝ち進んだのは◯◯（成績不明）」**
  という意味の通らない文になる。
  ★★**神宮は大会名に年も種目名も入っている。** 甲子園と同じ形にすると
  **「2025年 明治神宮野球大会 高校の部は、2025年の明治神宮大会（高校の部）です」**という
  同語反復になる。**大会の性格を書くほうに替えてある。**
  ★**日付を1つも持たない出典（三重・大阪ほか）では日程の文を出さない。推測で埋めない。**
- ★★**県ページの甲子園集計に `getKoshienDataset` を使わないこと**（2026-08-29 その3）。
  あちらは出場歴3,000件を4リクエストで取る `/rankings` 用のもの。
  ★★**県ページは `revalidate = 300`** なので、**49枚が5分ごとに作り直されうる**
  ——1時間に588回のデータセット取得になる。
  ★**`getPrefectureKoshienSummary`（その県の非正規化列だけ読む）を使う。**
- ★★**地方大会を持たない8地区の書き方**（北北海道・南北海道・青森・宮城・秋田・
  東東京・西東京・鳥取＝**規約で塞がれている6県**ぶん）。
  ★**「まだ対応していません」と書かない** —— 事実は作業が追いついていないのではなく、
  **出典の利用条件を確認したうえで見送っている。**
  ★**連盟を非難する書き方にしないこと。事実だけを短く書く。**
- ★★★**地方大会の記録を「全国で1つの順位」にしない**（2026-08-29。`src/lib/records.ts`）。
  よく当たるカードを全国で並べたら、**上位120組がほぼ全部 山口**になった。
  山口は26年ぶん、他県は数年ぶんしか収録していないので、
  **順位が「対戦の多さ」ではなく「収録の深さ」を表す。**
  ★**それを「多い順」と書いて出すのは読者に対して嘘に近い。県ごとに区切ること。**
  ★**地方大会の優勝校は `summarizeTournament` からしか取らない**
  （「決勝がちょうど1試合のときだけ」という規則がそこにある。
  ブロックごとに決勝がある大会で1つ選ぶと嘘になる）。
  ★**大差試合のページを作らない** —— 負けた学校を名指しで並べることになる。
  ★**延長戦も出さない** —— 地方大会の生成物は `note` を持っていない。**推測しない。**
  ★**注記に `DataNote` を使わない** —— あちらの本文は**甲子園の出典（Wikipedia）を
  名指し**しており、地方大会の出典（県ごとの高野連など）とは別物。
- **成績が不明な出場を「初戦敗退」に混ぜない。** 最高成績の一覧でも、
  段階が確定しない学校はどの段階にも入れず別に数える。
- **21世紀枠は `src/lib/data/twenty-first-century.ts`（生成物）。**
  年に1〜3件しか増えないのでDBに入れていない。
  `scripts/build-21st-century.mjs` が Wikipedia「選抜高等学校野球大会」の
  「21世紀枠出場校一覧」から作る。**選考理由の文章は取り込まない**
  （事実の抽出ではCC BY-SAの継承は発動しないが、文章を持ってくると発動する）。
  成績もこの表からは取らない。DBの `school_championships` と表記が揃わなくなるため。
- **甲子園のスコアを `WebFetch` の要約で取らない。★**
  Wikipediaの大会記事を要約させると**対戦相手も勝敗も入れ替わって出てくる**
  （実際に大社2024が1勝3敗、金足農の3回戦の相手が横浜でなく東海大熊本星翔、
  佐賀北が1回戦敗退、という出力になった）。
  `data/wikipedia-cache/summer-NNN.json` の `wikitext` を直接読むこと。
  勝ち上がり表は**太字が勝者**、`x` 付きがサヨナラ。
  書いたあとは `school_championships` の勝敗数と必ず検算する。
- ★★**全国大会の生成物は JSON にすること**（`koshien-games.json` / `jingu-games.json`）。
  TypeScript のリテラル配列にすると、**2,972件で TS2590**
  （"union type that is too complex to represent"）になり**型検査が通らない。**
  ★**型は読む側（`src/lib/koshien-games.ts` など）で1回だけ与える。**
- ★★**大会記事は「商業」を「商」と略す**（`高松商` `松山商` `広島商`）。
  学校マスタは正式名なので、**完全一致だけでは当たらない** ——
  実際に**高松商業・松山商業・広島商業が甲子園0試合**になっていた。
  ★**寄せるのは 商業→商・工業→工・農業→農 の3つと旧字体だけ。これ以上増やさないこと。**
- ★★**甲子園の試合単位の結果は `src/lib/data/koshien-games.ts`（生成物）。**
  `scripts/build-koshien-games.mjs`（`npm run koshien-games`）が
  `data/wikipedia-cache/` の **`wikitext` を直接読む**（要約に通さない。上の項）。
  ★★**wikitext の形は4つある**（2026-08-26 に2つ足して **2,972 → 6,094試合**・
  **169 → 197大会**になった。**それまで114大会が「決勝だけ」などの虫食い**だった。
  さらに**2大会は別の出典から補って199大会**。下の「補い」の項）:
  ①`{{Round8 seed}}` などのブラケット ②勝敗表（`wikitable`）
  ③**箇条書き**（1930〜1990年代。`readRoundLists`） ④`{{Linescore}}`（決勝）。
  ★**①は太字が勝者。スコアの大小から導かない**（不戦勝はスコアが無い）。
  ★★**①のブロックの最初の `RD1=` にはパイプが付いていない**（テンプレート名と同じ行
  から始まるため）。**パイプを必須にすると、その回戦の試合が全部「回戦なし」になる**
  （実測**1,134試合**。トーナメント表が2回戦から始まり、全員がシードに見えていた）。
  ★★**そこで「名前の無い試合は1回戦だろう」と当てないこと** ——
  1988年春は名前の無い試合に**1回戦と準々決勝が混ざっていた。**
  **当てる前に、読めていない場所が無いかを疑う。**
  ★**詰まったら `KOSHIEN_DEBUG=<大会名の一部>`**（読めた試合を回戦つきで全部出す）。
  ★★**②は「勝利」「敗戦」の列の見出しで勝者の側を確かめてから読む。**
  **回戦は節の見出しだけでなく、表の中の帯（`!colspan="6"|2回戦`）から来ることもある**
  （1985年夏）。★**節を丸ごと正規表現で切り出さないこと** ——
  **表の無い節が次の節の表を飲み込み、別の回戦の試合になる。行ごとに読む。**
  ★★**③は左が勝者。ただし信じるだけにせず、点数の大小と一致することを要求する**
  （食い違ったらその大会を1試合も出さない）。
  ★★**同点は引き分け。** 列の見出しが「勝利」でも `0 - 0` なら引き分けである
  （1958年夏の準々決勝 徳島商 0-0 魚津・延長18回）。**引き分けを「負け」に数えない** ——
  数えると**再試合で負けた側が2敗**になり、大会がまるごと落ちる。
  ★★**ノーゲーム（成立しなかった試合）は括弧でくくって書かれている**
  （`* （岐阜商 1 - 0 甲陽中）（1回裏終了・降雨ノーゲーム）`）。**試合として出さない。**
  ★★**決勝が引き分けだった年は Linescore が2つある**（1969年夏・2006年夏・1917年夏）。
  **決勝の節の中の Linescore を全部読む**（1つ目だけだと「決勝が引き分けのまま」になり、
  **負けていない学校が2校**として大会が落ちる）。★**節の外の Linescore は読まない** ——
  名勝負のスコアを1回戦の節に載せている記事がある（2003年夏・2026年春）。
  ★★**注釈テンプレートは種類を名指しで消さない。** `{{Efn|…}}` だけ落としていて
  `{{Refnest|group="注"|…}}` で2019年春が丸ごと落ちていた。**内側から順に `{{…}}` を消す。**
  ★★**パイプで割る前に `<ref>`・`{{Efn|…}}`・`[[記事名|表示]]` を落とす** ——
  **どれもパイプを含むのでセルが1つずれ、その試合が丸ごと落ちる**
  （第105回のクラーク国際・第75回選抜の花咲徳栄。**1試合欠けると大会ごと落ちる**）。
  ★★**試合がまるごとリンクの中に書かれている行がある**
  （`* [[鹿児島実業対東海大相模延長15回|鹿児島実 5 - 4 東海大相模（延長15回）]]`）。
  ★★**不戦勝は並びが記事によって逆**（`'''勝者'''（不戦勝）||敗者` と `敗者||'''勝者'''（不戦勝）`）。
  **「（不戦勝）が付いているほうが勝者」で読む。** 決め打ちすると**辞退した学校が
  「1度も負けていない学校」として残り、大会が落ちる**（2022年春）。
  ★★**校名の後ろの括弧は2種類**: **同名校の書き分け**（`海星（長崎）2 - 0 海星（三重）`）と
  **試合の注記**（`（延長10回）`）。**4文字以内で注記の語を含まないものだけ校名の一部**
  とする（全記事の行末の括弧33種類を数えて決めた）。
  ★**取り違えても嘘は出ない** —— どちらに倒しても校名がずれて検算に落ちるだけで、
  **別の学校の戦績として出ることはない。**
  ★★**初期の大会には敗者復活戦がある**（第2回・第3回選手権）。
  ★**節の見出し `==== 敗者復活戦 ====` を回戦として読むこと**（`EXTRA_ROUNDS`）。
  読まないと**その試合が落ち、勝ち上がった学校が「前の回戦の勝者でない」ことになって
  大会がまるごと出せなくなる**（1917年夏がそれで丸ごと欠けていた）。
  ★★**優勝校が1敗していることがある**（1917年夏の愛知一中は1回戦で負けてから優勝）。
  **優勝校が敗者復活戦に出ているときだけ「負けなしの学校が0校」を認める。**
  **その試合に出た学校だけ2敗を許す**（全体を緩めない）。
  ★★**注釈テンプレート（`{{Efn`/`{{Refnest`）も、記事全体から先に落とす。**
  **複数行にまたがり、中に `{{Linescore}}` が入っている記事がある**（2026年春）。
  ブラケットの塊が**注釈の中の `}}` で途中で終わり、準決勝以降が丸ごと落ちる。**
  ★**行ごとの掃除では届かない。** 閉じ括弧は数えて探し、**閉じていない注釈は触らない。**
  ★**回戦の切り替えが前の試合の行の末尾に紛れている記事がある**
  （`…|日本文理|0|RD3=準々決勝`）。**その試合はいまの回戦のまま読み、次の行から切り替える**
  （落とすと準々決勝が2回戦として画面に出る）。
  ★★**開催中にキャッシュした記事は、大会が終わったら取り直すこと**
  （`fetch-koshien-wikipedia.mjs` はキャッシュがあれば取りに行かない。**該当のファイルを消す**）。
  ★★**脚注は読む前に落とす。** `<ref>` の中に `{{Linescore}}` が入っている記事があり、
  **ブラケットの塊が脚注の中の `}}` で途中で終わる**（2003年夏は2回戦以降が全部落ちた）。
  ★**閉じない脚注（`<ref name="x"/>`）を先に落とすこと** —— 順番を逆にすると
  **次の `</ref>` まで消える**（2015年夏・2016年夏で31試合が消えた）。
  ★★**キャッシュした記事が「その回」のものか確かめること。**
  `第1回選抜高等学校野球大会` は**第20回（1948年）へのリダイレクト**で、
  第1回・第2回・第6回の選抜が別の大会の中身で保存されていた（`fetch-koshien-wikipedia.mjs`）。
  ★★**参照表（`koshien-tournaments.ts`）に無い大会も読むこと。** 199大会ぶんしか無いので、
  **「無ければ飛ばす」にすると警告も出ないまま欠ける。** 年は記事の Infobox から取れる。
  ★★**外部の一覧と優勝校を突き合わせる検算がある**
  （`scripts/verify-koshien-champions.mjs`。2026-08-26 時点で**197大会すべて一致**）。
  ★**突き合わせ相手（data-man.com）は出典ではない。** 記録の編纂そのものが商品のサイトで、
  **規約に禁止は無くても、試合データの取り込み先にはしない**
  （福岡で `fk-kokoyakyu.com` を採らなかったのと同じ線引き。**運営者の判断で
  「検算相手としてなら使う／自前で埋められない穴が出たら相談」**としてある）。
  ★★**`koshien-tournaments.ts` を「落とす検算」に使わないこと** ——
  **199大会のうち43件で「出場校数 − 1 ≠ 試合数」**になっており、
  2025年春は `30校・31試合` と**算数が合わない**。**ずれは警告に留める。**
  ★**落とす判断は内部で導ける不変条件でやる**:
  **①優勝校以外はちょうど1回だけ負ける ②2回戦以降は前の回戦の勝者**。
  **紙の外の数字を使わないので、参照データの誤りに巻き込まれない。**
  ★★**完全一致でも「同名の別校」に当たる**（2026-08-26）。
  **2003年夏の「金沢」（石川・私立）が、学校マスタの「金沢高校」＝横浜市立金沢
  （神奈川）の戦績として画面に出ていた。**
  ★**記事の「代表校」の表と箇条書きから都道府県を取って生成物に添えてある**
  （延べ12,188チームのうち10,512に県が付く）。**県が分かるときは一致を要求する**
  （`samePrefecture`）。★**取れない古い大会は校名だけで見る。推測で埋めない。**
  ★★**古い大会は代表校の欄が「地区」**（北陸・奥羽・南関東）で県が取れない。
  そこは**同じ校名が他の大会で持っている県を借りる**（`teamPrefecture`）。
  **県が1つに決まる校名だけ**（熊本の城北／徳島県立城北のような組は借りない）。
  実際に**1962年夏の「金沢」（石川）が横浜市立金沢の戦績として出ていた。**
  ★★**この索引はモジュールの読み込み時に作らないこと** ——
  `normalizeKoshienName` が下の `const OLD_KANJI` を使うので、
  **読み込み時に走らせると「初期化前に触った」で500になる**（実際になった）。**遅延で作る。**
  ★★**明治神宮は出典に県が無いので、甲子園の生成物から借りている**
  （`jinguPrefectureOf`。**県が1つに決まる校名だけ**。厳しくする方向にだけ使う）。
  ★**学校への結び付けは完全一致だけ**（`koshienGamesOf`）。
  記事は略称なので**部分一致は別の学校に当たる**（横浜／横浜清陵、市和歌山／和歌山）。
- ★★**自前の出典で埋められない大会だけ、別の出典から補っている**（2026-08-26。運営者の判断）。
  `scripts/build-koshien-supplements.mjs` → `src/lib/data/koshien-supplements.json`。
  いま入っているのは**1938年春・1984年春の50試合**（出典は data-man.com のトーナメント表）。
  ★★**1917年夏はここに入れかけたが、自前の出典で読めたので外した。**
  **「別の出典から補う」の前に、自前を読み切れているかを必ず疑うこと。**
  ★**既定の出典は ja.wikipedia のまま。穴が自前で埋まったら補いのほうを外すこと。**
  ★★**読み取り方は、自前で読めている大会で検算してから使う**（`--selftest`。
  2003年夏の48試合と**1試合も食い違わない**ことを確認済み）。**これが無いと嘘を入れる。**
  ★**補ったぶんは1試合ずつ `source` を持ち、大会ページの注記に出典を出す。**
  ★**検算に落ちた大会は補わない** —— 1917年夏は**敗者復活戦**があり、
  どちらの出典でも「優勝校以外は1回だけ負ける」に収まらないので見送った。
- ★★**明治神宮大会（高校の部）は `src/lib/data/jingu-games.ts`（生成物）。**
  `scripts/build-jingu-games.mjs`（`npm run jingu-games`）。
  出典は**日本学生野球協会**（主催者。規約の記載は本文に無く robots.txt は404）。
  ★★**大学の部を混ぜないこと** —— 同じ日程表に並ぶので、
  `高校`/`大学` の見出しの区切りを持って読む（混ぜると大学名が高校の戦績に出る）。
  ★★**末尾の「高」を落とさない**（`九州国際大高` → `九州国際大` は大学名に見える）。
  **落とすのは照合のときだけ。**
  ★★**Wikipedia「明治神宮野球大会」の歴代結果が非常に強い検算相手**
  （優勝校・準優勝校・決勝のスコア・出場校数の4つを持っている）。
  **出典の外から来る事実**なので「大会をまるごと読み違えた」を止められる。
  ★**突き合わせは部分列で**（出典 `九州国際大高` / Wikipedia `九州国際大付`）。
  ★**入っているのは2013〜2025年**。それ以前は `schedule.php` が空を返すので
  **静的ページ側に別の読み手が要る。**
- ★★**直接対決・通算成績**（2026-08-26）。`src/lib/head-to-head.ts`。
  学校ページの一覧と、2校のページ `/vs/<slugA>/<slugB>`（**slugは辞書順の1通りだけ**）。
  ★**元は自前の試合データだけ**（甲子園・神宮・その県の地方大会）。新しい出典は要らない。
  ★★**「◯勝◯敗」と書かない。両側の勝った数で書く**（「敗戦数を画面に出さない」を守る形）。
  ★**引き分けは別に数える**（引き分け再試合があるので「負け」に混ぜない）。
  ★★**静的生成しない。** 公立どうしの組は7,563あり、「3回以上の組だけ作る」にすると
  **学校ページから張ったリンクの大半が404になる**（学校ページは1回でも当たった相手を並べる）。
  **ISRで作り、sitemap には3回以上の組だけ載せる。**
  ★★**同じ校名の2校がある**（県立/市立の岐阜商業）。**ぶつかったときだけ正式名に落とす** ——
  そのままだと見出しが「岐阜商業高校 vs 岐阜商業高校」になる。
  ★**並びは対戦の多い順。勝率順にしない**（1勝0敗が10戦6勝より上に来る）。
- ★★**全国大会にも大会ごとのページがある**（2026-08-26 に作った）。
  `/koshien`（一覧）・`/koshien/<年-季節>`（190件）・`/jingu`・`/jingu/<年>`（24件）。
  **数え上げは `src/lib/national-tournaments.ts`**（地方大会の `regional-tournaments.ts` と同じ役割）。
  ★**slugはローマ字**（`2025-summer`）。日本語の大会名をURLにしない。
  ★★**画面の部品は地方大会と同じものを使う** —— `toRegionalGames` で `RegionalGame` の
  形に寄せて `RegionalBracket` / `RegionalGameList` / `buildRegionalBracket` に渡す。
  **利用者から見て「大会のページ」は1種類であるべき**で、全国と地方で並びや色を変えない。
  ★★**公立かどうかは `getSchoolNameIndex`（`src/lib/queries/schools.ts`）で引く。**
  生成物は校名の文字列しか持っていない（地方大会と違い slug が無い）ため。
  ★**照合は学校ページ（`koshienGamesOf` / `jinguGamesOf`）と同じ完全一致の規則**にそろえてある。
  **ずらすと「学校ページには出るのに大会ページでは公立扱いされない」食い違いが出る。**
  ★**同じ鍵に2校が当たったらその鍵は捨てる**（同名の県立・市立。当て推量で結び付けない）。
  ★**球場を補わない** —— 甲子園でも第1〜9回は豊中・鳴尾で、出典に球場は入っていない。
  ★**成績は「負けた回戦の名前」で出す**（`3回戦敗退`）。**「ベスト16」に言い換えない** ——
  出場校数が大会ごとに違うので、同じ回戦でも段階名が変わる。
  ★**トーナメント表は組めた大会だけ**（地方大会と同じ。回戦名の無い試合がある大会は組めない）。
- **勝ち上がりは `src/lib/content/tournament-runs.ts`（手書き）。**
  試合単位のテーブルを足すと人がSQLを流す作業が増え、忘れると詳細ページが落ちる。
  `phenomena.slug` と `phenomenonSlug` が対応しているので片方だけ変えないこと。
- ★**公立旋風は甲子園だけではない**（2026-08-20 に地方大会の4件を足した）。
  `level = 'prefectural'` は画面では**「地方大会」**と出す（「県大会」ではない。
  東東京・西東京の大会は県大会ではないし、サイトの他の場所も「地方大会」と呼んでいる）。
  SQLは `supabase/senpu_seed_chihou.sql`（**未適用**）。
  ★**21世紀枠でベスト4の2校（宜野座2001・利府2009）は甲子園の記録**なので、
  地方大会とは別ファイル `supabase/senpu_seed_21seiki.sql`（**未適用**）。
  ★**「21世紀枠でベスト4は2校だけ」は70件を1件ずつ数えた結果**
  （2020年の3校は大会中止・2013年の土佐は私立でマスタに無い、まで確かめてある）。
  **21世紀枠が増えたら数え直すこと。**
  - ★**地方大会の勝ち上がりは、その県で使っている出典からしか作らない。**
    県相模原2019（神奈川）は神奈川高校野球ステーション（地方大会の結果と同じ出典）。
    **出典表示はサイト名で出す**（連盟の名前で出さない）。
  - ★★**小山台2018/2019の勝ち上がりは個人ブログが出典**（2026-08-21。運営者が指定）。
    「わっぱ飯のイン斬り野球」の「温故知新」。小山台OBが観戦した試合の記録で、
    **出典表示はブログ名で出す**。★**取るのは回戦・対戦相手・スコアだけ**
    （観戦記の文章が本体のブログなので**文章は1文字も取らない**）。
    ★**検算は決勝だけ**（Wikipedia の歴代代表校の表と一致）。3回戦〜準決勝は出典が1つしか無い。
  - ★**日野2013に勝ち上がりを付けていないのは資料が無いから。**
    東京都高野連は「データ」の転載を制限しており、上のブログは小山台専門。
    Wikipedia の歴代代表校の表には**決勝のスコアしか無い**。
    **1試合だけ並べるとその1試合しか戦っていないように見える**ので節ごと出さない。
  - ★**見出しは `TournamentRun.heading` で分ける。** 既定の「甲子園での勝ち上がり」の
    ままだと、**地方大会の準々決勝が甲子園の準々決勝に見える**。
- **動画は権利者の公式チャンネルのものだけ埋め込む。**
  判断は `isEmbeddableVideo()` の1か所に閉じ込める。テレビ放送を個人が
  上げたものは、埋め込めても侵害動画と分かって案内すると幇助になりうる。
- **新聞社のスコアページからスコアを取らない。★**
  一球速報.com とバーチャル高校野球は**利用規約**で外してある（著作権ではない）。
  バーチャル高校野球は第8項(3)が営利目的の利用を禁じており、**目視での転記も塞がる**。
  「人が読んで渡せば規約に触れない」は**この2媒体には通用しない**。
  使えるのは Wikipedia・**運営者自身のスコアブック**・市販の記録集・**日本高野連公式**。
  ~~高野連は当年のみ~~ は誤り（2026-08-12 訂正）。夏の出場校一覧は第1回から、
  日別の試合結果は2008年からある。規約もサイトポリシーも無く robots.txt は404。
- **結果速報の出典は日本高野連**（2026-08-13 にWikipediaから切り替え）。
  `scripts/build-live-results.mjs`。**次戦の日付・第何試合・開始時刻が出せる**のが理由。
  - **日付を推測しない。** `/sensyuken/<年>/schedule/` の一覧に載っているリンクだけを辿る
  - **次戦の出典は2つ**（2026-08-13 追加）。日別ページは**日程が発表された日ぶんしか無い**ので、
    決まっているのに出せない次戦がある（鳴門渦潮の8/14がそうだった）。
    `/sensyuken/<年>/tournament/`（組み合わせ表）から**3回戦までの対戦カードを補う**。
    **日別ページ由来を必ず優先する**（組み合わせ表には開始時刻が無い）。
    補ったぶんは `next.provisional: true`。詳細はREADMEの「次戦の出典は2つある」
  - **準々決勝以降は補えない。** 勝ちチーム主将のくじ引きなので本当に未確定
  - **同じ地区で2件以上に当たったら結び付けない**（岐阜商業のように県立と市立が同名）
  - ★**県大会で県外の学校に結び付けない**（2026-08-14 修正）。地方大会の照合には
    「県内で引けなければ全国で引く」受け皿があるが、これは**地区大会の県外の相手**用。
    県大会にも効いていたため、その県の私立が県外の公立に化けていた
    （愛知「愛知」→滋賀県立愛知、熊本「城北」→徳島県立城北など計28試合）。
    ★**この誤りは警告に出ない。** 検算も校名の照合も通る。出典を足したら
    「結び付いた学校の県が大会の県と違わないか」を1度は突き合わせること
  - **サヨナラは出典によって書き方が違う。** 高野連はイニングのセルに大文字 `X`（`1X`）、
    Wikipediaは合計スコアに小文字 `x`。`Number("1X")` は NaN になる。
    **イニングスコアから推測で導かない**
  - その年のページが無い時期は**例外を投げず、生成物を書き換えずに終わる**
    （投げるとCIが3時間おきに失敗し続ける）
  - **春（選抜）は未対応。** `/senbatsu/` で回数は `年 - 1928`（夏は `年 - 1918`）
- **地方大会は `scripts/build-regional-results.mjs`**（2026-08-13 実装。長野・神奈川・
  埼玉・山梨・徳島・熊本・群馬・佐賀・奈良・愛媛・新潟・**愛知**・**京都**・**広島**・**三重**・**鹿児島**・**石川**・**岐阜**・**千葉**・**山形**・**静岡**・**山口**・**宮崎**・**福井**・**和歌山**・**滋賀**・**兵庫**の27県
  ＋ **2026-08-20 に 茨城・岡山・香川・高知・長崎**（方針変更）**・島根・岩手**（連盟以外の出典）**・大分・栃木**
  ＋ **2026-08-21 に 沖縄**（3季163試合）**・福島**（2季89試合）。**計38県**
  ＋ 同日に**山口の秋季**（48試合）と**大分の春季**（7試合）。**栃木は既に3季あった**
  ＋ **2026-08-21 に 福岡**（春夏213試合。**連盟以外の出典＝HSB flash**）。**計39県**
  ＋ **2026-08-24 に 富山**（夏35試合）。**計40県**
  ＋ ★★★**2026-09-01 その5 に 富山を 72 → 370試合**（2 → 9大会・2 → 8年）。**春季・秋季を足した。**
  ★~~春季・秋季は紙の形が違うかもしれないので測ってから足すこと~~ →
  **紙の形は3季ともまったく同じだった**（石川と同じで、開いたら違わなかった）。
  ★★**春季・秋季は「年度ページ」（`?page_id=NNNN`）から取る** ——
  トップに**２０１２年度〜２０２４年度**が並ぶ。**`--year` で遡れる。**
  ★**夏は今までどおりお知らせ（`?cat=5`）から取る**（既に読めている2大会を動かさないため）。
  ★**回数から年**: 春季 **+1928**／夏 +1918／秋季 **+1947**（2018〜2024年の14枚で確認）。
  ★**必ず外すもの**: **軟式**と**北信越地区大会**（他県の学校が出る。同じページに並ぶ）。
  ★★**春季・秋季は大会名の頭に西暦を足す**（宮崎・愛知と同じ）——
  **試合の日付を1つも持たない**ので、足さないと**7大会が全部「年の分からない大会」に落ちる**（実際に落ちた）。
  ★**夏には足さない**（`第N回…選手権` から年が導ける。足すと引き継ぎの鍵が変わるだけ）。
  ★★★**あわせて公開中の誤り4件を直した** ——
  `高岡商業 3-5 高岡`（紙は 3-1）・`南砺福野 0-0 不二越工業`（紙は 7-0。**5回コールド**）・
  `富山第一 10-9 富山中部`（10-0）・`富山北部 6-4 魚津`（6-1）。
  ★**回戦の列の間隔が 22 しかない**ので、得点の既定の窓（内側へ 32）が**隣の回戦の列まで届く。**
  **高さで先に選ぶと隣の回戦の得点のほうが「枝の中ほど」に近いことがある。**
  ★**窓を締める案は捨てた**（得点が列から離れている試合が読めなくなる。壊れが 0 → 7 に増えた）。
  ★**`scoreNearestColumn` で「列に近いほうを高さより先に見る」に変えた**（既定 `false`）。
  ★★**「勝った側の得点のほうが少ない（か同点の）試合があれば1試合も出さない」を検算に足した** ——
  **勝った側は枝の色から、得点は数字から**と別々の場所から来るので、食い違えば読み違えている。
  ★**第108回（2026夏）は準々決勝で終わっている** ——
  **紙の準決勝・決勝は日程だけで得点が無い**（連盟が完成版を出していない）。
  ＋ ★★**2026-08-25 に 大阪**（**15大会2,231試合**・2021〜2025年）。**計41県**
  **規約で外していた6県以外で最後に残っていた県。**
  ＋ ★★**2026-08-26 に 群馬を出典ごと入れ替えた**（19 → **1,292試合**・2006〜2026年）。
  それまで読んでいた日別ページは**3回戦以降の15試合しか公開していない**（`partial: true`）が、
  **「過去の試合結果」（`99_blank.html`）に平成18年からの勝ち上がり表PDFが並んでいた。**
  1枚に全試合（夏なら59チーム58試合）が載っており、**日別ページの4倍**が1枚で取れる。
  ★**紙の形は広島・三重と同じ左右2段組のスロット格子型**（`readTwoColumnBracket` ではなく
  `gunma.readSheet` に独自実装。窓の測り方が違う）。**詳しくはアダプタ冒頭のコメント。**
  ★★**決勝の2つの得点を半分ごとの組み立てに入れないこと** ——
  決勝は中央に左右1つずつ、**境目のすぐ内側**に置かれる。渡したまま組むと
  **半分の準決勝の得点が決勝の得点にすり替わる**（2025年夏は準決勝 12-0/2-17 が 12-4/2-3 になった）。
  ★**検算（チーム数−試合数=1・優勝校）はどちらも通ってしまう。**
  先に決勝を読み、**その x より外側だけ**を半分に渡す。
  ★**得点に括弧書きの注記が付く**（`8(7ｺ)` `(5ｺ)14` `(延10)9`）。`stripScoreNotes()` で落とす。
  **注記は左にも右にも付く**ので、残った数字の**断片内での位置を測り直す**こと
  （素通しすると別の回戦の帯に落ちる）。★**括弧が断片をまたいで割れている**こともある（`(7`／`ｺ`／`)9`）。
  ★**日別の結果ページ（別の場所にある出典）と突き合わせた** ——
  10大会104試合で**対戦の組み合わせは104/104が一致。**
  ★**スコアだけ2件、連盟自身の2つの資料が食い違う**
  （2024夏3回戦 明和県央 9-2/9-1 渋川、2024秋準々決勝 前橋商 0-7/0-6 健大高崎）。
  **出典どうしの不一致**なので、引用元（勝ち上がり表）のまま出している。
  ★**45枚中20枚が成立。** 落ちるのは**不戦勝のある紙7枚**（得点の無い枠が混ざると
  「その回戦の数字が試合数の2倍」が崩れ、**組めてしまうほうの壊れ方**をする）と、
  **平成22〜24年ごろの帯が半分ずれる紙**、**「優勝」が刷られていない紙3枚。**
  ★★**「やぐら表」＝勝ち上がりがそのまま刷ってある表**（`scripts/lib/yagura-bracket.mjs`）。
  **枝を推測しない** —— 紙が**勝った学校を次の列にもう一度刷る**ので、
  組み立てに要るのは「同じ列の隣どうしが対戦相手」だけ。
  ★**`slot-bracket.mjs`（座標から組む）とも `vector-bracket.mjs`（線を読む）とも別物。**
  ★**規約は無い**（`robots.txt` 404・転載/無断/複製/営利/著作/引用のどの掲示も無い）。
  ★★**フレーム構成なので外側だけ見ても分からない。** 中のフレームまで開いて確かめること。
  ★★**検算のうち2つが枝の外から来る**:
  **①紙に刷ってある「チーム数」＝出場校数、試合数＝チーム数−1**、
  **②連盟自身の歴代表**（`4-kako-kiroku/sensyuken-osaka.html` ほか）が
  **優勝校・準優勝校・決勝スコア・準決勝進出校の4つ**を持っている。
  2大会とも4つすべて一致した。
  ★★**15枚すべてが検算を通る**（過去5年ぶん×3季）。
  ★**落ちた大会は1試合も出さない**（石川の「構造は合うのに相手が違う」と同じ轍を踏まない）。
  ★★**紙の形は年で変わる。読めるようにした5つを、次に触る人のために残す**:
  ①**枝の括弧 `[` `]` が「線」ではなく「文字」**（2021〜2024年の夏）。
  素通しすると `]高石` になり**勝ち上がりが全部つながらない**（2024年夏で93件）。
  ②★★**不戦勝は得点欄が `○`／`×`**（2021年秋は**30試合**・2021年夏は0試合）。
  **枝の枠は使うが試合は行われていない**ので、**検算には数え、画面には出さない**
  （0対0にしないこと。島根で踏んだ轍）。
  ③**3位決定戦は「ラベルより下の中央の帯」だけ外す。**
  **ラベルより下にも本物の枝の行がある**（2021春は5行・2023秋は9行）ので、
  **丸ごと外すと3枚で列が奇数になる。**
  ④★**得点→校名の隙間（最大31pt）と、校名の中の隙間（13pt以下）は別もの。**
  同じ値で見ると、**校名の51pt手前にある1文字を拾って `ア太成学院大高` になる**（2021年夏）。
  ⑤**校名が2〜3行に折り返される**（連合チーム）。得点は**行のあいだ**に置かれる。
  ★**行の幅を広げて組み直さないこと** —— 側の判定が狂って左半分の34件が落ちる。
  ★**続きの行かどうかは「上の行の幅にぴったり収まるか」で決める**（はみ出しは2ptまで）。
  20pt 緩めたときは**別の学校の校名を巻き込んだ。**
  ⑥★★**不戦勝は3通りに刷られる。** どれも**枠は使うが試合は行われていない**ので、
  **検算には数え、画面には出さない**（0対0にしない）——
  **○と×の対**／**○だけで相手の欄が空**（2023年秋。相手は校名しか刷られていない）／
  **×と×**（2021年秋に3組。**両校とも出場取りやめで誰も上がらない。**
  紙はその先に**校名の無い×**を置いて辻褄を合わせている）。
  ★**検算の式は 出場校 ＋ 相手の欄が空の不戦勝 − 両校取りやめ ＝ 紙のチーム数。**
  ★**2021年秋は不戦勝が30試合**あり、これが同年秋が大きく壊れていた正体。
  ⑦★**字が壊れて読めない得点が1件ある**（2023年春）。
  **枠を無いことにすると列が奇数になって以降が全部ずれる**ので、
  **数字の列にぴったり載っている1文字だけ**を「読めない枠」として拾い、
  **勝者は次の列にどちらがいるかで決め、その試合は画面に出さない。**
  ★**文字を名指しで消さないこと**（別の紙で別の字が出たときに効かない）。
  ★**次に触る人は紙を1枚ずつ・年ごとに測り直すこと。**
  ★**この紙に試合ごとの日付は無い**（会期だけ）。`date: null` で出す。
  ★**`大教大池田`・`大教大天王寺` は結び付けない** —— 大阪教育大学附属は
  キャンパスごとに別チームで**同じ大会に両方出ている**が、
  学校マスタは1件しか持たないので、当てると**2チームが1校になる。**
  ★★**PDFに「描いてある枝の線」から読む最初の県**（`scripts/lib/vector-bracket.mjs`）。
  **座標から推測して組み立てる `slot-bracket.mjs` の条件を満たさない**ので
  長く「取れない県」に置いてあったが、**枝が線として描いてあった**（福岡のPDF版）。
  ★**線は `stroke` ではなく `eoFill`**（塗りつぶし図形）。太さ 2.52pt で、
  **「2pt未満」で絞ると1本も当たらない。**
  ★**赤と黒はぴったり接するとは限らない** —— **合流点に横線が入ると太さぶん空く**
  （許容を 1.2pt にすると右半分の8試合が1つも組めない）。
  ★★**回戦は「深さ」ではなく列の位置で決まる。** 抽選シードがあるので、
  深さで数えると**「2回戦から出た2校の対戦」が1回戦になる**（実際になった）。
  ★★**準々決勝までしか紙に入っていない。** 準決勝・決勝は出所が違うので
  `src/lib/content/regional-supplements.ts`（**手書き**）にある。下の項を読むこと。
  ＋ ★★★**2026-09-01 その5 に 群馬を 1,559 → 1,879試合**（25 → 30大会・14 → 17年）。
  **直したのは1か所** —— ★★**深い回戦どうしの列の間隔が、浅いところより狭い紙がある。**
  `1回戦 166.0 → 2回戦 196.9 → 3回戦 222.6 → 準々決勝 253.6 → **準決勝 264.6**`（間隔 31/26/31/**11**）。
  ★**帯をまとめる幅の既定は「1つ前の帯との間隔の 0.45 倍」**なので準々決勝で 13.95 になり、
  **11 しか離れていない準決勝の帯を飲み込む。**
  ★**数字は「中点から3スロット以内」で落ちるので準々決勝自体は正しく組める** ——
  **壊れるのは `lastY`** で、次の段の候補がその帯より下に限られ、
  **準決勝が消えて「第5段: 帯が見つからない」**になる。
  ★**症状は「半分の組み立てに失敗した（×/○）」＝片方の半分だけ落ちる。**
  ★**`roundBandGap: 8` を渡して直した**（静岡の春季で足した引数がそのまま効いた）。
  **45枚を読み直して、既に読めていた20枚が1試合も変わらないことを確認済み。**
  ★**入ったのは 2011春・2012春・2015春・2015秋・2020秋。**
  ★★★**2011年夏は追わないこと** —— 2回戦の列に **`#` が3つ**あり数字が13個しか読めない（必要16）。
  **0〜9のどの数字も他の場所には出ている**ので、**`#` が何かは決められない。**
  ★**2016年（夏秋）・2017年は出典に紙が無い**（一覧に「h29-1年」しかない）。
  ★**残りは 不戦勝3枚（勝った側が紙に無い）・優勝校が刷られていない2枚・半分の組み立て3枚。**
  ＋ ★★★**2026-08-31 その2 に 石川へ春季・秋季を足した**（362 → **958試合**・8 → 22大会）。
  ★★**「春季・秋季は準決勝以降が箱スコアに切り替わるので読み方が2つ要る」は誤りだった。**
  **紙を開いたら県大会の紙は3季ともまったく同じ形**で、**`seasons` に足しただけで読めた。**
  ★**滋賀の「スロット番号の行が無い」と同じ形の誤り** ——
  ★★**「読み方が2つ要る」と書いてあっても、紙を開いて確かめること。**
  ★★★**代表決定戦を出さない**（`SKIP_ROUNDS`）。北信越の県大会は**3校が地区大会へ進む**ので、
  **準決勝 → 代表決定戦 → 決勝** と紙に刷ってある。**見出しを知らないと `round` が
  「準決勝」のままになり、準決勝が3試合**になって大会が丸ごと落ちる。
  ★**読んだうえで出さない**（枠の検算は通す）。**件数と中身をログに出すこと。**
  ★★★**「優勝」の行が紙に2つある**（`優勝 9:00` と `優勝 小松大谷`）。
  **先に見つかったほうを採ると優勝校が `9:00` になる。**
  ★**落とすのは時刻と裸の数字だけ。「校名らしいほう」を選ばないこと** ——
  **決勝の勝者と突き合わせる検算が骨抜きになる。**
  ★**候補が2つ以上残ったら「読めなかった」として扱う**（当てない）。
  ★**索引（`?page_id=29`）は1998年からある。2007〜2012年だけ1本も無い。**
  残りが落ちる理由は README の表にある（校名の行が読めない／回戦か日付が分からない／読めない枠）。
  ＋ ★★★**2026-08-31 その2 に 愛知2017年春**（+51試合）。
  **決勝の横線が左右とも勝ち色で、しかも真ん中が刷られていない紙**があり、
  **色でも接点でも勝った側が決まらない。**
  ★★**垂れている縦線（stem）が勝った側の目印**（`vector-bracket.mjs` の `finalByStem`）。
  ★**推測ではない** —— **読めている 2024年春の紙も同じ縦線を色の変わり目に垂らしており、
  そこへ届いている側が優勝校**（享栄）だった。2017年春は真ん中の黒が無いだけ。
  ★**既定は `false`。渡さなければこの道は通らない**ので富山も他の年も変わらない
  （2018〜2022年を再生成して合計が動かないことを確認済み）。
  ★★**決勝の得点の窓（`finalScoreReach`）は広げないこと** ——
  2026-08-30 に広げたら**2024年春の決勝が別の数字に静かに入れ替わった。**
  stem で読んだ決勝にだけ効く `finalScoreReachStem` を渡す。
  ＋ ★★★**2026-08-31 その2 に 岐阜の春季**（+23試合）。**3位決定戦を外して初めて通った。**
  ★**この出典は回戦を書いていない**ので、**紙から「3位決定戦」と読むことはできない。**
  ★★**勝ち抜きの性質から決める** —— 優勝校以外は**ちょうど1度だけ負ける**ので、
  **両校ともそれ以前に負けている試合は枝ではない。**
  ★**外すのは、外した数がちょうど辻褄を合わせるときだけ**（1つでも余れば1試合も出さない）。
  ★**引き分けは負けに数えない**（引き分け再試合があるため）。
  ★**岐阜の秋季は出典自身が合計を打ち間違えている**（`郡上 0 4 0 0 0 4 1 0 0` の計が `11`）。
  **読み違いではない。** 大会が終わってから連盟が直しているかを見ること。
  ＋ ★★★**2026-08-31 その3 に 石川を 958 → 1,416試合**（22 → 32大会・12 → 15年）。
  **直したのは紙の読み方5つ。どれも実データで突き止めてある。**
  ★★★**①縮尺は「枠と枠の間隔」で測る**（`frameGap / 192`）。
  **枠の間隔と合計の位置の比はどの紙でも 0.87 前後**（192対167／266対231／379対332）。
  ★**それまでの「枠の左端から 120〜200 の数字のいちばん右」は、
  枠の広い紙では各回の得点を拾って縮尺が 0.88 になる**（本当は 1.39）。
  **校名の窓がずれて「校名の行が読めない」で大会が丸ごと落ちていた。**
  ★**合計の窓（110〜210）も縮尺に掛けること** —— 他の窓は最初から `win()` を通しており、
  **ここだけ素の値だった。**
  ★★★**②決勝の見出しは `決勝` と `決勝戦` の2通りある。**
  ★**知らないと `round` が前の見出しのまま**になり、
  **2018年春の決勝が「準決勝」として画面に出ていた**（訂正済み）。
  ★★**③代表決定戦の見出しは3通り**（`代表決定戦` / `第3代表決定戦` / **`第3代表、第4代表決定戦`**）。
  **名指しせず末尾で見る**（`(代表決定戦|位決定戦)$`）。
  ★★★**④引き分けは「同じ回戦・同じ顔合わせが後日にある」ことで確かめる。**
  ★**紙の注記に頼らないこと** —— **同じ県で「引き分け」と書く年と `(延長15回)` としか
  書かない年がある。** ★**対の無い同点は認めない**（空欄を 0 と読んだ結果であることが多い）。
  ★**検算は「チーム数 − 1 ＝ 決着した試合」**（岐阜と同じ数え方）。
  ★★**⑤不戦勝は枠の中に文で書いてある**（`北陸学院 と 鵬学園の試合は、鵬学園の不戦勝`）。
  **枠ごと飛ばす**（0対0にしない）。★**辞退した側はその大会に出てこなくなるが検算は崩れない。**
  ★★**注記の行は枠の3行下にあることもある**（`天候不良により中止　順延`）。
  **+2 までしか見ていなくて大会が丸ごと落ちていた。**
  ＋ ★★★**2026-09-01 その4 に 石川を 2,105 → 2,354試合**（46 → 51大会・23 → 24年）。
  **新しく入ったのは 2008年春・2010年夏・2011年春・2011年秋・2012年夏**の5大会。
  ★★★**あわせて日付が1日ずれていた10試合を直した** ——
  **日付の見出しは1行に2つ以上あり、枠ごとに割り当てる**（`dateBands`）。
  `３回戦(188) 準々決勝(476)` の下に `7月17日(15.5) 7月21日(403.9)` と並び、
  枠は `◆(15.5) ◆(209.7) ◆(403.9)`。**行をつないで最初の日付だけを採っていた。**
  ★★**「いちばん近い見出し」ではなく「枠の左にある最後の見出し」を採ること** ——
  真ん中の枠は左右どちらの見出しからも **194.2 でちょうど同着**になり、
  **並び順まかせで準々決勝の日が3回戦に付いていた。**
  ★★★**同じ字を同じ場所に2度描いてあるPDFがある**（2007年春。太字の重ね打ち）。
  **得点は和が倍になって落ち、`◆` の見出しは枠が4つに見えて1つ目の試合が2回出る。**
  **同じ位置の同じ字は1つ。**
  ★★★**各回の欄は等間隔。並びが途切れたらそこから先は各回ではない**（2012年夏）——
  7回コールドの枠に**合計と同じ数字がもう1つ**刷ってあり、和が倍になっていた。
  **欄の間隔の3倍を超えて空いたら打ち切る。**
  ★★**読めない字の欄（`#`）がある**（2008年春）——
  **画面に出すのは合計だけ**なので、**その行だけ「各回の和が合計を超えない」までゆるめる。**
  ★**不戦勝の文の読点は年で違う**（`の試合は、◯◯の不戦勝` / `の試合は◯◯の不戦勝です。`）。
  ★**開始時刻には「第何試合」の丸数字が付く年がある**（`優勝 ③14:00`）。
  ★★**いちばん右の枠にも右端が要る**（2010年夏の決勝は1枠だけで、右に注記が刷ってある）。
  **枠の幅は紙の中で一定**なので、**次の枠が始まる位置で切る。**
  ★★★**「1つの学校が負けるのは1度だけ」を検算に足した** ——
  **2007年春の紙は第1試合の枠を第2試合の場所にもう一度刷っており**（投手まで同じ）、
  **しかも決勝の枠に `◆球場` が無くて読めていない**ので、
  **「余分な1試合」と「足りない1試合」が打ち消し合って
  「チーム数 − 1 ＝ 決着した試合」を通ってしまう**（実際に通った）。
  ★**これで2007年春と2009年夏（鶴来が2敗）が落ちる。どちらも紙が壊れている。追わないこと。**
  ★★**一度書き出した大会は引き継ぎで残り続ける**ので、
  **落とすようにしたら生成物からその大会を消してから走らせ直すこと。**
  ★**残りは 1999〜2002・2000夏秋・2003春・2004秋・2005秋・2006夏・2007夏秋・2008秋・2009秋。**
  ~~★残りは 2005〜2012年（2007〜2012年は出典に1本も無い）~~
  → ★**どちらも誤り**（2026-09-01 その3・その4）。**索引の見出しの決め打ちをやめたら紙はあった。**
  ＋ ★★**愛知の2015・2016年について、前の見立ては誤りだった**（2026-08-31 その3 に確かめた）。
  ★~~勝った校名を回戦ごとに刷り直す形式~~ → **刷り直していない**（校名は外側に1度だけ）。
  ★★**本当の違いは「1枚に4つの山（【Ａ】〜【Ｄゾーン】）が入り、中央で優勝に収束する」こと。**
  枝は `eoFill` の赤黒なので `vector-bracket.mjs` は使えるが、**スロット番号の列が無い**ため
  `readTwoColumnSheet` の入口に入れない。★**`teams` を渡さない道（富山と同じ）に寄せれば読める見込み。**
  ＋ ★★★**2026-08-31 その4 に 石川を 1,416 → 1,558試合**（32 → 35大会・15 → 16年）。
  ★★**回戦の見出しが1行に2つ並ぶ紙がある**（2021年夏。`３回戦`(x=188) と `準々決勝`(x=476)）。
  **行をつなぐと `3回戦準々決勝` になってどの見出しにも当たらず、回戦が混ざる。**
  ★**見出しは自分の枠のかたまりの中央に刷ってある**ので、**枠にいちばん近い見出しを当てる。**
  ★**断片が全部見出しのときだけこの道を通る**（見出しが1つの紙は今までどおり）。
  ★★★**不戦勝は「上がった側」まで紙に書いてある**（`遊学館 と 星稜の試合は、遊学館の不戦勝`）。
  **枠を飛ばすだけだと勝ち上がりの検算に落ちる**ので、**検算のほうに不戦勝を教える。**
  ★**枠の見出し（`◆石川県立野球場第2試合`）を先に落とすこと** ——
  残すと外側の `(.+?)` が飲み込んで**1校目が `◆…第2試合遊学館` になる。**
  ★★**チーム数の検算は「出場校 − 1 ＝ 決着した試合 ＋ 不戦勝」**。
  **辞退した側が1試合も戦っていない年がある**ので、**不戦勝の2校も出場校に数える。**
  ★**「優勝」の候補は括弧で始まらない**（`優勝　（春は17回目）` と刷る紙がある）。
  ＋ ★★★**2026-09-01 その2 に 石川を 1,558 → 1,764試合**（35 → 39大会・16 → 18年。2003年まで）。
  **直したのは読み方4つ。★どれも「◆の位置は枠の中身と揃っていない」の系統。**
  ★★★**①各回の得点の下限は「枠の左端そのもの」**（それまで「枠の左端から34×縮尺」）——
  **同じ行の2つの枠で ◆ と校名のずれが 19 ポイント違う紙があり**
  （`◆(59) 金沢商(77)` と `◆(457) 金高専(456)`）、**縮尺2倍の紙では窓が 38 動く。**
  **右の枠だけ1回表が和に入らず「イニングの和が合計と合わない」で大会が落ちていた。**
  ★**下限は目安でしかなく、箱スコアを外しているのは上限のほう。**
  ★★★**②校名は「窓」ではなく「枠のあいだ」で拾う** ——
  **校名は枠の中で中央に組まれる**ので**左端は字数で 68〜98 とばらつく**が、
  **中央（左端＋幅の半分）はどの行でも同じ位置**（実測 178.9/313.3/558.1/692.5）。
  ★**あわせて「校名らしさ」を決めた**（括弧・数字を含まない／漢字か仮名を含む）——
  これで**バッテリー段の縦罫 `｜`**（2000年は `｜ vs ｜` と読んでいた）も**サヨナラの `x`** も落ちる。
  ★★★**③校名の行は枠ごとに探す** —— **左右の枠で行が違う紙がある**
  （右の枠の校名が、左の枠のバッテリー段と同じ行にある）。**打ち切りの判定も枠ごとに。**
  ★★★**④枠がページのいちばん下にあると、校名の行は次のページの頭にある。**
  **校名を探すときだけ次のページの頭を継ぎ足す**（枠の走査に入れると同じ試合を2回読む）。
  ★**中止・順延の注記が枠のあいだに1つだけ刷ってある紙**は、
  **数字が1つも無い枠にだけ**効かせて枠ごと飛ばす。
  ★**詰まったら `ISHIKAWA_DEBUG=1`。**
  ＋ ★★★**2026-09-01 その3 に 石川を 1,764 → 2,105試合**（39 → 46大会・18 → 23年。1998年まで）。
  ★★★**索引の見出しの決め打ちをやめたのがいちばん効いた** ——
  **年で書き方がまるで違う**（`第94回全国高校野球選手権石川大会` `92回選手権石川大会（夏）`
  `118回北信越県大会(春)` `116北信越石川県大会(春)`）。
  **「全国高等学校野球選手権石川大会」で決め打ちしていたので 2005年と2007〜2012年の紙を
  1本も開いていなかった**（「出典に1本も無い」は誤り）。
  ★★**紙の中の大会名はどの年もそろっている**ので、**索引は「どのPDFを開くか」を決めるだけでよい。**
  ★★★**夏と春秋の見分けは `石川大会`（夏）と `県大会`（春秋）** ——
  北信越の**本大会**は `第126回北信越大会（福井県開催）` で `県大会` を含まない
  （`県開` であって `県大` ではない）。
  **`118回北信越県大会(春)` と `118回北信越大会(春)` は「県」の1文字しか違わない。**
  ★**季節の印は `（春季）` だけでなく `（春）` もある。**
  ★★★**得点が1つの断片に潰れている紙がある**（2007〜2012年。`0 7x` `13 36 13`）。
  **そのイニングが和に入らず大会が丸ごと落ちていた。**
  **位置は断片の幅を文字数で割って出す**（滋賀の `explodeNumberRuns` と同じ）。
  ★**割り方が正しいことは検算そのものが示す**（`8+13+36+13` が印刷された合計 `70` と一致）。
  ★**サヨナラの印が `1 ×`（空白＋全角）の年がある**（2013年以降は `6x`）。
  ＋ ★★★**2026-09-01 その3 に 愛知の2017年秋**（+51試合）。**得点が全角の紙があった** ——
  **`\d` は全角に当たらない**ので「壊れ」として大会ごと落ちていた。
  **同じ行に半角と全角が混ざる**（`６(148.8)` と `8(393.3)`）。
  ★**`vector-bracket.mjs` の `digits` で半角に寄せる。**
  **拾えるようになるのはこれまで1つも読めていなかった全角だけ**なので、
  **富山も既存の愛知も1試合も変わらない**（集合で確認済み）。
  ★**`slotLineTol` を足した**（既定 4 のまま。枝の線と校名の行のずれは紙で違う）。
  ~~★★★愛知の2015・2016年の春季は「壊れ0」にしてはいけない~~
  → ★**2026-09-01 その4 に片づけた**（次の項）。
  ＋ ★★★**2026-09-01 その4 に 愛知の2015年春季**（+51試合。1,831 → **1,882試合**・20 → 23大会）。
  **あわせて公開されようとしていた誤りを2か所直した。**
  ★★★**①2016年秋の生成物に実在しない校名が4件あった**
  （`愛知啓成 10-2 津島・愛知啓成` と、**同じ学校どうしの試合**にまでなっていた）。
  **`readNoSlotSheet` が `slotLineTol` を渡しておらず**、既定の 4 に対しこの紙は
  **枝の線と校名の行が 5.8 ずれる**ので、**そのずれた行が連合チームの2行目と見なされていた。**
  ★★★**②2017年秋の準決勝が `愛産大三河 9-3 中京大中京` になっていた**（紙は **9-5**）。
  **得点の窓が内側へ 32 ポイント開いていた**ので、
  **紙の中央に横書きされた「３位：中京大中京」の ３**（x=242.4）を拾っていた。
  ★★**得点は「列の外側」に刷ってある紙がある**（`scoreAhead`）——
  2016年春の左half は**列より 4〜5.6 左**（2桁は 9.1 まで外へ出る）、
  2016年秋の左half は**列より 2.4 右**。**同じ県・同じ形の紙でも左右どちらにも出る。**
  ★★★**決勝の4つ目の形は「勝ち色と負け色なのに真ん中が空いている」**（`finalColorGap`）。
  **色が刷ってあるので勝った側は色で決まる。足りないのは「接していること」を求めない、それだけ。**
  ★**`finalByStem`・`finalByScore` はどちらも「左右とも勝ち色」の紙のためのもので、ここには当たらない。**
  ★★★**1試合の2つの得点は、合流点をはさんで同じだけ離して刷ってある**（`scorePairs`）。
  **「行のそば」でも「合流点のそば」でもない** —— 2016年春の準決勝は行のそば、
  2017年秋の3回戦は合流点のそば。**揃っているのは離れかたが同じことだけ。**
  ★**要るのは、準決勝の枝が決勝の行まで伸びていて、
  決勝の得点が準決勝の窓に入り x もほとんど同じだから**（実測 0.3 差）。
  ★**「自分の行から N 以内」という条件は試して捨てた**（2017年秋が30試合読めなくなる）。
  ★★**紙の中央の縦書きの優勝校と突き合わせる検算を足した**（検算4）——
  **2015年の記事の本文には優勝校が書かれていない**ので、枝の外から来る事実はそこにしか無い。
  ★**「優勝の次が校名」と読まないこと**（書き方が年で違う）。**列の中に出てくるかだけを見る。**
  ★**読むのは「同じ x に3つ以上の断片が並ぶ列」だけ** ——
  2017年秋の紙は同じ帯に結果を**横書き**で刷っており、つなぐと字の列になって正しい大会まで落ちる。
  ★★★**2016年春季は追わないこと。紙に準決勝の得点が2つ刷られていない** ——
  **紙の数字は 100 個、51試合ぶんには 102 個要る。** 足りない2つは右半分の準決勝。
  **左半分の準決勝には両方ある。組み立て自体は正しくできている**（優勝 享栄 は中央の縦書きと一致）。
  ★**この紙の2〜11ページ目は箱スコア** —— 2015年春は**10試合が1点も違わず一致した**。
  **次に触る人は2ページ目以降を必ず見ること。**
  ＋ ★★★**2026-09-01 その2 に 愛知の2016年秋**（+51試合）。**スロット番号の列が無い紙**
  （`readNoSlotSheet`）。**枝は `eoFill` で読めるのに、入口で落ちていた。**
  ★**校名の欄も中央も縦線の列から測る。**
  ★★★**決勝は左右とも勝ち色で、真ん中の 34.4 の空きに優勝校が縦書き**
  （`優勝 中京大中京`）。**色でも stem でも勝った側が決まらない。**
  ★★**しかも3位決定戦のほうは赤と黒が接している**ので、
  **色で決まる `findFinal()` がそちらを決勝として拾う**（実際に拾った）。
  ★★★**止めたのは枝の形から決まる条件** ——
  **決勝は「いちばん深い回戦の勝者2校の対戦」でなければならない。**
  3位決定戦に出るのは準決勝で負けた2校なので必ず落ちる。
  ★**勝った側は得点で決める**（`finalByScore`。**同点・読めないときは決勝を出さない**）。
  ★**決勝の得点は空きをまたいで置かれる**ので `finalScoreSpan: 55`。
  ★**`finalByScore` は既定 `false`。渡しているのはスロット番号の列が無い紙だけ**なので、
  **富山も、スロット番号のある愛知の年も1試合も変わらない**（集合で確認済み）。
  ＋ ★★★**愛知の2015・2016年は1枚ずつ作りが違う**（2026-08-31 その4 に調べた。**2016年秋は収録済み**）。
  ★**2016年秋だけは 51試合・52チーム・壊れ1 まで行く。** 要点は3つ:
  ★**シード記号 `①` は校名の欄の外側にあり、落とさないとその行の校名が丸ごと無効になる。**
  ★★**`normalize()`（NFKC）を通すと `①` は `1` になる**ので**生の文字列で判定すること。**
  ★**スロット番号の列が無い**ので `nameXLeft` / `nameXRight` / `centerX` は**縦線の列から測る。**
  ★★★**決勝は左右とも勝ち色で、隙間（34.4pt）に優勝校が縦書きで刷ってある。**
  **そのままだと `findFinal` が「3位決定戦」のほう（赤と黒が接している）を決勝として拾い、
  しかも「チーム数 − 試合数 = 1」を通ってしまう。**
  **止めたのは記事の本文の優勝校との突き合わせだけ。**
  ＋ ★★★**2026-08-31 その5 に 新潟を 189 → 1,162試合**（3 → 18大会・2 → 7年）。
  ★★**過去大会のページ（`koushikikako2/`）は「年 × 春/夏/秋」の表**で、
  **年・季節・回数が表から直に取れる**（開く前にどのファイルかが決まる）。
  ★**それまではファイル名に `haru`/`natu`/`aki` が入っているものを新しい順に3件**しか
  見ておらず、**2021年以前のファイルが1件も拾えていなかった。**
  ★**表は2010年から48件**（`.xlsx` は21件。**`.xls` は `exceljs` で開けない**）。
  ★★★**1つのファイルに2つの年が入っていることがある**（2023年春に令和5年と令和6年）。
  **「ファイルの最後に見た日付」で丸ごと判定していたので気づけなかった。**
  **試合ごとに日付の年を見て、表が言う年と合うものだけ採ること。**
  ★★★**決勝の行だけ「第N試合」が無い紙がある**（`| HARD OFF | 決勝戦 |`）。
  **`gameRow` が前の準決勝のままになり、決勝が「準決勝」として画面に出ていた。**
  **回戦の名前そのものが書いてある行も見出しとして扱う。**
  ★★**大会名は回数（表から）と年（ファイルの中の日付から）で組み立てる。**
  **回数から年を出さない**という前からの規則は守っている。
  ★★★**これで「名前と中身が1年ずれている」も直った** ——
  ~~新潟は `--year` で遡らないこと~~ は**もう有効ではない。**
  表の年とファイルの中の日付を突き合わせるので、前に2度データを壊した1年ずれは起きない。
  ★**名前が変わったので、生成物を消してから年ごとに走らせ直してある。**
  ＋ ★★★**旧形式の Excel（.xls）も読める**（2026-08-31 その5。運営者の承認）。
  **SheetJS（`xlsx`）を作者の配布元（`cdn.sheetjs.com` の tgz）から devDependency で入れてある。**
  ★**npm に出ている版は 0.18.5 で止まっていて既知の脆弱性の告知が残る。npm の `xlsx` に戻さないこと。**
  ★**新形式は今までどおり `exceljs`。** 一本化していない（既に読めている県の生成物が変わるおそれ）。
  ★★★**拡張子で新旧を判断しないこと。先頭4バイトで見る** ——
  `50 4B`（`PK`）なら新形式（zip）、`D0 CF 11 E0` なら旧形式（OLE2）。
  **兵庫は拡張子 `.xls` のまま中身も旧形式**だが、**新潟には `.xlsx` も混ざる。**
  ＋ ★★★**2026-08-31 その5 に 新潟を 1,162 → 2,828試合**（18 → 42大会・7 → 17年。2010〜2026年）。
  ★★★**`new RegExp("^第\d+試合$")` は文字列の中なので `\d` が `d` になる** —— このバグを
  **同じ日の前半で自分で入れていた。** 新しい形は「`1回戦` という1つのセル」でも見出しと判るので
  **気づけなかった**が、**古い形は回戦が `4` と `回戦` の2セルに割れている**ため
  **1シートあたり1試合しか取れていなかった。★`new RegExp("…")` の中では `[0-9]` と書くこと。**
  ★★**古い形（.xls）は見出しの行が無い。**「第N試合」の行の次の2行がそのまま両チームで、
  **合計は16列目**（新しい形の「計」と同じ列。9回でも延長でも動かない）。
  ★★★**深い回戦は2試合が横に並ぶ**（22列目から2試合目）。
  **左だけ読むと準々決勝が2試合・準決勝が1試合になる**（実際になった）。
  ★**回戦は詰めてつなぐこと**（空白でつなぐと `4 回戦` になって回戦が付かない）。
  ★**元号は年度で切り替える**（引き算1本だと2010年が `令和-8年度` になる）。
  ★★★**回戦の札が合わない大会は1試合も出さない** ——
  古いファイルは**読めなかった枠が前の見出しの回戦を引き継ぐ**ので、
  46大会中4大会が「2回戦が32試合」「決勝が0試合」になっていた。
  **回戦は画面に事実として出る。決勝1試合・準決勝2試合でない大会は丸ごと落とす。**
  ★**支部予選のシートも外す**（古いファイルは秋季が `支部１～３回戦` と `県1回戦…` に分かれている）。
  ＋ ★★★**2026-08-31 その5 に 兵庫を 148 → 1,472試合**（1 → 20大会・2017〜2026年）。
  **会場ごとのスコアシート（`.xls`）から取る**（`taikai/index.php` → 年度 → 大会 → scoresheets）。
  ★★**組合せ表はGIF画像で文字が1つも入っていない。スコアシートだけで足りる。**
  ★**2026年度だけスコアシートが無くPDF**（`collectPdf` に落とす）。
  **2016年度以前は地区大会の `.xls` しか無い。**
  ★**地区大会（`district`）と軟式（`N` で始まる／`nanshiki`）は必ず外す。**
  ★★**1シート＝1会場1日。**「学校名」の行は2種類（イニングの表と投手・捕手の表）あるので
  **`合計` があるほうだけ**見る。**合計の列は見出しから取る**
  （延長のときは十五の欄に「延長14回 ﾀｲﾌﾞﾚｰｸ」と書かれ、位置を決め打ちすると拾えない）。
  ★★★**踏んだところは3つとも「セルに割れて空白が入る」**:
  ★**大会名は「第」のセルの手前まで**（`第[0-9]+日` で切ると**日にちが別のセル**なので
  `…兵庫大会第日2025年…` になり大会名に日付がくっつく）。
  ★**空白を必ず落とす**（`平成 2 9年度…` になる）。
  ★**同じ大会に2つの書き方がある**（`全国高校野球選手権` と `全国高等学校野球選手権`）——
  **片方に寄せないと1つの大会が2つに割れる**（実測 38 → 22大会）。
  ★**回戦もセルに割れる**（`|決 勝|戦|`）——**空白を落とさないと決勝だけ回戦が付かない**（12大会）。
  ★**回戦の札が合わない大会は1試合も出さない**（新潟と同じ構え。6大会が落ちた）。
  ＋ ★★★**季節を足すときは「その季節のページだけを見ているか」を必ず確かめること**
  （2026-08-28。岐阜で踏んだ）。岐阜に春季・秋季を足したとき、
  **旧サイトの日別スコア表（選手権＝夏のページ）を読む処理を季節で分けていなかった**ので、
  **春・秋の収集でも同じ試合を読み、`season` の札だけ変えて返していた**
  （2021〜2023年の夏199試合が春と秋にも入った）。
  ★★**検算は通る**（中身は正しく、季節だけが違う）。**合計試合数も増えるので数だけ見ると気づけない。**
  ★**季節ごとの件数を数えること。**
  ＋ ★★**広島・三重・滋賀・岐阜は「紙はあるが読み直しが要る」**（2026-08-28 に調べた）。
  広島＝春季の紙はスロット列が3か所に分かれ夏の決め打ちが通らない／
  三重＝過去年の紙が年ごとに違って組み立てに落ちる
  （★★**2026-09-01 に解決**。下の「三重を箱スコアから取った」）／
  滋賀＝2023年度は春「連合チームの内訳が読めない」・夏「50チームに48試合」・秋「組み立てられない」／
  岐阜＝春季の報告書に回戦が書かれておらず、3位決定戦とみられる1試合を見分けられない。
  ＋ ★★★**2026-08-28 に 福岡を 257 → 1,438試合**（2大会 → 11大会・8年）。
  出典（HSB flash）の `/pasts` に**25大会・2018年まで**並んでいる。
  ★**春季・秋季は題が毎年まったく同じ**なので、**年は開いて「大会期間」の西暦を読むまで分からない。**
  ★★★**題が毎年同じ大会は、大会名に西暦を足すこと** ——
  **足さないと画面で何年ぶんかが1つの大会に潰れる**（`listTournaments` は「季節＋大会名」でまとめる）。
  ★**名前を変えたら生成物を消してから年ごとに走らせ直す**（引き継ぎの鍵が変わって二重に入る）。
  ＋ ★★★**2026-09-02 に 鹿児島・愛媛・長崎・高知 も HSB flash に切り替えた**（運営者の判断）。
  ★**4県とも連盟からはこれ以上取れなかった** ——
  **鹿児島**＝紙が16枚あるのに3枚しか読めない（年ごとに組み方が違う・不戦勝・優勝校が刷られていない）／
  **高知・長崎**＝連盟の日程APIが直近の年しか持っていない／
  **愛媛**＝スコアPDFは2022年度から。それ以前は日別HTMLで別の読み手が要る。
  ★★**この4県の連盟は取得を断っていない。** **規約で外している6県へ広げたのではない**
  （そちらは「取らない」まま。**動かさないこと**）。
  ★★**足すのではなく切り替え。** 連盟と HSB flash では大会名が**空白の有無だけ**違うことがあり
  （`第107回全国高等学校野球選手権鹿児島大会` と `第107回全国高等学校野球選手権 鹿児島大会`）、
  **両方を登録すると同じ大会が2つ並ぶ。**
  ★**生成物を消してから取り直すこと**（引き継ぎの鍵は大会名）。
  ★**連盟のアダプタは消していない**（`RETIRED_ADAPTERS`。`REGIONAL_RETIRED=1` で戻せる）——
  **どの紙がどう読めなかったかの記録は、そこにしか残っていない。**
  ★**県ごとの違いは4つだけ**（slug / district / host / `summer2020`）。`hsbAdapter` で作る。
  ★★**`summer2020` は2020年に選手権の代わりに開かれた県独自の大会の題。名指しで拾う** ——
  **県ごとに題がまるで違い、長崎だけ「夏季」の字が無い**（`令和2年度長崎県高等学校野球大会`）。
  ★★★**`--year` で遡ると、夏はいつも「いちばん新しい大会」が返っていた**（同日に直した）——
  索引（開催中／直近の大会）を**頼まれた年と照らさずに**返していたため。
  **大会名が同じなので生成物は壊れないが、目当ての年の夏が永遠に入らない。警告も出ない。**
  ★**気づけたのは、遡るたびのログに同じ大会名が並んだから。**
  ＋ ★★★**2026-09-02 その2 に 広島・京都・福島・福井・岩手・滋賀・岡山・三重・徳島 も
  HSB flash に切り替えた**（運営者の「高野連サイトでは追えない部分については、他のサイトも調べて」）。
  **57,931 → 62,961試合**（1,221 → 1,349大会）。
  ★**9県とも「連盟から取り切れていない県」**（画像でしか出していない・過去年が無い・
  紙が足りない、など。県ごとの理由は README の引き継ぎメモにある）。
  ★★**規約で外している6県へは広げていない**（そこは「取らない」まま。**動かさないこと**）。
  ★★★**切り替えの判断は「試合数」ではなく「季節ごとの大会数」まで見て決める** ——
  ★**岐阜は 344 → 448試合と増えるのに、夏の紙が1枚も読めず選手権が5大会とも消える**
  （春1夏5秋0 → 春6夏1秋5）。**試合数だけ見ていたら通していた。切り替えていない。**
  ★**アダプタ（`gifuHsb`）は理由と一緒に残してある**（夏が読めるようになれば切り替えられる）。
  ★**9県は、切り替える前の生成物と年・季節ごとの大会数を突き合わせて確かめた**
  （徳島だけ春季が 4 → 3 に減っているが、夏 7 → 8・秋 6 → 7 で年も増えるので承知のうえ）。
  ★★**大阪・長野・山口のように連盟のほうが厚い県は切り替えない。**
  ★★**富山・和歌山は「切り替えると減る」** —— 連盟に HSB flash が持っていない古い年がある
  （富山2014〜2019・和歌山2017〜2018）。**足すには「年で使い分ける」仕組みが要る。**
  ★★★**岡山は地区予選268試合を失った** —— 連盟は秋季・春季の**地区予選**まで出しているが、
  HSB flash は本大会の表しか持っていない。**それでも県全体では 538 → 715試合。**
  **戻すなら `RETIRED_ADAPTERS` の `okayama` を「地区予選だけ返す」ように絞ってから移すこと**
  （そのまま移すと本大会が二重に並ぶ）。
  ★★**岩手は出典が個人ブログ（白球ペンギン.com）でなくなった** ——
  あちらの免責事項は「文章や画像、動画等の著作物」で、**HSB flash の掲示（記事・写真）より広い。**
  ★**収録も 1年 → 8年。**
  ★★★**校名の書き方が連盟と違う。結び付かない校名が出たら、切り替える前の生成物と
  同じ試合を突き合わせて確かめること** —— 広島は
  **連盟「広島商」＝HSB「広島商業」／連盟「福山」＝HSB「市立福山」／連盟「呉」＝HSB「市立呉」**
  を、2026年夏の同じ試合（日付・回戦・相手・スコア）が1件も違わないことで確かめて
  `DISTRICT_ALIASES` に足した。★**広島には県立と市立の同名校が2組ある**ので、
  **この突き合わせが無ければ結び付けてはいけない。**
  ＋ ★★★**2026-09-02 その3 に HSB flash の「1段の紙」を読めるようにした**（`svg-bracket.mjs`）。
  **1枚にブロックがいくつも縦に並び、その優勝校だけで戦う「決勝トーナメント」が上に載っている紙**
  （岐阜の夏・2020年の代替大会）。★**これで岐阜も HSB flash へ切り替えた**（344 → 1,041試合）。
  ★**2段組の紙は1バイトも変わらない** —— 入口で今までどおり「連番の列がちょうど2本」を先に探し、
  **見つからなかったときだけ**1段として読む。
  ★★**山の切り分けは union-find。決勝トーナメントは「その山の出場校＝他の山の優勝校」で見つける**
  （紙の中の2か所が一致することを求める）。
  ★★**返すスロットから決勝トーナメントの山を外す** ——
  **同じ学校が紙に2度出ているだけ**で、外すと「チーム数 − 試合数 = 1」がそのまま成り立つ。
  ★★**ブロックが紙の上で逆さに並ぶ年がある**（岐阜の第105回）。**返すのはスロット番号の順。**
  ★**回戦は「ブロックの回戦 → 決勝トーナメントの回戦」と続けて数える**（ブロック決勝＝大会の準々決勝）。
  ★**左右で回戦の数が1つ違う紙も読める**（65校なら左33・右32）。★**回戦は決勝から数えること。**
  ★★★**「一覧が1校多い紙」を受けてはいけない**（高知・愛媛・徳島の春季）——
  **勝ち抜き表の下に「順位決定戦」があり、一覧の優勝・準優勝はそちらの結果。**
  **余った1校はその試合に出る学校**で、**表の勝者は大会の優勝校ではない。**
  一度受ける変更を入れたが、**印字の優勝校との突き合わせが止めた**ので取り消した。
  ★**取り消したあとは生成物を消してから走らせ直すこと**（引き継ぎが前の内容を残す）。
  ★**京都の2020年は8つの山で終わっており決勝トーナメントが無い**（ブロック大会）。**読めないまま。**
  ＋ ★★★**2026-09-02 その3 に 大阪 も HSB flash へ切り替えた**（2,231 → 2,908試合・15 → 19大会・5 → 8年）。
  ★**連盟からは5年ぶんしか取れておらず、2026年の紙はまだ無い。** どの季節も減っていないことを確かめた。
  ★★**大阪の紙は3段**（32ブロック →「4回戦・5回戦」の山が8つ →「準々決勝・準決勝・決勝」）。
  **段は2つと決めつけないこと** —— 下から順に「その山の出場校がすべて別の山の優勝校なら1つ上の段」で決める。
  ★★**連合チームの校名が2行に折り返されると「・」が消える**ので、
  **山どうしの突き合わせは中黒を外してから**（表「茨木工科・福井北摂つばさ・箕面東」／
  見出し「茨木工科・福井・北摂つばさ・箕面東」）。
  ★★★**紙の右側に別の小さな枝が同じ高さで刷ってあることがある**（秋季の `三位決定戦`）。
  **1段の紙では「スロット番号の列より左」の校名だけを見ること** ——
  分けないと `太成学院大高太成学院大高・近大附…` という校名になる。
  ★**長野は切り替えない** —— HSB flash の春季16校・秋季24校（支部予選のあとの県大会だけ）より、
  **連盟の63大会1,123試合のほうが厚い。**
  ＋ ★★**同じ回に 千葉 も HSB flash へ切り替えた**（1,089 → 1,934試合・14 → 22大会・7 → 8年）。
  ★**連盟は2020年が丸ごと無く、春も秋も飛び飛びだった。** HSB flash は春5→7・夏4→8・秋5→7で、
  **22枚すべてが検算を通り、警告が1本も出ない。**
  ★**連盟の支部予選は元から取っていない**（ブロック表で勝ち抜きの木ではない）ので失うものは無い。
  ★★★**これ以上の切り替えには「年で使い分ける仕組み」が要る** ——
  **愛知（2015年〜）・兵庫（2017年〜）・富山（2014年〜）・和歌山（2017年〜）は
  連盟のほうが古い年を持っており**、HSB flash はどの県も2019年から。
  **両方を登録すると同じ大会が2つの名前で並ぶ**（引き継ぎの鍵は大会名）ので、
  **「HSB flash は連盟が持っていない年だけ返す」作りと、試合ごとの出典表示が要る。**
  ＋ ★★★**同じ大会が2つの名前で並ぶことがある**（2026-09-02 その3。**佐賀7件・熊本1件**）。
  出典が**日ごとのページで大会名を書き分けている** ——
  「第107回全国高等学校野球**選手権**佐賀大会」22試合／「第107回全国高等学校野球佐賀大会」13試合、
  「第72回**NHK**杯」／「第72回**ＮＨＫ**杯」（全角）、「第143回 九州地区…熊本大会」／「… 熊本大会」（空白）。
  ★**割れていると画面で1つの大会が2つに見え、トーナメント表も組めない。**
  ★**空白・全角ラテン・「選手権」「記念」を外して同じになる名前は、試合数の多いほうへ寄せる**
  （`build-regional-results.mjs` の季節ごとの後処理）。
  ★★★**ただし「会期から1週間より離れていたら寄せない」** ——
  **出典が1日ぶんのページで大会名を書き間違えていることがある**（佐賀の2026-06-05）。
  **名前だけで寄せると、6月の試合が7月の選手権の「決勝」として並ぶ**（実際に並んだので取り消した）。
  ★**会期に収まっていることまでは求めない** —— 少ないほうは**大会の序盤の日**であることが多い。
  ＋ ★★**夏の優勝校は甲子園の代表校と突き合わせられる**（2026-09-02 その3。**310大会が一致**）。
  `src/lib/data/koshien-games.json` は代表校の県を持っており、**地方大会とは出典がまったく別。**
  ★**食い違い27件のうち26件は略し方の違い**（`専修大松戸`/`専大松戸`、`智辯学園`/`智弁学園` ほか）、
  **1件は検算する側の取り違え**（佐賀のNHK杯を選手権の決勝として拾っていた）。
  ★**新しい県を足したら、この突き合わせを1度やること。**
  ＋ ★★★**2026-09-03 に「連盟が持っていない年だけ HSB flash から足す」仕組みを入れた**
  （`hsbFillAdapter`）。**富山 370 → 1,084／愛知 1,882 → 2,393／兵庫 1,472 → 1,749／
  和歌山 620 → 845試合。** 66,872 → 68,599試合。
  ★**この4県は連盟のほうが古い年を持っている**（愛知2015年〜・兵庫2017年〜・富山2014年〜・
  和歌山2017年〜）ので、**切り替えると古い年が消える。**
  ★★**同じ slug のアダプタを2つ登録する**（主＝連盟、副＝HSB flash）。
  **主が先に走って生成物を書き、副はそれを読んで「主が1試合でも持っている 年×季節」は返さない。**
  ★**`ADAPTERS` では必ず主のうしろに置くこと。**
  ★★**県の出典表示（`sourceName`）は主のまま**にし、**副が足した試合には `source` を付ける**
  （愛知の CATVase と同じ形。**転記した経路が別ならその経路が本当の出所**）。
  ★**主のものかどうかは `source` の名前で見分ける。**
  ★★**この2本立ては切り替えより弱い** —— **主が1試合でも持っている年には入らない。**
  混ざって二重になるより取りこぼすほうを選んでいる。
  ★**確かめたこと**: 同じ試合が2つの大会に入っていないこと（4県とも重複0）・
  季節ごとの大会数が減らないこと・**夏の優勝校と甲子園の代表校の突き合わせ**
  （`scripts/verify-regional-summer.mjs`。316大会が一致）。
  ＋ ★★**同じ2本立てを24県へ広げた**（2026-09-03 その2。**68,599 → 71,721試合**）。
  **大分 986 → 1,799／群馬 1,879 → 2,516／長野 1,123 → 1,450／新潟 2,828 → 3,051／
  沖縄 2,224 → 2,452**、ほか神奈川・石川・島根・香川・栃木・山口・山形・宮崎・佐賀・奈良。
  ★**穴が「年×季節」の単位で空いている県ほど効く。** 茨城・埼玉・山梨・静岡・熊本は
  **穴が2020年の春（そもそも中止）だけ**なので0件だった。
  ★**同じ slug のアダプタが2つになるので、`loaders.ts` の生成と `districts` の集計は slug で畳むこと**
  （畳まないと TS1117 で型検査が落ち、抜粋と勝ち上がりがその県だけ二重に数える）。
  ＋ ★★★**同じ試合が2つ入っていることがある**（2026-09-03 その2。5件）。
  **重複を落とす鍵が「日付＋1校目＋2校目」だった**ので、**出典が左右逆に載せていると落ちない**
  （静岡 `袋井 6-静清 4` と `静清 4-袋井 6`）。**引き継いだぶんは鍵を通らない**ので、そこでも残る。
  ★**「日付＋校名2つ（並べ替え）＋得点2つ（並べ替え）」で落とす。**
  ★★**得点まで鍵に入れること** —— **同じ日に同じ顔合わせが2試合ある紙がある**
  （山梨 2023-04-19 は別の球場で2試合）。
  ＋ ★★★**同じ大会が2つの季節に割れていることがある**（長野3試合・佐賀2大会）。
  **出典の季節ごとのページが、別の季節の紙を1枚だけ拾っている。**
  ★**大会は1つの季節に属する**ので、**試合数の多いほうの季節に寄せてから、
  季節をまたいだ重複を落とす**（`build-regional-results.mjs` の県ごとの後処理）。
  ★**日付を持たない試合は大会名と回戦まで鍵に入れること** ——
  **別の年の同じ顔合わせ・同じ得点**が落ちる（富山・三重のように日付が無い出典がある）。
  ＋ ★★★**ページごとに Supabase を叩き直さないこと**（2026-09-04。**ビルドが落ちた**）。
  **`cache()`（React）はリクエスト1つのあいだしか効かない。ビルドはページ1枚が1リクエスト**なので、
  `getSchoolNameIndex` は**5,337枚ぶん schools 3,500行（1枚につき4リクエスト）を取り直していた。**
  ★**症状は `Failed to build /schools/… after 3 attempts`（1枚60秒の上限）で、
  落ちる枚数が回ごとにまるで違う**（0・7・149・222・238枚）。**遅いのは計算ではなく待ち。**
  ★**直し方はモジュールに1つだけ持つこと**（`src/lib/queries/schools.ts` の
  `fetchSchoolNameRows` / `relatedRows`）。**5分で作り直す**（県のページの `revalidate = 300` に合わせる）。
  ★**永久に持たない**（学校マスタを入れ替えても古いままになる）。★**失敗は持ち越さない。**
  ★**実測: 60秒超えが 238枚 → 0枚。**
  ＋ ★★★**HSB flash は2018年まである**（2026-09-04）。**2019年からしか取っていなかった19県に足した**
  （**71,721 → 72,817試合**。大阪 +248・千葉 +190・広島 +130・三重 +84・鹿児島 +64 ほか）。
  ★**2017年は無い**（大阪・千葉で確かめた。**底は2018年**）。
  ★★**連盟の archive を1年さかのぼる試みは15県で1県も増えなかった** ——
  山梨2006・香川2008・新潟2009・佐賀2009・熊本2010・茨城2011・栃木2012・富山2013・愛知2014・
  宮崎2014・埼玉2014・兵庫2016・静岡2016・群馬2005・沖縄2006。**同じ調べ直しをしないこと。**
  ＋ ★★★**高知・愛媛・徳島の春季は「順位決定戦」があるので出せない**（2026-09-04 に確定）。
  **勝ち抜き表の下に順位決定戦**（四国大会の枠を決める試合）があり、**日付と両校名だけで得点が無い。**
  ★**一覧の余りをその2校とみて受ける緩め方を入れたが、検算に止められて取り消した**:
  **高知2025春は 順位決定戦 高知 - 明徳義塾 を明徳義塾が勝ち、一覧の優勝は明徳義塾**
  （＝**勝ち抜き表の勝者は大会の優勝校ではない**）。
  ★**「◯回戦」は事実として画面に出る**ので、決勝でないものを決勝と書けない。
  ★**読み手は `placement`（順位決定戦の2校）を返す** —— **落とすときの警告に出る。**
  ★**入れるなら順位決定戦の得点を持つ別の出典が要る。**
  ＋ ★★★**不戦勝の連結線は色が分かれず1本で描かれる**（2026-09-04。大阪の第104回で突き止めた）。
  ふつうの試合は**勝ち色と負け色の2本**が端点を共有するので1試合と分かるが、
  **不戦勝は1本きり**なので**組にならず、山がそこで割れる**（`いちばん上の山が2つある`）。
  ★**紙には `不戦` と刷ってある。** ★**1本でも連結とみなすだけでは足りない** ——
  **どちらが上がったのかが色から決まらない**（両側とも赤で入ってくる。
  枝の色は「その枝の**左端**の試合に勝ったか」も表すため）。
  ＋ ★★★**2026-09-04 その3 に読めるようにした**（大阪 3,156 → 3,571試合）。
  ★★**上がった側は「紙の別のところに同じ校名がもう一度出てくるか」で決める** ——
  **ブロックで勝ち上がった学校は、上の段の山にスロットとしてもう一度刷ってある。**
  ★**推測ではなく「紙の中の2か所」の突き合わせ**で、**段を決めるのと同じ性質のもの。**
  ★**どちらも2回／どちらも1回なら決められない。1試合も出さない。**
  ★★**拾うのは「紙が `不戦` と刷ってある縦線」だけ**（中点の ±20/±10 以内）——
  組にならない縦線は他にもある（**三位決定戦の小さな枝**・**無効試合**）。
  ★**この字で拾うので、読めていた紙は1枚も変わらない**
  （組にならない縦線を今まで無視していた紙は、そこで枝が切れて必ず落ちていた）。
  ＋ ★★★**「無効」と刷ってある試合は、どちらも上がらない**（2026-09-04 その4。大阪2021秋 +127試合）。
  **不戦勝と同じく色の分かれていない1本の縦線**だが**意味が逆**で、
  **出ていく枝にも誰もいない**（枝の40ほど外側に `無効` と刷ってある）。
  ★**出典の「出場校別試合一覧」で確かめた** —— その試合に出た6校は
  **どれもこの1試合しか持たず、得点も空。次の試合を戦っていない。**
  ★★**そのかわり次の回戦が不戦勝になる**（紙に `不戦`。生きている側の枝が勝ち色）。
  ★★**枝を消さずに「誰もいない」印（DEAD）を置くこと** ——
  **消すと「まだ前の試合が終わっていない枝」と見分けが付かず、そこで止まる**
  （症状は `決勝に残ったのが 17 チーム`）。
  ★**無効の試合は画面に出さない。2校とも消えるので検算には2つ数える**
  （片側が DEAD の合流は0）。
  ＋ ★★★**回戦の帯は「試合」ではなく「枝の合流」から数えること**（2026-09-04 その4。群馬2022春）。
  **その回戦が不戦勝だけで試合が1つも無いことがある。**
  試合から数えると**その帯が丸ごと消えて、その側の回戦名が全部1つ浅くなる**
  （群馬は右半分の準決勝が不戦勝で、**準々決勝6試合・準決勝3試合**と画面に出ていた。正しくは 4・1）。
  ★**試合数は正しいまま**なので、**数だけ見ていると気づけない。**
  ★**どの帯にも試合がある紙では集合が同じ**なので、**既存の県は1バイトも変わらない。**
  ★★**同じずれの探し方は2つ**: **①決勝1・準決勝2・準々決勝4を超える大会**
  **②いちばん浅い回戦が1回戦でない大会**。**生成物を数えるだけで出せる。**
  ＋ ★★★**関数を消すときは呼び出しが残っていないか確かめること**（2026-09-04 その4）。
  `fetchOmyuNews` / `fetchOmyuNewsBody` / `fetchOmyuJson` の**定義だけが過去のコミットで消えていて**
  （山形が使わなくなった回）、**宮崎と島根の連盟アダプタが毎回
  `fetchOmyuNews is not defined` で落ち、3季とも「前の内容を残す」で凍っていた。**
  ★**画面は正しく見えるので気づけない。** **3つとも当時のまま戻してある。**
  ＋ ★★★**同じ年・同じ季節に大会が2つあることがある**（2026-09-04 その3。**警告も出ない欠け**）。
  **第100回の記念大会は1県から2校が出る**ので、`第100回…記念 南大阪大会` と `… 北大阪大会` が並ぶ。
  ★**HSB flash の過去大会は「年が合った1件」で `break` していた**ので、
  **北大阪の87試合が永久に入らなかった**（千葉も西千葉だけで東千葉が入っていない）。
  ★**年が合うものは全部読むこと。** 一覧は新しい順なので、
  **目当ての年より古いものが出たらそこで止める**（出典に余計な負担をかけない）。
  ＋ ★★★**2020年の代替大会は「ブロック大会」で、大会の決勝が無い**（2026-09-04 その3）。
  コロナで選手権が中止になった年の代替大会は、**紙が N 個の山で終わっている**
  （京都8・福岡4・大阪2・兵庫8）。**優勝校が複数いる。**
  ★★**そういう紙では 決勝・準決勝・準々決勝 という回戦名を1つも出さない** ——
  **紙のいちばん深い試合は「その山の決勝」であって、大会の決勝ではない。**
  `N回戦` は「その学校のN試合目」なので、そのまま事実として正しい。
  ★**優勝校も返さない**ので、`summarizeTournament` が「優勝◯◯」と書かない
  （あちらは決勝がちょうど1試合のときだけ優勝校を出す）。
  ★**トーナメント表も組まない**（`regional-bracket.ts` が「いちばん深い回戦が2試合以上」で断る）。
  ★★★**受けてよいのは「出場校の一覧と1対1で突き合わせられるとき」だけ**（`blocks`）——
  **読み違えでも同じ症状（山が2つ以上）が出る。**
  読み違えのときは**上の段のスロットが `blockSlots` に混ざる**ので、**一覧との1対1が必ず落ちる。**
  ★**そのうえで「記載の優勝校が、どれかの山の勝ち残りであること」も要求する**
  （出典は山が8つでも `優勝` を1つだけ書く。**記載が無い大会は受けない**）。
  ＋ ★★★**「主に無い大会」だけを名前で選んで足す副アダプタがある**（2026-09-04 その3。`okayamaTrials`）。
  **岡山は HSB flash に切り替えたときに連盟の地区予選268試合を失った**が、
  `hsbFillAdapter` の2本立ては**「主が1試合も持っていない 年×季節」**しか足さないので、
  **同じ年・同じ季節にある地区予選が永久に入らない。**
  ★**連盟のアダプタをそのまま呼び、大会名に `地区予選` が入るものだけ返す**（756 → 1,037試合）。
  ★★**本大会は1試合も返さないこと** —— 校名の書き方が両者で違うので
  **重複を落とす鍵（日付＋校名）を通り抜け、第108回が 54 → 97試合になった**（実測）。
  ★★**3季とも見に行くこと。書き出しは季節ごと**なので、
  **見に行かなかった季節は生成物から丸ごと消える**（夏を外したら主の446試合が消えた）。
  ★**0件が普通の季節には `addsOnly` を立てて ⚠️ ではなく ℹ️ を出す**
  （毎回鳴る警告は本物の異常を埋もれさせる）。
  ＋ ★★~~**兵庫は入れられなかった**~~（2026-08-28。**理由を残す**）。
  ★**春季の紙にスロット番号の行が無い**（`1〜N` と並ぶ列が1つも無い）。**推測で組み立てない。**
  ★**過去年は索引から辿れない** —— 年度ページから張ってあるのは
  **会場ごとの Excel スコアシート（`.xls`＝古い形式で `exceljs` では開けない）だけ**で、
  やぐら表のPDFへのリンクが無い。**URLの形を当てれば取れる年もあるが、紙の分かれ方が年で違う。**
  ＋ ★**2026-08-27 その2 に 京都へ春季**（70 → 85試合）。
  ~~春季は表の形が違う~~ は誤りで、**2次戦の紙は夏と同じ京都型**だった。
  ★★**日付が `5月10日` と書かれている**（夏は `7/4`）ので、
  **`M/D` しか見ない読み方では全試合が日付なしで落ちる**（`parseLabel` で直す）。
  ★**優勝が行の先頭に無い紙がある**（行の途中の「優勝」も拾う）。
  ★**紙は正式名・枝は略称**（`龍谷大学付属平安` と `龍谷大平安`）。**部分列なら同じ**とみなす。
  ★★**京都の過去年は取れない**（2026-09-01 に実際に開いて確定した）。
  ~~結果のリンクが外されている~~ は不正確で、**リンクはある。中身が違う** ——
  **`tournament/r07/` は組み合わせ表がJPG画像**（文字が1つも入っていない）、
  **`r06/` `r05/` `r04/` は組み合わせ表そのものが無い**（優勝校の写真だけ）。
  結果へのリンクは**一球速報のスコア**（`baseball.omyutech.com`）で、そこからは取らない。
  ★**画像はOCRが要るので対象外**（兵庫のGIFと同じ線引き）。**再調査しないこと。**
  ＋ ★★★**2026-08-30 に 愛知の夏を 173 → 1,040試合**（1大会 → 6大会・2021〜2026年）。
  **出典を CATVase.jp から連盟（`www.aichi-kouyaren.com`）に替えた**
  （規約は自分で開いて確かめた。robots.txt は404、転載・無断・複製・営利の記載も無い）。
  ★**CATVase.jp は捨てていない** —— 連盟の記事は大会が終わってから出るので、
  **開催中の大会だけそこから補う**（`source` にそのサイト名を持たせる。連盟の名前で出さない）。
  ★★**「読み手が3つ新規に要る」という前の見立ては外れていた** ——
  **枝が線として紙に描いてあり**、富山と同じ `vector-bracket.mjs` がほぼそのまま通った。
  ★**座標から組み立てられないから難しい、と結論する前に、枝が図形として入っていないか見ること。**
  ★★**`slot-bracket.mjs` では絶対に読めない紙**（シードが2回戦・3回戦から登場する）。
  ★★**検算でいちばん強いのは枚をまたぐ突き合わせ**（ブロック8枚の優勝校＝準々決勝以降の出場校8校）。
  さらに**第108回を CATVase.jp と突き合わせて 173対173・169が完全一致**を確認した。
  ★**`vector-bracket.mjs` に足した4つはどれも既定を変えていない**
  （`ops: ["fill"]` ／ `teams` を渡せる ／ `nameTol` ／ 同じ色の縦線をつなぐ）。
  **富山を再生成して生成物が1バイトも変わらないことを確認済み。**
  ★★★**引き継ぎの鍵の罠** —— **日付の無かった試合に日付が付くと、
  鍵（`大会名＋試合の日付の年`）が変わって前の生成物が引き継がれ、同じ試合が2つ並ぶ。**
  **検算はどれも通る。**歯止めを入れてある（詳しくはREADME）。
  ＋ ★★★**同日に春季・秋季も足した**（1,040 → **1,627試合**・6 → **18大会**）。
  ★**春季・秋季の紙にも枝が線として描いてあり、同じ読み手で読めた。**
  ★★**大会名は季節ごとに形を持つこと** —— **秋季の大会名にも「選手権」が入る**
  （春季 +1950／夏 +1918／秋季 +1947。**記事の掲載日と突き合わせて確かめた**）。
  ★★★**決勝の横線が3本に分かれていて、色の変わり目が2つできる。**
  **先に見つかったほうを採ると勝った側が左右あべこべになる**（実際になった）。
  **優勝校の枝が縦に伸びているほうが出会う点。**
  ★★**3位決定戦を出さない**（宮崎・沖縄と同じ）。半分のいちばん内側の列の試合は決勝に出る1つだけ。
  ★★**シード表記とスロット番号が同じ断片に入る**（`①1` `京 ２位 12`）。**丸数字の前後でも割る。**
  ★**夏のブロックの紙では割らないこと**（球場が `春日井②` で、割ると球場名が `②` になる）。
  ★★★**`--year` の実行が開催中の大会を静かに上書きしていた** ——
  その季節に大会が当たらない年を指定すると、CATVase の補完が走って
  **連盟の紙から読んだ第108回が置き換わっていた。検算はどれも通る。**
  ★★★**春季・秋季は大会名の頭に西暦を足す**（宮崎と同じ）——
  **試合の日付が1つも無い**ので、そのままだと `第N回…選手権` の規則で
  **秋季が1996年**になり、**春季は年が分からない大会**になる。
  ★**夏は日付があるので足さない**（足すと引き継ぎの鍵とURLが変わる）。
  ★**春季5大会・秋季7大会は古い紙で未対応**（詰まったら `AICHI_DEBUG=1`）。
  ＋ ★★★**2026-08-27 その2 に 千葉を 147 → 1,089試合**（28,185試合。1大会 → 14大会・7年）。
  **連盟のサイトマップから春・夏・秋の本大会のやぐら表PDFを取る**（`--year` で過去年）。
  ★★**同じ大会に記事が3つある**（本大会・予選・予選敗者復活戦）。
  **スラッグは途中で切れていて見分けられない**ので、**記事の題（`<title>`）で見分けること。**
  ★**予選は支部ごとのブロック表で勝ち抜きの木ではない**（取らない）。
  ★★**紙の大きさが年と季節で違う**（夏148チーム・春秋48〜64チーム）ので**座標は紙から測る。**
  ★★**中央の書き方が年で違う** —— `優勝◯◯高等学校` / `優勝◯◯高校` / **`優勝◯◯（初優勝）`（校名だけ）。**
  **「高校」で止める作りだと、付いていない年が1試合も出せない。**
  ★**2022年以前は準優勝を刷っていない**ので、**優勝校だけで検算する**（山口と同じ）。
  ★★**シード記号が校名と同じ側にある紙がある**（春の◎は校名とスロット番号のあいだ）。
  **範囲では切り分けられないので列ごと落とす。文字で消さないこと。**
  ★★**校名の外の端は「密な列」で測る** —— 記号の列に**記号でない字が1つ紛れている**ことがあり
  （夏の「宣」）、「いちばん外の文字」では測れない。
  ★**ただし「校名の1文字目の列」で切ってもいけない** ——
  **連合チームの校名はそれより外に字がある**（`八街` が `八` になった）。
  ＋ ★★**2026-08-27 その2 に 宮崎+88・奈良+147・静岡+78**（27,243試合）。
  **踏んだところは次のとおり**（詳しくはREADMEの「引き継ぎメモ（2026-08-27 その2 時点）」）。
  ★★**紙の形は同じ県・同じ季節でも年でまるごと変わる**（宮崎の夏）。
  左のスロット列が **2026年は x=116／2025年は x=132**、準決勝の帯は **251／279**。
  **決め打ちの `half`・`ranges` では前の年が1試合も出ない。★紙から測ること**（`summerGeometry`）。
  ★**決勝の欄を範囲から外すのがいちばん難しい** ——
  **準決勝の帯は数字が2つ**（スコアが連結線の両端）、**決勝は片側1つずつ**なので、
  **「境目に向かって数字が2つ以上ある最後の列」が準決勝、その次が決勝**とし、その中間で切る。
  ★★**お知らせに優勝校が書かれていない年がある**（宮崎の2025年夏）。
  **紙の中央の縦書きから読む**（`championFromCenter`）。
  ★**「優勝」が1断片の紙と、「優」「勝」が縦に離れている紙**の両方がある。
  ★★★**枝に日付が無い紙（`hasDates: false`）では、決勝にも日付を付けないこと。**
  中央にだけ決勝の日付（`7/26`）が刷ってある紙があり、拾うと
  **44試合のうち1試合だけ日付を持つ大会**になる。
  `latestSeasonGames` は**「試合の日付がいちばん新しい大会」を先に見る**ので、
  **日付を1つ持つ2025年の大会が、1つも持たない2026年の大会に勝ち、県のページに古い大会が出る。**
  ★**検算も警告も通る。画面を見るまで気づけない壊れ方。**
  ★★**日付が1つも無い大会は、大会名に西暦を足すと年が出せる**（宮崎の春季・秋季）。
  `第158回九州地区高等学校野球大会宮崎県予選` は**回数が地区大会の通し番号**なので、
  **紙の「期日」から読んだ年**を頭に足して `2026年 第158回…` にする（奈良と同じ西暦）。
  ★**`yearOfTournament` の「頭の西暦」の規則は `src/lib/regional-tournaments.ts` と
  `scripts/build-regional-results.mjs` の2か所にある。変えるときは両方直すこと。**
  ★★**名前を変えたら生成物を消してから年ごとに走らせ直すこと**（引き継ぎの鍵が `大会名＋年` なので、
  そのまま遡ると**同じ大会が2つ入る**）。
  ★★★**`</tr>` を閉じていない紙がある**（奈良の2013年ごろのHTML）。
  **表の最後の行だけ閉じていない**書き方で、`<tr>…</tr>` を求めると**その行が丸ごと落ち**、
  1試合が「見出し＋片方の校名」の2行になって**試合ごと捨てられる**（42表のうち34表が消えていた）。
  ★`tableRows(table, { closeOptional: true })` で受ける。**既定は変えていない。**
  ★★**索引のリンクに西暦が無い年がある**（奈良の2020年夏 `令和２年度奈良県高等学校夏季野球大会`。
  **選手権が中止で県独自の大会**）。**元号からも年を出すこと**（`labelYear`）。
  ★★**校名の下を切る幅は紙ごとに違う**（静岡）。
  春は校名の行の間隔が 7.4 以内で日程欄まで 25.1 空くが、
  **秋は校名の中に 12.6 の隙間があり、日程欄までは 19.2 しかない。**
  **12 で切ると校名が途中で切れる**（`常葉大菊川` → `常葉`）。
  ★**「校名が縦書きだから読めない」と結論する前に、切る位置を疑うこと。**
  ★★**スロット番号が `#` に潰れている紙がある**（静岡の2025年秋季。Excelの「列が狭い」印）。
  読める数字が9個しかないので**1回戦のスコアの行がスロット行に選ばれる。**
  ★**`#` はセルの位置にそのまま残る**ので、**x で並べた k 番目がスロット k**。
  **読める番号が並びの位置と一致することを確かめてから**書き戻す（当て推量ではない）。
  ★★**深い回戦だけ月まで刷ってある紙がある**（`28` に対し決勝が `10/4`）。
  裸の日しか数えないと**決勝の日付の行が見つからず**、
  「日付の行の個数＝回戦ごとの試合数」の検算に落ちて**1試合も出せない。**
  ★★★**日付を1つも持たない県が年をまたぐと、県のページが古い大会を出す**（2026-08-27 その2）。
  \`latestSeasonGames\` は日付が無いとき**季節の順（春→夏→秋）**で決めていたので、
  **2025年の春夏秋と2026年の春夏を持つ宮崎は「2025年の秋」**が先頭に出ていた（群馬・滋賀も）。
  ★**大会名から年を出して、いちばん新しい大会の季節を採る**（同じ年なら今までどおり季節の順）。
  ★**日付のある県は1件も変わらない。**
  ★★**回戦を出典が書いていない県がある**（奈良は決勝・準決勝・3位決定戦だけ）。
  ★**枝から回戦を組み立てないこと** —— **出典に載っていない試合がある大会が多く**
  （無敗の学校が2校以上ある大会が31件）、**深さを数えると回戦名がずれる。**
  **「◯回戦」は画面に事実として出る。当てない。**
  ＋ 同日に**宮崎の春季**（42試合）。★**「地区予選が4枚ある」という記録は誤りで、
  紙は夏・秋と同じ左右2段組の1枚だった**（4枚あるのは別の大会＝宮崎県高等学校野球選手権大会））。
  ★★**福岡の出典はSVGのトーナメント表で、枝が線として描いてある**
  （`scripts/lib/svg-bracket.mjs`）。**座標から推測して組み立てるのとは別物**で、
  石川で踏んだ「構造は合うのに相手が違う」が起きない。
  ★**`stroke="red"` が勝った側**なので、**得点との一致を検算にできる。**
  ★★**このサイトは47都道府県ぶんある。規約で外している6県もここからなら取れてしまう。**
  **連盟が断っているものを別経路で取るかは運営者の判断。福岡だけにしてある。**
  ★**`fk-kokoyakyu.com` は採らなかった** —— 規約に禁止は無いが、
  **フリーの編集者が運営する商業メディアで、記録の編纂が本業**。
  **禁止条項が無いことと、その人の商売を取ってよいことは別。**
  ★★**福島の「32試合しか読めない／32試合しか載っていない」はどちらも誤りだった**
  （2026-08-21）。**見出し行の数を試合数と取り違えていた** ——
  ★**スコア表は1行に試合が横に2つ並ぶことがある**（段まで数えれば61試合）。
  ★**2桁の数字は1桁より左端が出る**ので、校名の列とスコアの列の境目は
  **列の間隔の半分**で取ること（「見出しの少し左」だと2桁の得点だけ校名にくっつく）。
  ★★**1枚から複数の代表が出るブロック表は `winners` で組む**（2026-08-21。山口の秋季）。
  ★**ブロックの切れ目は探さない** —— 各段で隣どうしを組むので、
  **どのブロックも段数が同じなら切れ目を知らなくても組は正しくなる。**
  ★★**「段数が同じ」は、紙に刷ってある代表校名との突き合わせで担保すること**
  （山口はさらに「次の紙＝県決勝大会の出場校と一致するか」という**枚をまたぐ検算**が効く）。
  ★★**縦書きで2列に組まれた校名は「右の列から」読む**（`nameColumns: "desc"`）。
  ★★**上下2段の紙を「スロット行の中点」で割らないこと。** 校名はスロット行の**下**に伸びる。
  **紙の中でいちばん広い隙間（段と段のあいだの余白）で割る。**
  ★★**字の大きさは、行の隙間より確かな手掛かりになることがある**（2026-08-21。大分の春季）。
  校名の下の「支部」の行は**隙間では切れない**（校名が縦に引き伸ばして組まれている）が、
  **幅は 15.4 対 10.3 ではっきり違う**（`pdf-text.mjs` の `width`）。
  ★★**日付のある季節と無い季節が混ざると、県のページが古い季節を出す**（2026-08-21。大分）。
  `latestSeasonGames` は「日付のいちばん新しい試合の季節」を採るので、
  **日付の無い夏が、日付のある春に負ける。**
  ★**アダプタを足したら、県のページがどの季節を出すかも必ず見ること。**
  ★★**開催中も更新される紙には強い検算をそのままかけない。**
  「チーム数 − 試合数 = 1」を常に要求すると**大会期間中ずっと1試合も出せない。**
  **「決勝が読めたか」で強弱を分ける**（弱いほうは「前の回戦で負けた学校が
  次の回戦に出ていない」。これは途中でも成り立つ）。
  ★★**沖縄を「1〜N と並ぶ行が無い」として外していたのは誤りだった**（2026-08-21）。
  **1〜56 の行はきちんとあった**（滋賀と同じ「見つからない」と「無い」の取り違え）。
  ★**やぐら表の「試合番号の行」は1回戦の帯として通ってしまう**（沖縄・大分）。
  中点がスロットの境目に乗るため。**滋賀と同じ「組めてしまう」壊れ方。**
  落とし方は「**スロット行より上で、0 を除いた数字が 1 ずつ増える行**」。
  ★**大分を「営利目的の複製を禁止」として外していたのは誤りだった**（2026-08-20）。
  原文にその条項は無く、自動分類が「非営利」の語だけを拾っていた。
  ★**47連盟の「制限あり12件」は、他も原文を読み直す価値がある。**
  ★~~**岩手の出典（白球ペンギン.com）は免責事項に「無断転載の禁止」がある。**~~
  → ★★**2026-09-02 その2 に 岩手は HSB flash へ切り替えた**ので、**この出典はもう使っていない**
  （`RETIRED_ADAPTERS` にある）。**あちらの掲示のほうが名指しが広い**
  （「文章や画像、動画等の著作物」）ことが切り替えの理由の1つ。
  ★**「記事の文章は1文字も取らない」の線は HSB flash でも同じ。**
  **公開前に一度問い合わせる**という宿題は HSB flash のほうに残っている。
  ★★**季節は県ごとに違う。**「アダプタがあるから他の季節も同じ紙」は**成り立たない**
  （2026-08-19 に静岡・宮崎・福井で確かめた）。
  静岡は**夏が左右2段組なのに春は京都型**、宮崎は**座標も年の出し方も検算材料も別**、
  福井は**春に日付が1つも無い**（秋にはある）。**季節ごとに測り直すこと。**
  ★★**同じ季節でも年で紙がまるごとずれる**（2026-08-21。宮崎の春季）。
  2025春と2026春は**左のスロット列が 123 と 105、右が 462 と 482**で、
  **範囲を決め打ちすると片方の年しか読めない。**
  ★**スロット番号の列を紙から探して、範囲はそこからの相対で取る。**
  ★★★**`finalAt` の既定（`"middle"`＝いちばん下の2つ）は、決勝と準決勝が
  中央の同じ帯に並ぶ紙では必ず取り違える**（2026-08-26。三重）。
  三重の紙は `3(259) 1(291)│0(299) 0(332)` と**1行に4つ**並び、
  内側の2つが決勝・外側の2組が準決勝。既定のままだったので
  **2025年の決勝が `津商 3-2 津田学園` として画面に出ていた**
  （連盟自身の決勝結果PDFは `津田学園 1-0 津商`。**勝者も点数も違う**）。
  ★★**直すには3つを同時に入れること。1つでも欠けるとどちらかの年が壊れる**:
  **①`finalAt: "innermost"`／②`explodeNumberRuns`**（2026年の紙は決勝の得点が
  `3 12` と1つの断片で、ほどかないと準決勝を拾う）**／③`half` を紙ごとに測る**
  （決め打ち 300 に対し実測 293.0・293.1。**7ポイントで取り違える**）。
  ★**同じ形の紙を読んでいる県は疑うこと。**
  ★★**決勝と3位決定戦の内側・外側は当てにならない**（宮崎の春季）。
  `finalAt: "innermost"` は「境目をはさむ組のうちいちばん内側」を決勝とするが、
  **2026春は3位決定戦のほうが内側**（決勝16 対 3決15。秋は決勝15 対 3決41で逆）、
  **2025春は同じ幅7で「先に見つかったほう」任せ**だった。
  ★**取り違えると画面に嘘の決勝スコアが出る**（実際に 1-6 が決勝として出た）。
  ★**幅ではなく紙の区画で決める** —— 中央の**3位決定戦のラベルの y**を
  `centerFloor` に渡して、**それより下を中央の走査から丸ごと外す。**
  ★**ラベルの行の「中央の断片」も落としてから組み立てる** ——
  2025春のラベルは `3`(x=282) と `位`(x=293) に割れており、
  **`3` が準決勝の帯（x=279）に3ポイント差で並んで「数字3個（必要2個）」になる。**
  ★★**3位決定戦の出場校は、準決勝の敗者と突き合わせられる**（宮崎の春季）。
  ラベルの下に**両校名が縦書きで刷ってある**ので、**枝とは別の場所から来る事実**として使える。
  ★**優勝校が刷られていない年でも「準決勝の相手が違う」を止められる**（石川の壊れ方）。
  ★**完全一致で比べること** —— 同じ紙の同じ書き方なので略し方は揃う。
  **部分一致で緩めると `都城` と `都城東` を取り違える**（2025春に両方いる）。
  ★★**日程表の「試合数」の合計が検算になる**（宮崎の春季）。
  球場ごと・日ごとの試合数が紙の下に刷ってあり、**合計＝組み立て＋3位決定戦**。
  ★**日ごとではなく合計を見るのは、雨天順延に強いから**（順延は日のあいだで
  動かすだけで合計を変えない）。★**球場名のある行だけ数える**
  （見出しの `日程` `月日` の行にも数字が並ぶ）。
  ★**この検算は、優勝校も刷ってある年（2025春）で両方が一致することを
  確かめてから、刷っていない年（2026春）に使った。**
  ★★**縦書きの注記の列は、日程表を落としてから作ること**（宮崎の春季）。
  `stripVerticalNotes` は「同じ x の文字をつなぐ」ので、切る前は
  `（2季ぶり14回目）6火22×○×` になり**形が合わず1件も落ちなかった**
  （沖縄で凡例が校名に吸われたのと同じ「下端が列に入る」罠）。
  ★★**紙のいちばん下の凡例が校名にくっつく**（2026-08-21。沖縄）。
  縦書きの校名は「同じ x の文字をつなぐ」ので、**連合・合同チームの注記が
  近くのスロットに丸ごと吸われる**（`＊宮古総工：宮古総実・宮古工業エナジック`）。
  ★**行ごと落とす**（`【` か `＊` で始まる断片しか無い行）。**文字で消さないこと**
  （千葉で「宣」を巻き込んだのと同じ轍）。
  ★**そのうえで凡例は捨てずに使う** —— `略称：校名・校名` を `expand` に渡して
  連合チームを展開する。展開しないと `isCombinedTeam` に当たらず**1校に結び付く。**
  ★★**年は「回数」から出さず、紙に刷ってある「会期」の元号から出す**（沖縄）。
  春季・秋季は選手権と別の系列で、**春の紙には九州地区大会の回数まで刷ってある。**
  会期は**枝とは別の場所から来る事実**なので、
  **「組み立てた試合がそこに収まるか」がそのまま検算になる。**
  ★**ただし「最終日の一致」は落とす条件にしないこと。** 会期は**予定として先に刷られる**ので、
  **雨天順延で決勝が1日ずれると、正しい大会を丸ごと捨てる。**
  ★**2026-08-16〜18 に 20県 → 27県。詳細はREADMEの
  「★★2026-08-18 に 兵庫」「★★2026-08-17 に 滋賀」
  「★★2026-08-16〜17 に 20県 → 25県」と、各アダプタ冒頭のコメント。** 要点だけ:
  - ★★**1つの大会が複数枚のPDFに分かれている県がある**（2026-08-18。兵庫は4枚）。
    **1枚では「チーム数−試合数=1」が成立しない。**
    ★**枚をまたぐ検算がいちばん強い** ——
    **次の紙の出場校が、前の紙の勝者と1対1で対応するか**を見る
    （兵庫は「5回戦の16校＝ブロックの優勝校16校」）。
    1ブロックでも読み違えれば必ずどれかが余る
  - ★★**紙ごとに略し方が違う**（兵庫。本体「市尼崎・西脇工」／5回戦「市立尼崎・西脇工業」）。
    枚をまたぐ検算で**完全一致を要求すると必ず落ちる。**
    **どちらかがもう一方の部分列なら同じ**とみなす
    （緩いのは1組の比べ方だけで、要求は「過不足なく1対1で対応する」）
  - ★★**校名と得点がスロット番号の同じ側にある表**は `assembleSlotBracket` の
    前提（校名と回戦が反対側）に合わない。**スロット番号の列を
    校名と得点のあいだへ動かしてから渡す**（スロット軸が y なら x を動かしても順番は変わらない）。
    ★**得点の列から20ポイント以上離すこと**（近いと1回戦の得点をスロット番号として拾う）
  - ★**1回戦が1試合しかないブロックがある**（兵庫の9チームのブロック）。
    既定は「2試合ぶん以上」なので**1回戦の帯が飛ばされる** → `minFirstRound`
  - ★**ブロックの大きさを決め打ちしない**（兵庫は9チームと10チームが混在）
  - ★★**「取れない」と分類した県は、根拠を1つずつ実データで確かめ直すこと。**
    2026-08-17 だけで**3件が誤りだった**（福井「一部しか公開していない」／
    和歌山「検算材料が無い」／**滋賀「スロット番号の行が無い」**）。
    滋賀は**行がちゃんとあった**のに、**全角で書かれていて、しかも
    「１５ １６ １７ １８」と1つの断片に潰れていた**ので当たらなかっただけ。
    ★**「無い」と書く前に、その1行を目で確かめること**（滋賀は5項目のうち4項目が誤り）
  - ★★**「数字が余って組めない」より「組めてしまう」ほうが危ない**（2026-08-17。滋賀）。
    滋賀は**縦書きのコールド「7回」**（数字の1行下に「回」）が落とせておらず、
    **その帯（8個）が1回戦（4試合）として読まれた。**
    コールドの数字は**試合の中心＝スロットの境目**に置かれるので、
    境目どうしの中点は `(n+0.5 + m+0.5)/2`＝**n+m が奇数ならまた境目に乗る**
    （1/2の確率で検査を通る。滋賀は4組すべて通った）。
    `stripVerticalInningMarks()` で落とす
  - ★**omyutech製SPAの県は「お知らせAPI」から連盟のPDFに辿れる**（静岡・山口・宮崎）。
    `other-api.omyutech.com/otherapi/rest/newsandtopic/list?userID=&language=&leagueId=<N>&type=N`
    と `.../detail?...&newsId=<M>`。`leagueId` は `main.*.chunk.js` にある県ごとの定数。
    ★**下の「ブラウザで開かないと分からない」は誤り**（2026-08-16 の記述）。
    ★**山形の「APIは追ってもサイト再ビルドで壊れる」も誤り** — 入口も `leagueId` も
    チャンクのハッシュが変わっても同じ。**山形もこの形に寄せられる**
  - ★★**茨城は取らないと判断した**（2026-08-17）。`/schedule` の中身は `webtoppage` API が
    返す `gameId` `team1Scores` … で、**一球速報の試合データベースそのもの**。
    **エンドポイントが `otherapi` 側であることは根拠にならない** —
    分けているのは**配信経路ではなく作った主体**
  - ★★**「結果を一部しか出していない」と判断した県は、画像とPDFを疑い直すこと。**
    福井は「優勝校の4試合だけ」としていたが**HTML本文しか見ていなかった**。
    同じページの画像 `…最終結果_page-0001.jpg`（**`_page-0001` は PDF変換の跡**）の
    元PDFに全26試合が入っていた。WordPress なら
    `/wp-json/wp/v2/media?mime_type=application/pdf` で添付を一覧できる
  - ★★**和歌山は準決勝の組み合わせを抽選で決める**（表に明記）。
    **枝をそのまま伸ばすと実在しない対戦ができる**（実際に作った）。
    甲子園の準々決勝以降と同じで**補わない**。
    ★★**ただし「補わない」と「出せない」は別だった**（2026-08-18）。
    **抽選の結果そのものが紙に縦書きで書いてある**ので、枝を伸ばすのではなく
    **そこを読めば出せる**（35試合すべて）。実際、枝の順（熊野vs智辯和歌山）と
    抽選後（熊野vs耐久）は**違っていた。**
    ★**検算は「その4校が準々決勝の勝者の並べ替えであること」。**
    ★**読めるようになったら `partial` を外すこと** ——
    付けたままだと**将来その読み取りが壊れて試合が消えても警告が出ない**
    ＋ ★★★**2026-09-01 に 和歌山を 35 → 551試合**（1 → 22大会・1 → 9年。2017〜2026年）。
    **春季・秋季は「大会履歴」（`history.html`）から取る**（夏は今までどおり `news.html`）。
    ★★**抽選があるのは夏だけ。** 春季は9枚とも `抽選` の字が無いことを確かめてあるので、
    **枝だけで組める（検算を弱めていない）。**
    ★★**紙の形は2つ** —— **A型は校名がスロット番号の左**（そのまま渡せる）、
    **B型は校名がスロット番号の右**（兵庫と同じで**スロット番号の列を校名と得点のあいだへ動かす**）。
    ★**見分けはスロット番号と同じ高さの字が左右どちらに多いか。**
    **「左に字がどれだけあるか」では見分けられない**（B型は左端に球場の凡例が縦書きで入る）。
    ★★★**検算は「紙の右端に刷ってある勝ち上がり校と、順番まで一致するか」。**
    **一次予選は二次予選へ進む4校**が並ぶので4校ぶん突き合わせられる。
    ★**いちばん右の列だけ読むこと**（B型は回戦ごとに勝者を刷り直すので準優勝校が混ざる）。
    ★★**勝ち上がる数は紙に書いていない**ので 1・2・4・8 を順に試し、
    **「チーム数 − 試合数 ＝ その数」と勝ち上がり校の一致の両方**を満たしたものだけ採る。
    ★★**得点とコールドの回数が1つの断片に潰れる**（`118` `1213`）。
    **2桁の得点だけが「N回」と接する**（1桁は42ポイント空く）ので、
    **回がすぐ右にある3文字以上の数字は先頭2文字が得点。**
    ★**`stripInningMarks` を先に、`splitMergedInnings` を後に**かけること
    （逆にすると `116` → `11` の直後に得点まで消える）。
    ★**不戦勝のある紙は組み立てられない**（3枚。大阪・石川と同じ）。
    ★**この紙は `Generated by E-league ©OmyuTech.` と刷ってある**が、
    **連盟のサイトに連盟名義で置かれたPDF**なので 2026-08-20 の方針のとおり。
    **スコアAPIからは取っていない。**
  ＋ ★★★**2026-09-01 その4 に 和歌山の夏を「大会履歴」からも取れるようにした**（551 → 620試合・22 → 24大会）。
  ★**お知らせ（`news.html`）は大会が終わると新人戦に入れ替わり、勝ち上がりトーナメントのPDFが消える**ので、
  **そこだけに頼ると過去年が取れず、いまの年も凍る**（実際に第108回の35試合が凍っていた）。
  ★**`history.html` の「選手権大会和歌山大会」から取る。読み手はお知らせのときと同じ `readSheet`。**
  ★★★**検算を「優勝校だけ」に弱めた**（運営者の判断）——
  **お知らせは優勝校と準優勝校を別々の見出しに書いているが、大会履歴の紙に刷ってあるのは優勝校だけ。**
  ★**弱めたのはそこだけで、①チーム数 − 試合数 = 1 ②準決勝に進んだ4校が準々決勝の勝者の並べ替えであること
  ③読んだ優勝校が準々決勝の勝者4校の中にいること、は残してある。**
  ★★**優勝校は「行の並び」で読む** —— `主催` の行の次にくる字だけの行が校名で、
  **`優勝` の字は校名より下**にある（あいだに `3年連続` `29回目の` が入る）。
  **`優勝` の位置で探さないこと。**
  ★**読めたのは2023・2025・2026年**（2026年は大会履歴の紙から**同じ35試合が1件も違わず出た**）。
  ★**2017〜2021年はやぐら型で紙の形がまるで違う**（`主催` の行も抽選の注記も無い。**その5枚はここで止まる**）。
  ★**2022・2024年は段の数が合わない**（1回戦24個・2回戦16個・3回戦8個まで読めて、その次の帯が無い）。
  ★★**2023年は「記念大会」で大会名に `記念` が入る** ——
  **読み手の大会名の形に `(?:記念)?` を入れるまで、その年だけ静かに0試合だった**（警告も出ない）。
    ~~★★夏（`news.html`）は更新できなくなっている~~ → ★**2026-09-01 その4 に解決**（上の項）。旧： ——
    お知らせが新人戦に入れ替わってPDFが消え、引き継ぎで35試合が凍っている。
    `history.html` に夏のPDFもあるが、**検算に使う準優勝校が紙に無い**（詳細はREADME）。
    ＋ ★★★**県のページが「何年も前の秋」を先頭に出していた**（2026-09-01 その6。**大分・愛知・和歌山**）。
    `latestSeasonGames` の**迷ったときの逃げ道**が `byOrder`（＝その県が持っている季節のうち
    **いちばん後ろの季節**）で、**年をまったく見ていない。**
    ★**日付の無い季節が1つでもあると必ずここへ来る**ので、秋を持っていれば必ず秋になる。
    ★**同じ関数にすでに「大会名から年を出していちばん新しい大会の季節を採る」がある**
    （`newestSeasonByYear`。2026-08-27 に宮崎のために足したもの）。**迷ったときもそちらを使う。**
    ★**41県で新旧を突き合わせ、変わるのはこの3県だけ**であることを確かめてある。
  ＋ ★★★**2026-09-01 その6 に 大分を 47 → 986試合**（2 → 26大会・1 → 13年。1998〜2015年）。
    **速報の一覧（`/sokuho/`）には今年の紙しか無い。それが薄さの上限だった。**
    ★★**トップの「過去の大会結果と記録」（`cgi/pgame_disp.cgi`）に 1998年度〜2023年度の
    年別ページがある。** `method="post"` だが**GETのクエリでも同じものが返る**ので
    共通の `fetchHtml` がそのまま使える（Shift_JIS。`decode()` が meta を見て直す）。
    ★**この一覧の「年度」は暦年**（第132回九州予選＝平成25年3月 が 2013 に載っている）。
    ★★**紙は2種類** —— ①**縦のやぐら表**（選手権大分大会・九州地区大分県予選。
    スロット番号が縦一列で回戦は左へ）②**横のやぐら表**（大分県高等学校野球選手権大会。
    速報の春季・秋季とまったく同じなので `readPrefSheet` をそのまま使う）。
    ★**いまの速報の夏の紙は横**（京都型）。**向きが年で変わっている。**
    ★★★**①で踏んだところ4つ**:
    **(a) `pdfPages` の「行にまとめる幅 3」が広すぎる** —— この紙はスロットの間隔が
    **15.3ポイント**しかなく 3 は 0.2 スロット。まとめの鍵は「先に見つけた行の y」なので
    数珠つなぎにずれ、**得点の断片が 42.95 にあるのに行の y が 45.1** になっていた。
    ★**本文は既定の幅で、枝は 0.6 で、と2回読む**（`pdfPages` に幅を渡せるようにした。既定は 3 のまま）。
    **(b) 1試合の2つの得点が1つの断片に潰れている**（`11 10` `7 0` `4 14`）。
    ★**`explodeNumberRuns` は使えない** —— あれは断片が**スロット軸**に伸びる紙のためのもので、
    この紙の断片は**回戦の軸**に伸びる。ほどくと2つ目の数字が**隣の日付の列へ飛ぶ。**
    ★**`flatFragments: true` で「断片は点」として読む**（鹿児島と同じ）。
    ★★**ただし潰れた断片は「下側の得点の位置」に刷ってある**ので中点が境目から **0.49** ずれ、
    許容 0.45 をわずかに超えて1回戦が読めない。**分かれている組の上下の間隔をその紙から測って、
    その半分だけ上へ戻す**（`liftMergedScores`）。**決め打ちの数字は使っていない。**
    **(c) 日付の列は落としてから組み立てる** —— 日付は**その回戦の得点の列の10ポイント内側**に
    **試合ごとに1つ**、**日にちだけ**（`14`）で刷ってあるので数字として読まれ、
    **1回戦の帯として通ってしまう**（滋賀・沖縄と同じ「組めてしまう」壊れ方。
    実測では 0.47 ずれで**たまたま**落ちていた。**運に頼らない**）。
    ★**見分けは値ではなく形で**：「数字だけの列」で、その 7〜13 ポイント外側に
    **ちょうど2倍の個数の数字を持つ列**があること。★**落とすので日付は出さない**（`date: null`）。
    **(d) 校名は横書き。** 向きを入れ替えたあとは `nameOrder: "desc"`（"asc" だと `柳ヶ浦` が `浦ヶ柳`）。
    ★★**検算3つ**: ①チーム数 − 試合数 = 1 ②**紙に縦書きで刷ってある `優勝◯◯高校`**
    （同じ x の断片を上から順につなぐ。★`準優勝` の列を拾わないこと。
    ★**校名に入らない字（括弧・コロン）を認めないこと** —— 2012年夏は同じ列に
    `（初優勝）` と `【選手宣誓：別府鶴見丘高校` が並び、緩いと
    **`）【選手宣誓：別府鶴見丘` を優勝校として読む**）③**紙の「大会期間」の元号年が取りに行った年と一致**。
    ★★★**あわせて甲子園の出場校と突き合わせた（12大会すべて一致）** ——
    このリポジトリは `src/lib/data/koshien-games.json` に**代表校の県**を持っている。
    **出典もサイトもまったく別**（あちらは ja.wikipedia の大会記事）なので、
    **「構造の検算は通るのに優勝校が違う」を外から止められる。**
    ★**紙の優勝校との一致は「1文字目から2文字」しか見ていない**（`大分商業` と `大分東` は通る）ので、
    **次に夏の県大会を足す人はこの突き合わせを必ず1度やること。**
    ★★**季節は期間の最初の月**（3〜6月＝春季／7〜8月＝夏／9〜11月＝秋季）。
    九州地区の県予選は **3月23日〜4月4日**・**9月15日〜10月4日**のように月をまたぐ。
    ★★**九州地区大分県予選は大会名の頭に西暦を足す**（宮崎・愛知・富山と同じ。
    回数は九州地区大会の通し番号で年が出せず、日付も出さないため）。
    **選手権大分大会には足さない**（`+1918` で年が出る）。
    ★★★**あわせて「大分の春季が毎回落ちて凍っていた」のを直した** ——
    `第149回大分県高等学校野球選手権大会` は **149 + 1918 = 2067年**になり、
    **「大会名の年と試合の年が食い違う」検査に毎回落ちて「前の内容を残す」で凍っていた**
    （画面は正しく見えるので気づけない）。★**ありえない年になったら、その規則は当たっていない。**
    `yearOfTournament`（**スクリプト側とサイト側の2か所**）と検査の両方に歯止めを入れ、
    **検査の順番も `yearOfTournament` にそろえた**（西暦 → 元号 → 回数。
    `2017年 第70回愛知県…選手権大会` のように**両方入っている名前**があるため）。
    ★**まだ読めない**: **2007〜2011年**（PDFに文字が1つも入っていない＝画像）・
    **2005年**（記事にPDFが無い）・**2016〜2022年**（記事そのものが無い）・
    **2023年**（画像）・**2014年の第134回**（**不戦勝が1件**あり枠が埋まらない）。
    ★**OCRは対象外**（京都の画像・兵庫のGIFと同じ線引き）。
  ＋ ★★**2026-09-01 その6 に 福井を 78 → 105試合**（3 → 4大会。2025年の夏が入った）。
    **直したのは読み方2つ**:
    ★★**「優勝」が行の先頭に無い年がある** —— 2026年は `優勝\t敦賀気比高校（…）` だが、
    2025年は `セーレン…で実施\t優勝\t敦賀気比高校（…）\t（敦）---…`。
    **行の先頭で探していたので26試合が丸ごと落ちていた。断片で探して次の断片を採る。**
    ★★**日付を1つも刷っていない紙がある**（2025年の夏）。**枝に日付が無いだけ**なので
    そこを理由に大会ごと落とさない（`date: null`。**春季は前からそうしていた**）。
    **中途半端に欠けているときだけ落とす。**
    ★★**日付を1つも持たない大会は大会名の頭に西暦を足す**（宮崎・愛知・富山と同じ）——
    `第154回北信越地区高等学校野球大会福井県大会（春季）` の回数は北信越大会の通し番号なので、
    **足すまで「年の分からない大会」として県のページの別枠に落ちていた。**
    ★**添付の一覧（WordPressのメディア）は 2024年5月ごろまでしか無い。**
    **2025年の春は県大会の結果PDFそのものが無い**（組合せ表と北信越本大会の紙だけ）。
    ★**`152haru_katiagari` は北信越の本大会**（他県の学校が出る）。**県大会と間違えないこと。**
  ＋ ★★**2026-09-02 に 滋賀を 320 → 368試合**（令和5年度の春季が入った）。
    ★★**連合チームの凡例で、丸数字とコロンの順番が年で逆になる** ——
    令和6年度は `合：① 湖南農業・甲南・…`、**令和5年度は `連合①：信楽･愛知・…`**。
    **「丸数字のあとにコロンは来ない」と決めていた**ので令和5年度は1件も読めず、
    **「連合チームの内訳が紙から読めない」で春が丸ごと落ちていた。**
    ★**中黒が半角の年がある**（`信楽･愛知`）。**寄せないと画面の区切りが1件だけ半角になる。**
    ★**残るのは令和5年度の夏**（50チームに48試合）**と秋**（組み立てられない）。
  ＋ ★★**2026-09-02 に 広島の過去年を実装したが、試合は1件も増えていない**（**紙のほうが足りない**）。
    ★**入口はある** —— 「硬式部 各種大会」の中の**「過去の大会」**（`sites.google.com/view/taikaikankei/大会関係過年度`）
    から年度ごとの Google Sites に辿れる。**トップページには入口が無い。**
    ★★**リンク名の入り方が年で違う** —— いまの年は `aria-label="組み合わせ表"`、
    **2024年は `<a>` の中の文字が `組み合わせ表（PDF形式）`。片方だけ見ていると0枚**（実際に0枚だった）。
    ★★**紙の寸法が年でまるで違う**（いまの年は x が 42〜2,900、**2024年は 42〜546**）ので、
    **左右の境目は紙から測る**（`halfOf`）。決め打ちの 1400 では右半分が丸ごと左に入る。
    ★★★**スロット番号が逆さの半分がある** —— **2024年の紙は右半分だけ 86→44**（下へ行くほど減る）。
    `assembleSlotBracket` は「上から 1,2,3…」が前提なので**連番が1つも見つからず組み立て前に落ちる。**
    ★**`readTwoColumnBracket` に `mirrorSlots` を足した**（**既定は両方 false**。使う県だけ）。
    ★★**それでも2024年の夏は出せない** —— **日付の札が78試合ぶんしか無い**
    （`7/12呉(28)12:30` の `(28)` が試合番号。最大 `(78)`）。**86チーム＝85試合**のはずで、
    **7試合ぶんが紙に無い。読み手を直しても増えない。**
    ★★**春季・秋季は左右2段組ではない**（スロット番号の連番が**左9個・右16個**）。
    **題と年の出し方は `TITLES` に測ってある**が、**1段の紙として別の読み手が要る**ので
    `seasons` には入れていない（足すと毎回の実行で警告が2本出るだけ）。
  ＋ ★★**2026-09-01 その6 に 福島の「過去の大会記録」は使えないと確かめた。**
    `renmeikankei/past_record/indexrecord.html` に春・夏・秋の戦績PDFが並ぶが、
    **4本とも文字が1行も入っていない（画像）。** ★**再調査しないこと。**
  ＋ ★★**2026-09-01 その6 に 岐阜の `verify` で `実業` も畳むようにした**（+62試合）。
    **`東濃実業`（1回戦）と `東濃実`（準々決勝）が別のチームとして数えられ**、
    63チームの大会が **64チーム**に見えて「決着した試合が1つ足りない」で
    **2025年の夏が丸ごと落ちていた。**
    ★★**2024年（第106回）は出典側の欠落**（新サイトの一覧に第107回と第108回しか無く、旧サイトにも日別の結果が無い）。
    ★**この畳み方は数えるためだけのもの**（画面に出す校名は紙のまま）。
    ＋ ★★★**2026-09-01 に 三重を箱スコアから取った**（114 → 445試合。2 → 10大会・4年）。
    `result_category/past/` の**回戦ごとの「詳細試合結果」PDF**は
    **トーナメント表ではなく箱スコア**（石川と同じ、いちばん強い形）。
    ★★**検算は「各回の得点の和＝印刷された合計」**（試合ごと。**合わない試合だけ落とす** ——
    箱スコアは試合が独立していて、1件の読み違いが他に伝播しない）。
    ★★**列は見出し（`チーム名 … 計`）から測ること** ——
    紙によって校名の欄が x=18〜58 だったり x=59〜124 だったりする。
    **`x < 150` の決め打ちは、別の紙で得点を校名に食い込ませる**（`海星200`）。
    ★★**年は「お知らせの掲載日」から出す**（静岡と同じ。紙には月日しか無い）。
    ★**大会名の回数から年を出さないこと** —— `第72回春季東海地区…` は
    **東海地区大会の通し番号**（宮崎の「第158回」と同じ）。
    ★**掲載日より後の試合はありえない**ので、そこを検算にする（1月に前年の秋を載せる形も受ける）。
    ★★★**サヨナラの印は半角と全角が混ざる**（`2x` と `2ｘ`。**同じ紙の中に両方ある**）。
    **NFKC で寄せないとその回の得点が読めず、その試合だけ静かに落ちる。**
    ★★**校名の空白は2種類ある** —— `四 日 市 四 郷` は字間（落とす）、
    `南伊勢 石薬師 四郷` は**連合チームの区切り**（残す）。
    **空白で割った塊が全部1文字かで見分ける**（`cleanBoxScoreName`）。
    ★**校名が2行に折り返される紙がある。「・」は補わず空白でつなぐ。**
    ★★**トーナメント表と二重に入らないよう、大会名で寄せて試合数の多いほうを採る**
    （表は日付を持たず箱スコアは持つので、重複の鍵が食い違って両方残る）。
    ★★**収録するのは「三重県の大会」だけ** —— `三重大会` `三重県大会` を含まないものは外す
    （`第104回全国高等学校野球選手権大会` は甲子園、
    `第75回秋季東海地区高等学校野球大会` は東海地区大会そのもの）。`地区予選` `シード決め` も外す。
  - ★**`slot-bracket.mjs` に足した9つ。どれも既定を変えていない**
    （毎回**全県を再生成して既存県の生成物が1バイトも変わらない**ことを確認した）:
    `hitSpan`（スコアが連結線の両端に置かれる表。山口・宮崎・和歌山）／
    `maxGap`（離れた「回」がスコアを消すのを止める。宮崎）／
    `verify.teams`（出典が書いている出場チーム数と突き合わせる。宮崎・滋賀）／
    `×` の除去（サヨナラ `7×`。福井）／
    **`explodeNumberRuns()`**（潰れた数字の断片をほどく。滋賀）／
    **`pairedScores`**（`11-1` を左右2つの数として読む。滋賀）／
    **`stripVerticalInningMarks()`**（縦書きのコールド。滋賀）／
    **`labelReach`**（日付・球場がスコアの帯より**上**にある表。滋賀）／
    ★**`roundBandGap`**（回戦の帯をまとめる幅の**上限**。静岡の春季。
    **日付の帯がスコアの帯の11〜12ポイント下にある表**では、既定の
    「回戦の間隔の0.45倍」が広すぎて日付を巻き込み、数字がちょうど1.5倍になって必ず落ちる）／
    ★★**`fragmentWidth`**（2026-09-01。大分。**断片の中の字送りを、代表的な文字幅ではなく
    実測の幅から出す**。`pairedScores` を立てても同じことが起きるが、あちらは
    **`11-1` を2つの数として読む**という別の働きも付いてくるので分けてある。
    2012年春の紙は **`11 ５` と半角と全角が1つの断片に混ざる**ので、
    代表的な文字幅では2つの位置が 0.84 スロット離れ、**中点が境目から 0.50 ずれて
    1回戦の帯ごと捨てられていた**）
  - ★★**`pdf-text.mjs` の `pdfPages` に「行にまとめる幅」を渡せる**（2026-09-01。大分。既定は 3 のまま）。
    **スロットの間隔が 15 ポイントしかない紙では 3 が 0.2 スロットにあたり**、
    まとめの鍵が「先に見つけた行の y」なので数珠つなぎにずれる。
    ★**小さくすると本文の行はばらばらになる**ので、**本文は既定の幅で、枝は細かい幅で、と2回読む。**
  - ★**断片の中の位置は「代表的な間隔 × 文字数」で見積もらないこと**（2026-08-17）。
    紙によって字送りが違い、実測で最大5ポイント（＝0.4スロット）ずれた。
    **`pdf-text.mjs` が返す `width`（断片の幅）を文字数で割る。**
    既存の読み手は `x` と `text` しか見ていないので、足すだけで済んだ
  - ★**詰まったら `BRACKET_DEBUG=1`。** 1回戦の帯の選ばれ方、各回戦の
    「中点／帯の数字と位置／一致数」、**組み立て前に落ちた理由**、
    **組み立てた試合の一覧**が出る（山口・和歌山・滋賀はこれで解けた）。
    ~~★**生成物には公立が絡む試合しか残らない**ので、
    **私立どうしの試合が合っているかはこの一覧でしか確かめられない**~~
    → ★**2026-08-21 に方針が変わり、生成物にも私立どうしの試合が入る**ので、
    **生成物を直接見て確かめられる**（下の「私立の戦績も引用する」）
  - ★★**「同じ県の別の季節」でも測り直すこと**（2026-08-17。山口の春季）。
    紙の形は夏とまったく同じなのに**4か所で落ちた**:
    ①**年の出し方**（春は「令和8年度」で回数が無く、`第N回 + 1918` が例外になる → `yearOf`）
    ②**シード記号と校名が1つの断片になっているスロットがある**
    （`① 下 関 国 際`。記号の列を外す `ranges` に巻き込まれて校名が消える →
    `splitLeadingMark()` で**断片を割ってから列で外す**。★記号を文字で消さない）
    ③**中央の縦書き「（３年ぶり８回目の優勝）」の `回` が103ポイント左のスコアを消す**
    （宮崎と同じ → `inningMarkGap`）
    ④**シード記号の x が季節で違う**（春 545.6／夏 556。`ranges` を季節ごとに持つ）
  - ★★**継続試合は日付を2つ持つ**（2026-08-17。山口の春季）。
    雨で中断した試合は翌日に再開されるので、紙に開始日と再開日の両方が載る。
    - **日付の個数の検算は「紙が『継続試合』と書いている個数だけ」超過を認める**
      （`continuationMark`）。**「1件くらいずれてもよい」にしないこと**
    - ★**再開日が枝ではなく「注記の列」に書かれている紙がある。**
      枝には開始日しか無く、`pickDate` では拾えない。
      **注記と枝の対応づけはしない**（座標の近さしか手掛かりが無く、
      間違えると別の試合に別の日が付く。**それは「日付が無い」より悪い**）。
      **中断した日の試合をまとめて日付なしにする**（`dropContinuationDates`）
  ★**出典のURLを追えない県では、ファイルIDを直に持ってよい**（2026-08-16。山形）。
  山形はSPAで、Driveのリンクが omyutech の告知APIからしか辿れない
  （★上記のとおりAPI自体は安定しているので、寄せ替えてよい）。
  ★**連盟が同じファイルを上書き更新している**ので、IDを固定しても大会中は自動で追随する
  （お知らせは6/25付なのにPDFには7/26の決勝まで入っていた）。
  **大会名はPDFから読むこと**（IDと一緒に書くと差し替え時に古い名前が出る）。
  ★**出典は連盟だけではない。** 埼玉・神奈川は個人運営の情報サイト、
  **愛知は開催中の大会だけ県内のケーブルテレビ局14社の応援サイト**（`catvase.jp`）から
  補っている（★**2026-08-30 に主たる出典は連盟に替えた**）。
  連盟が出していない県はこの筋を探すこと。**出典表示は必ずそのサイトの名前で出す**
  （連盟の名前で出さない）。
  ★**出典を増やす前に README の「都道府県高野連サイトの規約調査」を読むこと。**
  47連盟を調査済みで、**12連盟は転載・複製を制限している**（北海道・青森・秋田・
  東京・鳥取は「データ」、埼玉・福島は「内容」「コンテンツ」を名指し、
  大分は営利目的の複製を禁止、岩手・宮城・島根・栃木は写真・記事の無断転載を禁止で
  うち3県は「リンクはトップページへ」）。**この12連盟を `ADAPTERS` に足さない。**
  ★**`data/federation-sites.json` の自動分類を鵜呑みにしない。** 福島・栃木は
  Copyright表記と同じ行にあったため分類から漏れていた（2026-08-13 に訂正）。
  足す前にその連盟のページを開いて「転載・無断・複製・営利」を自分で検索すること。
  ★★**2026-08-20 に方針を変えた。** 以下の「omyutech からは取らない」は**もう有効ではない。**
  新しい整理は「**連盟の公式サイト上で連盟名義で公開されている試合結果は、
  連盟の著作物として扱い、その数値（事実）を引用する**」（運営者判断）。
  これで**茨城・岡山・香川・高知・長崎の5県**を足した（`omyuAdapter`）。
  ★**前提を知らずに戻さないこと。** 塞いでいたのは**著作権ではなく利用規約の第20条**で、
  **著作権の面では元から問題になっていなかった**（スコアは事実）。
  判断材料と踏んだところは README の「★★2026-08-20 に方針を変えた」に全部ある。
  ★**文章は取らない。数値・校名・日付・球場・回戦だけ**にすること。
  ★~~一球速報（omyutech）については「取ってはいけないのは omyutech から取ること」だけ。~~
  8連盟（山形・茨城・静岡・山口・香川・高知・長崎・宮崎）はサイトの中身を
  `other-api.omyutech.com` から描いている（2026-08-14 判明）。
  **そこからは取らない**（連盟の規約ではなく業者側の規約で塞がれる。
  JavaScriptで描いていて取れない、ではなく取ってはいけない）。
  ★★**しかし「一球速報の県だから連盟からは取れない」は誤りだった**（2026-08-15）。
  **この分類にしていた岐阜・千葉・福井は3件とも、連盟が自分で結果を出していた**
  （岐阜＝日別のスコア表PDF11本、千葉＝やぐら表PDF、福井＝HTML。岐阜・千葉は実装済み）。
  ★**「サイトに omyutech がある」と「連盟から取れない」は無関係。**
  鹿児島・石川・千葉・岐阜は omyutech へのリンクを持ちながら連盟自身のPDFから取っている。
  ★**2026-08-16 に9県すべてを人がブラウザで開いて確かめた。**
  **殻は9県とも同じ omyutech の SPA だが、添付ファイルの置き場所は県ごとに違う**:
  **山形・静岡＝Google Drive**（omyutech と無関係。山形は実装済み）、
  **茨城・山口・宮崎＝`safe-api.omyutech.com` に連盟の文書**、
  岡山・香川・高知・長崎＝連盟の結果文書が見つからず。
  ~~★判定はブラウザで記事を開いて添付の行き先を見る。~~
  ★**2026-08-16 に自動化できた**（上記のお知らせAPI）。ブラウザは要らない。
  ★**運営者の判断で「連盟が作った文書なら omyutech のファイル置き場からでも取る」と決めた**
  （2026-08-16）。**スコアAPI（`baseballapi`）からは取らない**のは変わらない。
  詳細はREADMEの「★★旧『一球速報』9県を実際に開いて確かめた」。
  ★**2026-08-14 に47県すべての可否を確認した。HTMLでスコアを出している連盟は
  もう残っていない。** 残るは「PDFの14県」「Excelの新潟」「個人運営の情報サイト」。
  **連盟のサイトをもう一度見に行っても増えない**（READMEの「47都道府県の可否」）
  - ★**PDFの13県も全部開いた（2026-08-14）。スコア表だったのは愛媛だけ**で、
    残り12県は**トーナメント表**。組合せ表は点がどの対戦のものかが
    **線の位置でしか決まらない**（文字の並びに入っていない）。**推測で組み立てない。**
  - ★**トーナメント表は原則として出典にしない**（2026-08-14 に富山・石川で確認）。
    石川は格子状で組み立てられ、**検算（準々決勝4・準決勝2・決勝1）も通った**のに、
    **決勝の相手が違っていた**（事実は金沢3-4x遊学館、組み立ては金沢vs小松大谷）。
    許容幅を狭めれば決勝が組めず「決勝で負けた学校が決勝進出のまま」残る。
    **検算を通ってもなお間違える。**
  - ★**校名の欄と、その外の列を混ぜないこと**（2026-08-15。千葉）。
    千葉はシード記号（Ａ・Ｂ・Ｃ）が校名とは**別の列**に並ぶ
    （左 x=31 ／ 校名 37〜78、右 校名 513〜556 ／ 記号 x=561）。
    ★**記号を「文字で消す」作りにしないこと。** 右の x=560 に記号でない
    「宣」が1つ紛れており、`千葉東` が `千葉東宣` になっていた
    （**画面に実在しない校名が出るところだった**）。**列ごと範囲で外す**（`ranges`）。
    ★**全角ラテン文字を無条件に落とすのも駄目**（「光英ＶＥＲＩＴＡＳ」が壊れる）。
  - ★**同名の県立・市立がある県は対応表で受ける**（千葉は5組で最多。
    千葉・船橋・柏・松戸・銚子）。出典が「市立◯◯」と書き分けていて、
    **同じ表に両方が別のスロットとして載っている**なら、
    「市立が付かないほうが県立」は推測ではなく読み取り。
    **書き分けが無ければ結び付けないこと。**
  - ★**引き分けを「負け」と書かない**（2026-08-15。岐阜）。高校野球には
    **引き分け再試合**がある（市岐阜商 0-0 県岐阜商 が翌日 0-10 で再試合）。
    `won` は両方 false になるので、**「勝っていない＝負け」と読むと画面に事実と
    違うことが出る。** スコアが同じかで判定する（`RegionalDistrictCard` は △ と書く）。
    検算も**「引き分けを除いた決着した試合が チーム数−1」**にすること。
  - ★**同じ学校が回戦で書き分けられる出典がある**（2026-08-15。岐阜で5組:
    中津商/中津商業・多治見工/多治見工業・岐阜聖徳学園/岐阜聖徳など）。
    **畳まずに数えると「のべ出場校 − 試合数」が合わず検算が落ちる。**
  - ★**組み立てを考える前に、PDFの全ページを見ること**（2026-08-15。石川）。
    石川の `108nyagura-kekka-photo.pdf` は**1ページ目がやぐら表で、
    2〜5ページが試合ごとのイニングスコア表**だった。
    **スコア表があれば組み立ては要らない**（どの点がどの対戦かが紙に書いてある）。
    ★**石川を足したのは「トーナメント表を出典にする」決定の変更ではない。**
    **やぐら表は今も組み立てていない**（優勝校の検算にだけ使う）。
    スコア表型の検算は組み立て型より強い:
    ①各回の得点の和 == 印刷された合計（試合ごと）
    ②3回戦以降の出場校が全員「前の回戦の勝者」か・勝ったのに次にいない学校がいないか
    ——**②が、以前すり抜けた「構造は合うのに対戦相手が違う」を必ず捕まえる。**
    ★★**2026-08-21 にまた同じ轍を踏んでいたのが見つかった**（大分の支部予選）。
    「県北・久大以外の支部の紙が見つからない」としていたが、**同じPDFの2〜4ページ目**に
    別杵・大分・県南豊肥の3支部がそのまま入っていた。
    ★**アダプタが `parsed[0]` しか読んでいないなら、それは「1ページ目しか見ていない」**
    ということ。**「無い」と書く前に必ずページ数を数えること。**
  - ★★**「お知らせ一覧」しか見ていない県は、別の索引を探すこと**（2026-08-26。石川）。
    石川は `?page_id=213`（今年ぶん16件しか残らない）から辿っていたので遡れなかったが、
    **`?page_id=29`「過去データ」に1998年からの年ごとの一覧**があり、
    各年に試合結果PDFが並んでいた（41 → 169試合）。
    ★**年の見出し（`<h1 class="entry-title2">2015年</h1>`）で切って、その年の塊だけ見る。**
    ★**中止・順延の枠がある** —— `◆球場 第1試合 / 飯田 - 羽咋 雨天により中止、順延`。
    **枠はあるが試合は行われていない**ので**枠ごと飛ばす**（順延先の日に改めて載る）。
    読もうとして「読めない枠がある」で**4年ぶんを丸ごと落としていた。**
    ★**枠の中の窓は紙によって違う**（2019年は合計が +141.8、2026年は +166.8）。
    ★**「枠の中の順番（校名→各回→合計）で読む」案は試して戻した** ——
    **準々決勝以降は打者ごとの成績まで並ぶ箱スコア**になり、順番では合計を取り違える。
    **窓を紙ごとに測り直す作りが要る。2021年以前はそこから。**
    ★~~春季・秋季（北信越地区の県大会）も同じ理由で未対応~~ → **2026-08-31 その2 に足した**（上の項）。
    ★**北信越の「第154回」は通し番号。年も季節も出さないこと**（月で決める）。
  - ★**京都・広島・三重・鹿児島は例外として組み立てている**（鹿児島は 2026-08-15）。
    組み立ては `scripts/lib/slot-bracket.mjs`。**扱える表の条件がそこに書いてある。**
    要点は2つで、**どちらも石川は満たさない**:
    ①**1回戦のスコアがスロットの中心に置かれる**ので、シード（不戦）と対戦の
    区別が推測なしで付く ②**各回戦の数字の個数が試合数のちょうど2倍**なので、
    左から順に対応させられ位置の推測が要らない。
    ★**新しい県に流用する前に、この2つを実データで確かめること。**
    ★**検算は「枝とは別の場所に書いてある事実」と突き合わせる**
    （合計試合数・優勝校・日ごとの試合数・球場ごとの試合数）。
    **1つでも合わなければその大会を1試合も出さない。**
    ★**検算材料が無い表もある**（広島には優勝校も合計も無い）。その場合は
    構造の検算（スロットが欠けない・チーム数−試合数=1・日付の個数=試合数）で
    代用するが、**検知力は落ちる**ので外部との突き合わせを必ず1度行うこと
  - ★★★**同じ県でも紙の組み方が年でまるで違う**（2026-08-27。鹿児島を 61 → 181試合）。
    **1つの読み方で16枚を通そうとしないこと。** 実データで突き止めた6つは全部
    **既定を変えない引数**にしてある（`--pref mie` `--pref chiba` を再生成して
    生成物が1バイトも変わらないことを確認済み）:
    - ★★**コールドの丸数字は前に付く紙と後ろに付く紙がある**（`10⑤` と `⑥11` `⑦ 8`）。
      `numbersOf` は**後ろしか落とさない**ので、前に付く紙は**その試合のスコアが丸ごと消える**
      （第153回は1回戦が30個→25個になり、帯を取り違えて大会ごと落ちていた）→ `leadingInningMark`
    - ★★**帯は「断片の左端」ではなく「断片の中の数字の位置」でまとめる** → `bandAtCenter`。
      左端で見ると桁数と丸数字で**9〜11ポイントずれ**、幅を広げて吸収すると**深い回戦を巻き込む**
      （第157回は準々↔準決が40ポイントなのに準決↔決勝は16ポイントしかない）。
      ★**寄せるのはスコアの断片だけ** —— 幅60ポイントの `県9日11：30` に同じ計算をすると
      **30ポイント動いて日付の帯が壊れる**
    - ★★**スロットが縦の紙では、断片はスロット軸には点** → `flatFragments`。
      `numbersOf` の「断片の中の文字位置から見積もる」は**横一列の紙（京都）のための作り**で、
      縦の紙では `⑤ 10` の `10` が丸数字1文字ぶん動く（0.46スロットずれて帯ごと落ちた）
    - ★★**離れた「回」がスコアを消す**（宮崎と同じ）→ `inningMarkGap`。
      中央の縦書き「（2年連続7回目）」の `回` が、**同じ行の117ポイント左**のスコアを消していた
    - ★★★**スロット番号の外側の行は、行ごと落としてから組み立てる。**
      会期の行（幅206ポイント）の左端が**準決勝と決勝の得点のあいだ**に落ち、
      **そこを踏み台にして帯がつながらず、準決勝の2つが別々の帯に割れる。**
      ★**大会名の行だけ残す**（そこから大会名を読むため）。★**凡例と会期は `raw` から読む**
    - ★★**いちばん深い帯は「枝の張る幅の中の、いちばん外側の2つの中点が予測した中点に来るか」で選ぶ。**
      準決勝は**スコアが連結線の両端**にあるので中点では当たらず、
      **決勝の得点しか無い帯のほうが勝って準決勝を取り逃がす。**
      ★この条件なら**中央の縦書きの数字**（中点が合わない）は落ちる。★許容は 1.2
      （深い回戦は誤差が積もる）
  - ★★**決勝にだけ日付が刷られていない紙がある**（`finalDateOptional`。2026-08-27。鹿児島）。
    同じ県でも年で違う —— 第108回はラベル61件＝試合61件、**第106回・第157回は1件少ない。**
    ★★**このとき決勝に付く日付は別の試合のラベルを拾ったもの**で、
    **放っておくと画面に嘘の日付が出る**（第157回は準決勝の 10/9 が決勝になっていた）。
    ★**1件足りないときは決勝の日付を捨てて `null` で出す。枝の検算は緩めない。**
  - ★**不戦勝のある紙は組み立てられない**（群馬・鹿児島）。紙が
    `市来農芸が棄権の為伊集院が不戦勝` と**文で書いている**ので、
    **その文を読んで「不戦勝がN件」と言ってから落とすこと**（「数字が足りない」より原因が分かる）。
  - ★**鹿児島は「表の外」に検算材料があった**（2026-08-15）。連盟のトップページが
    「決勝戦 神村学園高等部 ９ ー ０ 鹿児島実業」と**文章で**書いている。
    **表の枝とは別の場所から来る事実**なので、石川で通ってしまった
    「構造は合うのに決勝の相手が違う」を止められる。**出典のページ本文も検算材料として探すこと。**
    校名は部分一致で比べる（表「神村学園」／文章「神村学園高等部」）が、
    **点数と勝敗は完全一致を要求する。**
  - ★**上下2段組・決勝が準決勝と同じ帯・日付に月が無い表も扱える**（鹿児島で実装）。
    `finalInCenter` / `parseLabel` / `expand` / `finalAt: "center"`。
    ★**日付の月を決め打ちしない**（鹿児島は表の開催期間の行から決めている）。
    ★**日付の帯は「スコアの帯に近い順」ではなく「前の回戦の帯より上のいちばん低い帯」で選ぶ。**
    準決勝と決勝はどちらも1試合なので個数では区別が付かず、近さで選ぶと入れ替わる
    （鹿児島は日付の位置が**上半分と下半分でも違った**ので、段数の決め打ちも効かない）
  - ★**校名が2行に組まれている表がある**（三重の連合チーム2件。2026-08-15 に修正）。
    **同じ行の文字はスロット軸の座標が等しい**ので、先にスロット軸でまとめてから
    `nameOrder` の向きで読む。**「・」は補わない**（どこが学校の切れ目かは字間でしか出ず、推測になる）
  - ★**トーナメント表は「1〜N と並ぶ行があるか」で一発で選別できる**（2026-08-14）。
    無い表（沖縄）は**勝者の校名を回戦ごとに書き直す形式**で、参加校数すら数えられない。
    **先に日付と検算材料の有無も見る。**
    ★**ただし「検算材料が無い」の判定は甘くなりやすい**（2026-08-17）。
    和歌山は日付が無いのは本当だったが、**お知らせの一覧に優勝校と準優勝校が
    別々の記事として載っていた。** 出典のページ本文まで探すこと。
    ★★**滋賀は「1〜N の行が無い」と分類していたが誤りで、2026-08-17 に実装した**
    （夏46・春45）。**行はあった** —— 全角で、しかも「１５ １６ １７ １８」と
    潰れていたので `/^\d+$/` に当たらなかっただけ。
    ★**「行が見つからない」と「行が無い」は別。**
    見つからないときは**全角・潰れ・2行に割れ**を疑うこと。
    詳細はREADMEの「見分け方」と「★★2026-08-17 に 滋賀」
  - ★**外部の情報源と食い違ったら、どちらが誤りかを「その後の試合」で確かめる。**
    広島で外部が「安芸府中 3-2 武田」と書きながら、同じ情報源が
    武田の2回戦・3回戦を載せていた（＝武田が勝っている）。**外部が誤りだった**
  - ★★**検索結果の要約を突き合わせの材料にしないこと**（2026-08-17。山口の春季）。
    要約は準決勝を「2-0」と書いていたが、**連盟の紙は `0` と `10`**
    （断片の幅も1桁/2桁で合っており、**4/27版と4/28版の2つの紙で同じ**）。
    **中断時点のスコアと混同していた**とみられる。
    **一次資料（連盟のPDF・お知らせ本文）まで降りること。**
    Wikipediaの要約で対戦相手が入れ替わったのと同じ轍
  - **Excelは `scripts/lib/xlsx-rows.mjs` で読む**（2026-08-14 導入。`exceljs`）。
    新潟がこれ。★**1行に試合が横に2つ並ぶ**ので、見出しの「校名」列を全部拾うこと
    （左だけ読んで試合がちょうど半分になり、検算で気づいた）。
    ★**「本大会」「甲子園」のシートを外す**（他県の学校が混ざる）
  - **PDFは `scripts/lib/pdf-text.mjs` で読む**（2026-08-14 導入。`pdfjs-dist`）。
    **devDependency。サイト側のコードから import しない**（35MBがバンドルに入る）。
    ★**座標つきで取り出して行に組み直す。** pdf.jsが返す並びは**描画順であって
    読む順ではない**ので、素直につなぐと表が壊れる。
    ★**1枚のPDFの失敗で1季節を落とさない**（例外を投げず null を返す）
  - **埼玉・神奈川は連盟ではなく個人運営の情報サイトが出典。** 出典表示はサイト名で出す
  - **甲子園（`live-results.ts`）とは別スクリプト・別ファイル。** 出典が県ごとに違い、
    1つにまとめると1県のサイト変更で全国の生成が止まる
  - ★★**取るときは私立の戦績も引用し、着目するところを公立にする**
    （2026-08-21 に運営者が決めた。**それまでは公立が絡む試合だけを生成物に残していた**）。
    **生成物（`src/lib/data/regional/<県>.ts`）には私立どうしの試合も入っている。**
    ★**絞り込みは表示側（`latestSeasonGames`）にある。**
    **県のページに私立どうしの試合を出すという意味ではない。**
    ★**片方だけ戻さないこと**（生成側だけ戻すと画面は変わらないが
    トーナメント表が作れなくなり、表示側だけ外すと画面に私立どうしの試合が並ぶ）。
    ★**なぜ必要だったか**: 落とすと**トーナメント表が作れない。**
    枝が欠けるだけでなく、**次の公立の試合に誰が上がってきたのかも辿れない**
    （方針変更前は全4,033試合に対し私立どうしが**0件**＝全部落ちていた）。
    ★**ベストNを数えるための `allGames` を別に持っていたのも同じ理由**で、
    「表示は公立だけ／数えるのは全部」を2本のデータでやっていた。
  - **トップは抜粋（`regional-pickup.ts`）しか読まない。** 県ごとの全試合は
    `src/lib/data/regional/<県>.ts`（1県約100KB）。47県ぶんを1つにすると6MBになる
  - ★★**進捗地図（`/regional`）は `regional-progress.ts`（4.4KB）だけを読む**
    （2026-08-22）。49地区ぶんを一度に描くので**県ごとのファイルは読めない。**
    ★**季節は「今日の日付」で決めない**（いちばん新しい試合の季節）。
    ★**大会の年を出してから「今季」かを判定する**（`yearOfTournament`）——
    **日付が1つも無い季節がある**ので（実測：夏7県・春3県・秋1県）、
    「新しい試合から120日」の窓が効かず、**前年の決勝を『終了』として出していた**（宮崎）。
    ★**1つの季節に複数の大会が入る県がある**（徳島の秋は5大会）。
    **選ぶのは「最後の試合がいちばん新しい大会」。試合数の多い大会ではない**
    —— 試合数で選ぶと**終わったブロック予選を「県の今季」として出す。**
  - ★★**収録する大会の範囲が決まっている**（2026-08-23。運営者）。
    **甲子園**（春のセンバツ・夏の選手権）／**都道府県**（春季・夏・秋季）／
    **地区大会**（東北・関東・北信越・東海・近畿・中国・四国・九州 の春季・秋季）／
    **明治神宮大会**。
    ★★**新人大会を「秋季大会」として入れないこと。** 秋に開かれるので
    季節の判定は秋になるが、**春季・夏・秋季とは別の大会**である。
    **1年生大会・錬成会・連盟杯・招待試合も同じ扱いで外す。**
    判定は `isTargetTournament`（**名前で外す。開催月では区別が付かない**）。
    ★実際に**徳島の新人大会5大会28試合**が「秋の大会」として入っていた。
  - ★★**組み合わせ（未実施の試合）は `upcoming` という別の入れ物に入れる**
    （2026-08-22。大分の秋が最初）。★**`games` に混ぜないこと** ——
    `games` は**スコアと勝敗を必ず持つ**形で、画面・トーナメント表・検算の
    すべてがそれを前提にしている。**足すだけなら既存に1バイトも影響しない。**
    ★**出すのは対戦相手が決まっている回戦だけ**（準決勝・決勝の枠を作って
    相手を空にしない）。★**結果が出た組は生成側で落とす** ——
    **その鍵に大会名を必ず入れること**（校名だけだと**別の大会の同じ顔合わせ**に当たる）。
  - ★★**大会名は年で変わることがある**（2026-08-22。山口）。
    `令和7年度山口県スポーツ大会高校野球競技(硬式)` が
    `令和8年度山口県新人高等学校野球大会` に改称され、**今年の大会を丸ごと
    拾えないまま去年の秋を「いまの秋」として出し続けていた。**
    ★**「秋が去年のままではないか」を毎季確かめること。**
    ★**開催中は「2枚そろわないと出さない」を緩める**（福島と同じ）——
    県決勝大会の紙は大会の後半にならないと出ないので、
    そのままだと**そのあいだ空白になる。** 落とすのは枚をまたぐ検算だけにして、
    **飛ばしたことはログに出す。**
  - ★★**トーナメント表は生成物から組み直す**（`src/lib/regional-bracket.ts`。2026-08-22）。
    **生成物は枝を持っていない**（スロット番号も勝者の行き先も無い）が、
    **全試合が揃っていれば「この回戦の学校は前の回戦のどの試合に勝って来たか」で枝が決まる。**
    ★**私立の戦績を残す方針変更があって初めて成り立つ**（上の項）。
    ★★**組めない大会は組まない**（実測95大会中57大会が成立）。落ちるのは
    **①いちばん深い回戦が2試合以上**（ブロック予選＝木ではない）
    **②決勝から辿り着けない試合がある**（出典に載っていない試合がある。いちばん効く検算）
    **③同じ試合が2つの試合へ繋がる**（`連合` のように**校名が一意でない**）。
    ★**1つでも当たれば null を返し、試合の一覧だけを出す。**
    「だいたい合っている表」を出さない（石川の「構造は合うのに相手が違う」と同じ轍）。
  - ★★**大会ごとのページがある**（`/prefectures/<県>/<年-季節>`。2026-08-24）。
    県のページは**いちばん新しい大会**だけを出し（24件まで）、
    **過去の大会は1大会ずつ別ページ**で全試合とトーナメント表を出す。
    ★**slugはローマ字**（`2026-summer`）。日本語の大会名をURLにしない。
    ★**1つの季節に複数の大会が入る県がある**ので、そのときは `-2` `-3` と連番。
    ★★**連番は「大会名の並び順」で決める** —— 試合数や日付で決めると、
    **出典が更新されて試合が増えただけでURLが入れ替わる。**
    ★**大会が多い県がある**（長野は63大会）。県のページでは8件だけ出し、
    残りは `<details>` で畳む（`TournamentLinks`）。
    ★**「ほかN件」と数だけ書いて省かないこと** —— 過去の大会に辿り着けなくなる。
    ★**大会のページは試合を絞らない。** その大会を見に来た人に「一部です」と返さない。
    ★**年は `yearOfTournament`。** サイト側（`regional-tournaments.ts`）と
    スクリプト側に**同じ規則が2つある**ので、変えるときは両方直すこと。
  - **県のページ（`/prefectures/<slug>`）はその県のファイルだけを動的 import で読む**
    （2026-08-13 実装）。索引 `src/lib/data/regional/index.ts` も生成物なので手で書かない。
    **静的 import にしないこと**（どの県のページにも全国ぶんが入る）。
    出すのはいちばん新しい季節の24試合まで。**取れている総数を画面に必ず書く**
  - ★★**県のページの並びは「いちばん新しい大会 → 大会をたどる → 公立旋風 → 学校一覧」**
    （2026-08-25。運営者の指示）。**学校一覧を先頭に戻さないこと** ——
    検索から来た人が最初に見るのが素の学校一覧だと見に来た甲斐が無く、
    いちばん見たいトーナメント表が折りたたみの下に沈む。
    ★**学校一覧はページから消していない**（下に移しただけ）ので索引には従来どおり載る。
    ★★**地方大会を持たない8地区では学校一覧を先頭に戻す**（`hasRegional`）。
    北北海道・南北海道・青森・宮城・秋田・東東京・西東京・鳥取＝**規約で塞がれている6県**ぶん。
    **入れ忘れると、その8地区だけ「空の見出しから始まるページ」になる。**
  - ★**トーナメント表の見出しは大会名にする。** 「トーナメント表」という一般名だと
    **ページ最初の見出しが何の大会か分からない**（長い検索語に当たらない）。
  - ★★**大会の一覧は年でまとめる**（`TournamentLinks`。2026-08-25）。
    佐賀64・長野61・熊本46 という県があり、平らに並べるとどの年か分からない。
    ★**「年度」ではなく「年」**（データは暦年で、高校野球の年度とは**秋がずれる**）。
    ★**1つの年×季節に大会が複数ある県がある**ので**春/夏/秋の3つに決め打ちしない。**
    ★**大会が6以下の県はまとめない**（41県中23県は3大会以下）。
    ★★**タブ（JavaScript）にしないこと。** `<details>` なら**畳んだままでも
    `Ctrl+F` で当たりクローラからも見える。** タブにすると
    **過去の大会がクローラから見えなくなり、大会ページへの内部リンクが死ぬ。**
  - ★**「いちばん新しい季節」を配列の先頭で決めないこと**（2026-08-17。滋賀）。
    `latestSeasonGames()` は日付のある試合があればそれで決めるが、
    **滋賀は季節を2つ持ち、どちらも日付が無い**（紙に日にちしか書かれていない）。
    先頭を採ると**アダプタの `seasons` に書いた順**で画面が変わるので、
    **季節の順（春→夏→秋）で決める。** ★秋が出たらここを見直すこと
  - ★★**季節の中で「どの大会か」を試合数で決めないこと**（2026-08-25。大阪）。
    ~~日付の無い県は大会名が1つしか無い~~ は**成り立たなくなった** ——
    **大阪は日付を1つも持たないのに5年ぶん×3季＝15大会ある。**
    日付での並べ替えが全部同点になり、次の「試合数の多い順」で決まっていたため、
    **2023年の秋（公立114試合）が2025年の秋（103試合）より新しい**ことになり、
    **県のページの先頭に2年前の大会が出ていた**（三重も第107回が出ていた）。
    ★**大会名から年を出して比べる**（`yearOfTournament`）。
  - ★★**`yearOfTournament` は「括弧の中の西暦」を先に見る**（2026-08-25）。
    `令和5(2023)年度…` は **`令和(\d+)年` に当たらない**（`令和5` の次が `(`）。
    ★**規則は `src/lib/regional-tournaments.ts` と
    `scripts/build-regional-results.mjs` の2か所にある。変えるときは両方直すこと。**
  - **県のページの日付には年を付ける。** トップ（抜粋）は年を省いているが、
    県のページは前年の秋しか無い県があるので年が無いと見分けが付かない
  - ★★**県ごとに、組み立てた直後に書き出す**（2026-08-21。`writeDistrict`）。
    ★**最後にまとめて書かないこと。1県で落ちると実行まるごとが失われる。**
    2026-08-20 に実際に起きた —— 栃木の出典を `fetch`（undici）で取ると
    **Node の HTTP パーサが内部アサーションで落ち**（`assert(!this.paused)`）、
    ★**`try/catch` では拾えない**（イベントループから投げられる）ので
    **プロセスごと死に、それまでの35県ぶんが1つも書き出されなかった。**
    ★**出典側の回避策（栃木だけ `node:https` を使う）は原因の手当てにすぎず、
    どの県でも同じことが起きうる。** 取れたそばから書き出すのが本筋。
    ★抜粋（`regional-pickup.ts`）と索引（`loaders.ts`）は全県が要るので最後のまま。
    途中で死んでも**前の実行のものが残るだけ**で、県のページは壊れない
  - ★★**季節単位の歯止めがある**（2026-08-19 に入れた）。
    生成物を書き出す前に**いまのファイルを読み**、**季節ごとに「前は試合があったのに
    今回0」なら、その季節は前の内容を残す。** 入れる前は
    **日別ページ1枚の `fetch failed` で佐賀の夏32試合が丸ごと消えた**
    （県としては春34・秋34が取れていたので「取れている」と判断されていた）。
    ★**`kept` ではなく `seasonGames` が0のときに見ること**
    （`kept` は120日の窓で切るので、1試合でも取れていれば0にならない）。
    ★**「前より減った」で止めない**（出典が誤りを直して減ることはある）。**0のときだけ。**
    ★**それでも「中身が変わる」ほうは止まらない。** 再生成したら、
    自分が触った県以外の生成物が変わっていないか必ず見ること
  - **自動更新は甲子園と別のワークフロー**（`update-regional-results.yml`。
    2026-08-14）。**1日2回**（19時・22時）。甲子園と同じ3時間おきにしない —
    出典に個人運営のサイトが2つあり、1回で日別ページを数十枚取るため。
    ★**1試合も取れなかった県のファイルと、空の抜粋は書き換えない**
    （出典が変わったときに、その県の試合が消えたコミットを積まないため）
  - **抜粋の選定は生成時、シャッフルは表示時。** 生成時に混ぜるとCIが
    中身の無いコミットを積み続ける
  - **勝ち上がりは「4回戦突破」で出す。** ベストNは全試合が取れている前提が要り、
    取りこぼすと実際より小さく出る。トーナメント表は3県とも画像で読めなかった
  - ★**決勝まで終わった大会は勝ち上がり一覧に出さない**（終わった大会に勝ち上がりはいない）
  - **検算は準々決勝・準決勝・決勝の試合数が 4・2・1 か。** 校名では検算しない
    （出典が同じ学校を回戦ごとに書き分ける）。**全回戦に半分を要求しない**
    （シード校が2回戦・3回戦から登場する）
- ★★★**試合中の速報だけは生成物にしない。描くときに取りに行く**（2026-09-05。`src/lib/live/hsb.ts`）。
  **取得 → コミット → 再ビルド**の経路は**ビルドだけで約20分**なので、
  **どんなに詰めても30分遅れ**になり、**1試合動くたびに全ページを焼き直す。**
  ★**速報は `fetch` の `next.revalidate` を秒で回す別系統**にしてある
  （`/live/<県>` と `/live/<県>/<token>`）。**既存の地方大会データは1バイトも触っていない。**
  ★**出典は HSB flash。** 3階層とも1リクエストで済む ——
  **全国の状態は `hsbflash.jp/top` の9.6KBに47県ぶん入っている**（県ごとに叩かない）。
  ★★**負荷は「試合時間帯（8〜20時）だけ60秒」**（運営者と決めた）。
  **訪問者ごとには取りに行かない**（Next のキャッシュは県ごとに1つ。誰も見ていない県は0回）。
  ★★**`fetchLiveDistricts` の既定を60秒にしないこと** ——
  **Next はページの中でいちばん短い間隔を採る**ので、
  **トップの Supabase 問い合わせが毎分走る。**
  ★★**県のページ（`/prefectures/<県>`）に速報を埋め込まないこと**。同じ理由
  （あちらは `revalidate = 300` で問い合わせが多い）。**入口のリンクだけ置く。**
  ★★**規約で外している6県には広げない**（`EXCLUDED_HOSTS`）。
  **HSB flash は47県ぶんあるので、何もしなければ出てしまう。**
  ★**「取れなかった」ではなく「収録していない」と書き分ける**（出典の不調と読み違えられる）。
  ★★**この出典は状態でクラスを足す。完全一致で拾わないこと** ——
  `inning_score total`（合計）と `geme_state geme_state_end`（試合終了）で
  **静かに欠けた**（合計が取れず15回目の得点を合計と取り違え、終了の表示が消えた）。
  ★**`geme_state` は出典の綴りのまま**（`game` ではない）。
  ★**未開始の試合は得点を出さない**（`0 - 0` にしない）。**詳細のリンクも張らない。**
  ★**速報のページは `noindex`**（明日には別の中身になる）。
  ★**自動更新には止めるボタンを付ける**（WCAG 2.2.2）。**裏のタブでは取り直さない。**
- **結果速報の枠は時期で中身が入れ替わる**（`src/lib/results-slot.ts`）。
  **今日の日付で判定しない。「最後の試合が新しいほうを出す」。**
  日付で切ると大会の谷間に何も出ない期間ができ、雨天順延にも追随できない。
- **出典の表示は実際の出所と一致させること。**
  手元に同じ記録があっても、転記した経路が別ならその経路が本当の出所。
  読者に対する出典表示の信頼性は規約の問題より優先する。
  `sources` の `url` は任意（運営者の記録・書籍にはURLが無い）。
  ★★**1つの県の中で出所が分かれることがある**（2026-08-24。富山）。
  連盟の紙が準々決勝までしか埋まっておらず、**決勝は Wikipedia、準決勝は運営者の記録**。
  `RegionalGame.source` に**その試合だけの出所**を書く（県の `sourceName` を上書きする）。
  ★**「手元に同じ記録があるから」で県の出典にまとめないこと。**
- **都道府県セレクタの表示切り替えは画面幅ではなくコンテナ幅で判定する。**
  幅の狭いカラムに入れたときにマスが潰れて県名が読めなくなるため
  （`.prefecture-map` のコンテナクエリ）。
  **タイル地図の左上（1〜8列・1〜5行）は丸ごと空くので `heading` / `aside` で使う。**
  トップは見出し（1〜2行目）と凡例・数字・「学校名から探す」（3〜5行目、
  `PrefectureMapGuide`）を入れている。**見出しを地図の外に出さないこと** —
  地図の左上が下から始まって北北海道と上端が揃わず空きができる。
  ★**中身は割り当てた行ぶんの高さに収めること。** 行が `auto` なので、高いと
  同じ行の北海道・東北のマスまで伸びて高さが揃わなくなる。**足したら
  「入れる前と行の高さが変わらないこと」を実測して確かめる**（README参照）。
- 日本語Webフォントは読み込まない（スマホの表示速度優先）。システムフォントスタックを使う。
- ★★**抜けている年は `scripts/sweep-regional-gaps.mjs` で掃く**（2026-09-01 その5）。
  `node --env-file=.env.local scripts/sweep-regional-gaps.mjs --pref <県> [--from <年>] [--dry]`
  ★**やっているのは3つだけ** —— ①生成物から「収録範囲の中で1試合も無い年」を出す
  ②その年を1つずつ `--year` で取りに行く ③落ちた年について**出典が出した警告をそのまま**並べる。
  ★**読み手も検算も1つも増やしていない。穴は1枚ずつ紙を開いて直すしかない。**
  ★★**出典に優しくするため、全県まとめて走らせる口はわざと作っていない。1県ずつ。**
  ★**警告が1つも無い年は「出典に紙が無い」か「アダプタが `--year` を受け取らない」。**
  ★★**掃く前に「その県は遡っても壊れないか」を必ず確かめること**
  （`tournament: null` の県と、出典が1年ずれた名前を返す県は静かに上書きされる）。
- ★★**過去の大会を遡って集める**（2026-08-24。運営者の「遡れるだけ遡って」）。
  `--year <西暦>` で年を指定して走らせる。**何度でも走らせて積み上げられる。**
  - ★★**「今回取れなかった大会」は前の生成物から引き継ぐ**（`build-regional-results.mjs`）。
    これが無いと**過去年を取るたびに今年が消える**（逆も同じ）。
    ★**引き継ぐ単位は「大会」**なので、今回取れた大会は新しいほうで置き換わる。
    ★**年ではなく大会名で見分ける**（日付を1つも持たない出典が9県ある）。
  - ★★★**大会名から導ける年と、試合の日付の年が食い違ったら、その季節は捨てる。**
    **新潟で実際にデータを壊してから入れた歯止め** —— 新潟は大会名を試合とは別の
    場所から取るので、`--year 2024` で走らせると**2024年の試合に「令和8年度」の
    大会名が付き、正しい2026年のデータを上書きした。**
    ★**警告では足りない。上書きは静かに起き、画面には「令和8年度」として2年前の
    試合が並ぶ。** 検算はどれも通っていた。★**気づけたのは日付を目で見たから。**
  - ★★★**この歯止めは「1年だけ遡ったとき」には効かない**（2026-08-27。新潟を2度目に壊した）。
    **秋季が暦年をまたぐぶんの ±1年を許してある**ので、
    **`--year <去年>` はちょうどその幅に収まって素通しする** ——
    `第154回…（令和8年度春季）` という **2026年の名前**に **2025年の試合**が入り、
    **正しい2026年の春61試合・夏65試合を上書きした**（復元済み）。
    ★**2年以上前なら止まる。危ないのは1年だけ遡ったとき。**
    ★★~~新潟は `--year` で遡らないこと~~ → **2026-08-31 その5 に直した**（上の項）。**旧：** 出典が年度の頭に**ファイル名だけ差し替え、
    中身は前の季節のまま**という作りで、いまコミットされている秋
    （`第155回…（令和8年度秋季）`）も **2025-09 の試合**が入っている。
    **触るときは必ず日付を目で見ること。**
  - ★★★**遡る前に「その県は遡っても壊れないか」を確かめること**（2026-08-27。2県を壊して復元した）。
    **引き継ぎの鍵は大会名だけ**なので、次の2つで**静かに上書きされる**:
    - ★★**`tournament: null` の大会がある県**（実測：**神奈川262試合・奈良92試合**）。
      **名前が無いので年をまたいで同じ鍵**になり、遡るたびに前の年が消える。
      奈良を `--year 2020〜2023` で走らせたら、**2014年の92試合が2023年の102試合に置き換わった。**
    - ★★**出典が「1年ずれた名前」を返す県**（島根）。`--year 2020` で取った試合が
      **`2021年秋季島根県大会` という名前で入り、本物の2021年秋37試合を上書きした。**
      ★**±1年の歯止めはここをすり抜ける**（名前2021・日付2020 で差は1）。
    ★**どちらも警告は出ない。** 気づけたのは**遡ったあとに `収録状況.md` の年を見たから。**
    ★**直すなら「引き継ぎの鍵に年を足す」と「取りに行った年と試合の年を突き合わせる」の2つ。**
    **それまでは、この2つに当たる県を `--year` で遡らないこと。**
  - ★**`--year` を受け取らないアダプタがある**（千葉・静岡・山口・宮崎ほか）。
    そういう県は年を渡しても最新の紙を取り直すだけ。**足すには県ごとの実装が要る。**
  - ★★★**生成物の形を変えたら `previousDistrict` を必ず一緒に直すこと。**
    JSON に移したときにここを `.ts` のままにしていて、
    **「前の生成物が無い」と判断され引き継ぎが丸ごと効かなくなった。**
    その状態で遡らせた**神奈川・島根・栃木が過去年だけになり、今年が消えた**（復元済み）。
    ★**警告も例外も出ない。静かに壊れる。**
  - ★★**過去年のURLを組み立てる県では、今年の固定名を候補に残さないこと**（栃木）。
    回数形が404のときに**今年の紙を過去年として読む**。
  - ★**大会名から年が導ける形は3つ**: `第N回…選手権`（+1918）／`令和N年`（+2018）／
    **`YYYY年`**（島根。**これを見ていなくて5件の年混入を素通ししていた**）。
  - ★**出典に優しくすること。** 1年ぶんにつき紙を取りに行く。
    **一度取れば生成物に残る**ので、同じ年を何度も取り直さない。
  - ★★**遡ったあとは「公立が絡む件数」も見ること**（2026-09-02。岡山で1度だけ観測）。
    `--year 2023` で走らせた回（**その年の紙は1枚も無く「前の内容を残す」だけだった**）に、
    **生成物の20試合から `name` と `slug` が落ちていた**
    （`倉敷天城高校/kurashikiamaki` → `天城/null`）。**今年で走らせ直したら元どおり。**
    ★**原因は突き止めていない**（観測できたのは1回だけ）。
    ★★**試合は1つも増減しないまま、学校への結び付きだけが静かに減る**ので、
    **試合数だけ見ていると気づけない。**
  - ★★**遡ったあとは「年ごとの網羅」を数えること**（2026-08-26。山梨）。
    14年ぶんを一括で走らせたら**2024年だけ丸ごと入っていなかった。**
    その年の取得が一度こけただけで、**警告も出ず、あとの年の実行でも埋まらない**
    —— 引き継ぎは「前の生成物にある大会」しか戻さないので、
    **一度も入らなかった年は永遠に入らない。**
    ★**合計の試合数だけ見ていると気づけない。単独で走らせ直せば入る。**
  - ★★**年の打ち間違いと「前の年の消し残り」を取り違えないこと**（2026-08-26。山梨）。
    どちらも「紙の年と違う日」だが、**落としてよいのは消し残りだけ。**
    ★**見分けは「大会N日目」の重なり** —— 消し残りは**同じ日番号が二重**になり、
    打ち間違いは**1〜Nが1回ずつ**でその並びの穴を埋めている。
    **落とすほうに倒したら、2013〜2016年の準々決勝・準決勝が静かに消えた。**
    打ち間違いのときは**月日はそのままに年を読み替える**（島根と同じ）。
  - ★★**イニング表は「計」の列から読むこと**（2026-08-26。山梨）。
    **中断した試合は「計」が空**で刷られ、翌日に改めて行われる。
    「後ろから最初の数字」を合計にすると**最終回の得点を拾って嘘の試合ができる**
    （`農林 1-2 日川`。実際は翌日 日川 11-0 農林）。
    ★**両チームの最終回が 0 だと 0対0 の幻の引き分けになる**（島根と同じ形）。
    ★**見出し行のある表と無い表が同じ紙に混ざる**ので、
    **「校名があって計が読める行」の先頭2つ**を採ること
    （`rows.slice(1, 3)` の決め打ちで見出しの無い表の試合が丸ごと落ちていた）。
- ★★**ニュースとXは 2026-08-24 に画面から外した**（運営者の判断。運用予定が無い）。
  **まだ無いものへ誘導しない。** 消したのは入口だけで、**コードは全部残してある。**
  戻す場所は5か所 —— `NAV`（`constants.ts`）／ヒーローのボタンと説明文／
  `SITE.description`／sitemap の `/news`／ヘッダー・フッター・`/contact` の X。
  ★**`XFollowCard` と `XIcon` も残してある。**
  ★★**いま `/contact` に連絡手段が1つも無い**（`OPERATOR.contactEmail` が未設定）。
  **応援メッセージの削除依頼を受ける窓口**なので、**公開前に必ず設定すること。**
- ★★**公立旋風のトップ枠は「順位」ではない**（2026-08-24）。
  `highlight_rank` の上位3件ではなく、**公開されている全件からランダムに5件**
  （`getRandomPhenomena`）。★**番号を振らないこと** —— 毎回入れ替わるものに
  順位を付けると「1位＝いちばんすごい旋風」に見える。
  ★**件数はヒーローの高さから実測して決めてある。** 余白や列を変えたら測り直す。
  ★**混ぜるのはサーバー側。** クライアントで混ぜると初期表示と食い違ってちらつく。

## ディレクトリ

- `src/app/` — ルーティング（App Router）
- `src/components/` — layout / news / schools / phenomenon / features / rankings / community / common / ads
- `src/lib/queries/` — DBアクセス層
- `src/lib/data/` — **スクリプトが生成するデータ。直接編集しない**
  （`twenty-first-century.ts` / `koshien-tournaments.ts` / `live-results.ts` /
  `regional-pickup.ts` / **`regional/<県>.json`**）
  - ★★**地方大会は JSON**（2026-08-24 に `.ts` から移した）。
    過去年を遡って集めたら**熊本2,350件・埼玉2,513件**になり、
    **どちらも TS2590 で型検査が落ちた**（甲子園2,972件・神宮で先に踏んだのと同じ）。
    ★**県ごとの上限ではなく1ファイルの要素数の問題**なので、**遡るほど必ずどの県も当たる。**
    ★**型は `regional/loaders.ts` で1回だけ与える。**
    ★**JSONにはコメントが書けない**ので「生成物・直接編集しない」の注記は `loaders.ts` にある。
- ★**`収録状況.md`（リポジトリ直下）— 県ごとに何年ぶん入っているかの一覧。生成物。**
  `build-regional-results.mjs` が走るたびに書き直す。**直接編集しない。**
  ★**遡ったあとは必ずここの「抜けている年」を見ること**（一度こけた年は警告も出ずに欠ける）。
- `data/federation-sites.json` — 47都道府県高野連サイトの規約調査の生データ
  （`scripts/survey-federations.mjs` が作る）
- `public/schools/` — **学校の校舎写真（生成物・3,059枚）。直接編集しない。**
  `scripts/build-school-images.mjs` が作る。**gitにコミットする**
- `src/lib/content/` — **手で書く編集コンテンツ**（`tournament-runs.ts` /
  `regional-supplements.ts`）。生成物と紛らわしいのでディレクトリを分けている
  - ★★**`regional-supplements.ts` は「出典が県の出典と違う試合」だけ**（2026-08-24）。
    出典が同じなら**アダプタで取れるはず**で、手で書く理由が無い。
    ★**取れるものを手で書かないこと** —— 出典が更新されても追随せず、
    **古い値が残り続ける。**
    ★**同じ試合が生成物に現れたら生成物が勝つ**（本来の出所が載せたということ）。
    いま入っているのは**富山の準決勝2・決勝1**だけ。
- `src/lib/constants.ts` — サイト定数・47都道府県マスタ・各種区分
- `supabase/migrations/` — スキーマ（SQLをGit管理）
