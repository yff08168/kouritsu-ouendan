<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# 公立応援団

公立高校野球応援サイト。全国の公立・国立高校の野球を「見る・知る・応援する」ためのWebメディア。

**現在の進捗・未完了事項・次にやることは `README.md` にまとめてある。作業を始める前に読むこと。**
要約：MVP全12フェーズ完了。未公開。運営者情報とドメイン取得が未了。

## 開発環境の注意点

**Node.js はポータブル版をシステムにインストールせず使っている。** PATH が通っていないため、
npm / npx を実行する前に必ず PATH を通すこと。

```powershell
$env:Path = "C:\Users\81809\tools\node-v24.19.0-win-x64;" + $env:Path
```

手動で起動する場合はプロジェクト直下の `開発サーバーを起動.bat` を使う。

## コマンド

| コマンド | 内容 |
|---|---|
| `npm run dev` | 開発サーバー |
| `npm run lint` | ESLint |
| `npm run typecheck` | tsc --noEmit |
| `npm run check` | lint → typecheck → build を通しで実行 |

コミット前は `npm run check` を通すこと。

## 設計上の決めごと

- **収録対象は「私立以外」**。県立・市立・町村立・組合立に加え、**国立と高専も含む**。
  `is_public` のような真偽値は持たず、`establishment` と `school_kind` の2軸で表現する。
- **東京都立国立高校（くにたち）は「都立」であって「国立（こくりつ）」ではない。**
  設置区分を機械判定すると必ず誤分類するので、この種の学校は手動確認する。
- **URLのslugはローマ字**。日本語URLはエンコードで読めなくなるため使わない。
- **画像は `image_url` / `image_credit` / `image_source_url` の3点セット**で持つ。
  Wikimedia Commons の CC BY-SA 画像は帰属表示が法的義務のため。
- **画像を外部URLから直接参照しない。** 自分のドメインから配信する。
  `next.config.ts` の `remotePatterns` は Supabase Storage のホストだけを許可している。
  **学校の校舎写真3,059枚は Supabase Storage ではなく `public/schools/` に置いてある**
  （2026-08-13）。この規則の目的は「外部URLを直接参照しない」ことで、
  `public/` でも満たせるうえ、バケットの作成も service_role キーも要らない
  （このリポジトリは service_role キーを持たない運用）。ロゴとヒーローも `public/` にある。
  **`public/schools/` は必ずコミットすること。** gitに入れないとVercelのビルドに
  画像が含まれず全部リンク切れになる。生成は `scripts/build-school-images.mjs`。
- **ニュースの全文転載はしない。** 見出し＋自作の要約＋出典名＋元記事リンクまで。
- **選手個人のページ・個人成績は作らない**（未成年の個人情報配慮）。
  **写真に選手が写ることはこれとは別扱い**（2026-08-12 方針変更）。
  運営者自身が観客席から撮った試合写真はヒーローで使っている。
  報道写真や第三者が撮った試合写真は著作権の面から引き続き不可。
- **データ取得は `src/lib/queries/` を経由する。** ページから直接 supabase-js を呼ばない。
- **`src/types/database.ts` は手書き。** Supabase CLI の型自動生成は使っていない
  （ローカルにツールを増やさないため）。`supabase/migrations/` を変更したら
  **必ずこのファイルも合わせること。** ずれても TypeScript は気づけない。
  `node --env-file=.env.local scripts/check-supabase.mjs` で実データを見て確認する。
- **クエリで `status = 'published'` を書かない。** RLS が公開済みの行しか返さない。
  アプリ側で二重に条件を書くと、RLSが壊れたときに気づけなくなる。
- **広告は `AdSlot` コンポーネント経由のみ。** ページに広告ネットワーク固有のコードを書かない。
- **未公開コンテンツの除外は RLS で強制する。** アプリ側のクエリ条件だけに頼らない。
- ~~**利用者の投稿は都道府県単位まで。学校ページに自由記述欄を置かない。**~~
  → **2026-08-20 に運営者の判断で撤回。応援メッセージは学校単位になった**
  （`supabase/migrations/0008_school_cheer_messages.sql`）。**元に戻さないこと。**
  ★**「選手個人のページ・個人成績は作らない」という上の方針は変わっていない。**
  置き場所が学校ページに移ったぶん個人名が書かれやすいので、歯止めは全部残してある
  ── 投稿は必ず draft で入り承認したものだけ公開、投稿欄に個人名不可の注意書き、
  利用規約 5-2 の「選手・生徒個人を名指しした内容は掲載しない」。
  **承認する人がこの基準で見ることが最後の砦。**
  ★**投稿欄は学校ページにしか無い。** 都道府県ページはその県の学校あての投稿を
  宛先つきで並べる集約表示で、投稿欄は置かない（2か所に置くと利用者が迷い、
  承認作業も2系統になるため）。
  ★**`prefecture_id` はクライアントから受け取らない。** DBのトリガが学校から引く。
  受け取ると、ある県の学校あての投稿を別の県のページに混ぜ込める。
- **anon の書き込みはRLSとDBトリガだけが守っている。** anonキーは公開鍵なので、
  アプリを経由しない書き込みが常に可能。API ルートを挟んでも裏口は塞げないため、
  検査をフロント側やクエリ層に書いても防御にならない。必ずDB側に置くこと。
- **集計トリガは `security definer` にする。** そうしないと呼び出した anon の権限で
  動き、RLSポリシーが無いテーブルへの update が**エラーにならず0行更新**になる。
  数が増えない不具合になり、画面上は成功に見えるので気づきにくい
  （`supabase/migrations/0006_community_fixes.sql` の経緯）。
- **オレンジ（accent）は小面積のアクセントのみ。** 面で使わない。
- **ロゴ画像は `npm run logo` で生成する。** `public/logo*.png` を直接編集しない。
  原本は `assets/logo-source.png`。詳細は README の「ロゴ」。
- **ヒーローの写真も同じで `npm run hero`。** 原本は `assets/hero/`、
  出力は `public/hero/`、表示順とクレジットは `src/lib/hero.ts`。
  **写真の上には無彩色の黒の膜を重ねる**（`globals.css` の `.hero-veil`）。
  一様に濃くすると写真が見えないので、**見出しのある左側だけ濃くする**。
  **ネイビーの膜に戻さない**（2026-08-13 に変更。写真が青くカブって見づらかった）。
  濃さは「いちばん明るい青空の上でもオレンジの見出しが3:1」から決めてある
  （白文字の4.5:1より厳しいのはオレンジのほう）。
  **区切りは % ではなく px**（460px / 660px）。`<br>` で折り返しを決めているので
  いちばん長い行の幅は一定で、% だと狭い画面のときだけ文字がはみ出す
  **`prefers-reduced-motion` では切り替えない。**
  **出典が確認できない写真を足さない**（広告を載せる＝営利なので個人利用限定の素材は不可）。
  出典は `HeroSlide.source` に必ず書く（画面には出さない記録用）。画面に出す
  `credit` は表示義務があるときだけ。
- **Wikimedia Commons の写真は使ってよい**（2026-08-13 決定）。
  ~~2026-08-12 に「使わない」と決めていたが撤回した。~~ ヒーローでも学校の写真でも使える。
  **CC BY / CC BY-SA は帰属表示が法的義務**なので、撮影者・ライセンス名・
  出典へのリンク・改変の告知（縮小や切り抜きをしたなら）を必ず画面に出すこと。
  ヒーローで使うなら `HeroSlide.credit` に入れる（`source` だけでは足りない）。
- **学校の校舎写真は ja.wikipedia から取り込んでいる**（2026-08-13。3,059校）。
  **この画像を「方針に反する」と判断して消さないこと。**
  取り込みの規則は `scripts/build-school-images.mjs` と README にある。要点だけ:
  - **ライセンスが判別できないものは使わない。** ja.wikipedia にある以上フリーの
    可能性は高いが、可能性で他人の著作物は使わない（3件除外）
  - **GFDL単独も使わない。** 複製にライセンス全文を添えることを求めるため（2件除外）
  - **校舎写真でない代表画像がある。** 県の位置図・高専のロゴなど。名前と拡張子で
    落とし、さらに**複数校が同じ画像を使っていたら除外**する（48件除外）
- **学校ごとの記章（`SchoolEmblem`）の色は、実際のチームカラーではない。**
  slugから決まるサイトの配色。機械的に当てられないし、当て推量は誤情報になる。
  色数を増やすほど「本物の色だ」と誤解されるので5色に絞ってある。
- **一覧で使う短い校名は `src/lib/school-name.ts`。** 末尾の「高校」だけを落とす。
  **見出し（学校ページのh1）と検索結果では使わない**（一意に伝える必要があるため）。
  「中等教育学校」「高専」「分校」は落とさない（何の学校か分からなくなる）。
  同じ県で衝突する3校（県岐阜商・市岐阜商・市和歌山）だけslugで上書きしている。
  ★**これは「画面に出す校名」の規則。照合用の別名とは別**（2026-08-13 追記）。
  出典は「城ノ内」「阿南高専」「科学技術」のように書くので、
  `build-regional-results.mjs` は照合のためにそれらの形も候補に持つ。
- **敗戦数を画面に出さない。** DBには `losses` が入っているが、表示するのは勝利数だけ。
  ランキングでも同じ（`wins`勝 とだけ書く）。
- **`prefectures` は都道府県ではなく「甲子園の大会区分」49件。**
  北海道は北北海道・南北海道、東京は東東京・西東京に分かれる。
  分割していない45件の id はJISコードのまま、分割した4件は48〜51。
  学校の住所としての「北海道」「東京都」は `schools.city` 側にある。
- **学校名・所在地・設置区分を手で書かない。** 文部科学省の学校コード一覧から
  `scripts/build-school-seed.mjs` で生成する。生成AIに校名を書かせると
  実在しない学校が混ざる。補ってよいのは読み・表示名・地区だけ（README参照）。
- **甲子園出場歴・戦績も生成AIに書かせない。** 校名と同じ扱いで、出典のある事実だけを入れる。
  統廃合・校名変更で現存しない学校の出場歴は現存校に引き継ぎ、`note` に旧校名を残す。
  **出典サイトは規約を読んでから選ぶ。** 見るのは①自動取得の禁止 ②**営利目的での利用の禁止**
  の2点。②があると目視での転記も塞がる（このサイトは広告を載せるので営利）。
  一球速報.com・バーチャル高校野球はどちらも確認済みで除外（README参照）。
- **硬式野球部の有無は各県高野連の加盟校名簿でしか判断しない。**
  校名や設置区分から推測しない。名簿は `data/baseball-clubs.json`。
  部が無い学校は削除せず `status = 'draft'` にする。
- **ランキング（`/rankings`）の集計はアプリ側でやる。DBのビューを足さない。**
  このプロジェクトは Supabase CLI を使わず人がSQL Editorで適用する運用なので、
  ビューを増やすと「適用し忘れてページが落ちる」が起きる。対象は出場歴3千件・
  学校700件で、1回あたり4リクエスト・1.5秒。ページの `revalidate` は1日。
  **PostgREST は1回に1,000行しか返さない。** 出場歴はページングで取る必要があり、
  そのとき**並び順を一意に決めてから**取ること（`school_id, year, season`）。
  並びが不定だとページの境目で行が重複したり抜けたりする。
- **ランキングには「私立を収録していない」と必ず書く。** 学校マスタが公立だけなので
  ここでの1位は全国1位ではない。`DataNote` コンポーネントを各ページに置く。
- **成績が不明な出場を「初戦敗退」に混ぜない。** 最高成績の一覧でも、
  段階が確定しない学校はどの段階にも入れず別に数える。
- **21世紀枠は `src/lib/data/twenty-first-century.ts`（生成物）。**
  年に1〜3件しか増えないのでDBに入れていない。
  `scripts/build-21st-century.mjs` が Wikipedia「選抜高等学校野球大会」の
  「21世紀枠出場校一覧」から作る。**選考理由の文章は取り込まない**
  （事実の抽出ではCC BY-SAの継承は発動しないが、文章を持ってくると発動する）。
  成績もこの表からは取らない。DBの `school_championships` と表記が揃わなくなるため。
- **甲子園のスコアを `WebFetch` の要約で取らない。★**
  Wikipediaの大会記事を要約させると**対戦相手も勝敗も入れ替わって出てくる**
  （実際に大社2024が1勝3敗、金足農の3回戦の相手が横浜でなく東海大熊本星翔、
  佐賀北が1回戦敗退、という出力になった）。
  `data/wikipedia-cache/summer-NNN.json` の `wikitext` を直接読むこと。
  勝ち上がり表は**太字が勝者**、`x` 付きがサヨナラ。
  書いたあとは `school_championships` の勝敗数と必ず検算する。
- **勝ち上がりは `src/lib/content/tournament-runs.ts`（手書き）。**
  試合単位のテーブルを足すと人がSQLを流す作業が増え、忘れると詳細ページが落ちる。
  `phenomena.slug` と `phenomenonSlug` が対応しているので片方だけ変えないこと。
- ★**公立旋風は甲子園だけではない**（2026-08-20 に地方大会の4件を足した）。
  `level = 'prefectural'` は画面では**「地方大会」**と出す（「県大会」ではない。
  東東京・西東京の大会は県大会ではないし、サイトの他の場所も「地方大会」と呼んでいる）。
  SQLは `supabase/senpu_seed_chihou.sql`（**未適用**）。
  ★**21世紀枠でベスト4の2校（宜野座2001・利府2009）は甲子園の記録**なので、
  地方大会とは別ファイル `supabase/senpu_seed_21seiki.sql`（**未適用**）。
  ★**「21世紀枠でベスト4は2校だけ」は70件を1件ずつ数えた結果**
  （2020年の3校は大会中止・2013年の土佐は私立でマスタに無い、まで確かめてある）。
  **21世紀枠が増えたら数え直すこと。**
  - ★**地方大会の勝ち上がりは、その県で使っている出典からしか作らない。**
    県相模原2019（神奈川）は神奈川高校野球ステーション（地方大会の結果と同じ出典）。
    **出典表示はサイト名で出す**（連盟の名前で出さない）。
  - ★★**小山台2018/2019の勝ち上がりは個人ブログが出典**（2026-08-21。運営者が指定）。
    「わっぱ飯のイン斬り野球」の「温故知新」。小山台OBが観戦した試合の記録で、
    **出典表示はブログ名で出す**。★**取るのは回戦・対戦相手・スコアだけ**
    （観戦記の文章が本体のブログなので**文章は1文字も取らない**）。
    ★**検算は決勝だけ**（Wikipedia の歴代代表校の表と一致）。3回戦〜準決勝は出典が1つしか無い。
  - ★**日野2013に勝ち上がりを付けていないのは資料が無いから。**
    東京都高野連は「データ」の転載を制限しており、上のブログは小山台専門。
    Wikipedia の歴代代表校の表には**決勝のスコアしか無い**。
    **1試合だけ並べるとその1試合しか戦っていないように見える**ので節ごと出さない。
  - ★**見出しは `TournamentRun.heading` で分ける。** 既定の「甲子園での勝ち上がり」の
    ままだと、**地方大会の準々決勝が甲子園の準々決勝に見える**。
- **動画は権利者の公式チャンネルのものだけ埋め込む。**
  判断は `isEmbeddableVideo()` の1か所に閉じ込める。テレビ放送を個人が
  上げたものは、埋め込めても侵害動画と分かって案内すると幇助になりうる。
- **新聞社のスコアページからスコアを取らない。★**
  一球速報.com とバーチャル高校野球は**利用規約**で外してある（著作権ではない）。
  バーチャル高校野球は第8項(3)が営利目的の利用を禁じており、**目視での転記も塞がる**。
  「人が読んで渡せば規約に触れない」は**この2媒体には通用しない**。
  使えるのは Wikipedia・**運営者自身のスコアブック**・市販の記録集・**日本高野連公式**。
  ~~高野連は当年のみ~~ は誤り（2026-08-12 訂正）。夏の出場校一覧は第1回から、
  日別の試合結果は2008年からある。規約もサイトポリシーも無く robots.txt は404。
- **結果速報の出典は日本高野連**（2026-08-13 にWikipediaから切り替え）。
  `scripts/build-live-results.mjs`。**次戦の日付・第何試合・開始時刻が出せる**のが理由。
  - **日付を推測しない。** `/sensyuken/<年>/schedule/` の一覧に載っているリンクだけを辿る
  - **次戦の出典は2つ**（2026-08-13 追加）。日別ページは**日程が発表された日ぶんしか無い**ので、
    決まっているのに出せない次戦がある（鳴門渦潮の8/14がそうだった）。
    `/sensyuken/<年>/tournament/`（組み合わせ表）から**3回戦までの対戦カードを補う**。
    **日別ページ由来を必ず優先する**（組み合わせ表には開始時刻が無い）。
    補ったぶんは `next.provisional: true`。詳細はREADMEの「次戦の出典は2つある」
  - **準々決勝以降は補えない。** 勝ちチーム主将のくじ引きなので本当に未確定
  - **同じ地区で2件以上に当たったら結び付けない**（岐阜商業のように県立と市立が同名）
  - ★**県大会で県外の学校に結び付けない**（2026-08-14 修正）。地方大会の照合には
    「県内で引けなければ全国で引く」受け皿があるが、これは**地区大会の県外の相手**用。
    県大会にも効いていたため、その県の私立が県外の公立に化けていた
    （愛知「愛知」→滋賀県立愛知、熊本「城北」→徳島県立城北など計28試合）。
    ★**この誤りは警告に出ない。** 検算も校名の照合も通る。出典を足したら
    「結び付いた学校の県が大会の県と違わないか」を1度は突き合わせること
  - **サヨナラは出典によって書き方が違う。** 高野連はイニングのセルに大文字 `X`（`1X`）、
    Wikipediaは合計スコアに小文字 `x`。`Number("1X")` は NaN になる。
    **イニングスコアから推測で導かない**
  - その年のページが無い時期は**例外を投げず、生成物を書き換えずに終わる**
    （投げるとCIが3時間おきに失敗し続ける）
  - **春（選抜）は未対応。** `/senbatsu/` で回数は `年 - 1928`（夏は `年 - 1918`）
- **地方大会は `scripts/build-regional-results.mjs`**（2026-08-13 実装。長野・神奈川・
  埼玉・山梨・徳島・熊本・群馬・佐賀・奈良・愛媛・新潟・**愛知**・**京都**・**広島**・**三重**・**鹿児島**・**石川**・**岐阜**・**千葉**・**山形**・**静岡**・**山口**・**宮崎**・**福井**・**和歌山**・**滋賀**・**兵庫**の27県
  ＋ **2026-08-20 に 茨城・岡山・香川・高知・長崎**（方針変更）**・島根・岩手**（連盟以外の出典）**・大分・栃木**
  ＋ **2026-08-21 に 沖縄**（3季163試合）**・福島**（2季89試合）。**計38県**
  ＋ 同日に**山口の秋季**（48試合）と**大分の春季**（7試合）。**栃木は既に3季あった**
  ＋ **2026-08-21 に 福岡**（春夏213試合。**連盟以外の出典＝HSB flash**）。**計39県**
  ＋ 同日に**宮崎の春季**（42試合）。★**「地区予選が4枚ある」という記録は誤りで、
  紙は夏・秋と同じ左右2段組の1枚だった**（4枚あるのは別の大会＝宮崎県高等学校野球選手権大会））。
  ★★**福岡の出典はSVGのトーナメント表で、枝が線として描いてある**
  （`scripts/lib/svg-bracket.mjs`）。**座標から推測して組み立てるのとは別物**で、
  石川で踏んだ「構造は合うのに相手が違う」が起きない。
  ★**`stroke="red"` が勝った側**なので、**得点との一致を検算にできる。**
  ★★**このサイトは47都道府県ぶんある。規約で外している6県もここからなら取れてしまう。**
  **連盟が断っているものを別経路で取るかは運営者の判断。福岡だけにしてある。**
  ★**`fk-kokoyakyu.com` は採らなかった** —— 規約に禁止は無いが、
  **フリーの編集者が運営する商業メディアで、記録の編纂が本業**。
  **禁止条項が無いことと、その人の商売を取ってよいことは別。**
  ★★**福島の「32試合しか読めない／32試合しか載っていない」はどちらも誤りだった**
  （2026-08-21）。**見出し行の数を試合数と取り違えていた** ——
  ★**スコア表は1行に試合が横に2つ並ぶことがある**（段まで数えれば61試合）。
  ★**2桁の数字は1桁より左端が出る**ので、校名の列とスコアの列の境目は
  **列の間隔の半分**で取ること（「見出しの少し左」だと2桁の得点だけ校名にくっつく）。
  ★★**1枚から複数の代表が出るブロック表は `winners` で組む**（2026-08-21。山口の秋季）。
  ★**ブロックの切れ目は探さない** —— 各段で隣どうしを組むので、
  **どのブロックも段数が同じなら切れ目を知らなくても組は正しくなる。**
  ★★**「段数が同じ」は、紙に刷ってある代表校名との突き合わせで担保すること**
  （山口はさらに「次の紙＝県決勝大会の出場校と一致するか」という**枚をまたぐ検算**が効く）。
  ★★**縦書きで2列に組まれた校名は「右の列から」読む**（`nameColumns: "desc"`）。
  ★★**上下2段の紙を「スロット行の中点」で割らないこと。** 校名はスロット行の**下**に伸びる。
  **紙の中でいちばん広い隙間（段と段のあいだの余白）で割る。**
  ★★**字の大きさは、行の隙間より確かな手掛かりになることがある**（2026-08-21。大分の春季）。
  校名の下の「支部」の行は**隙間では切れない**（校名が縦に引き伸ばして組まれている）が、
  **幅は 15.4 対 10.3 ではっきり違う**（`pdf-text.mjs` の `width`）。
  ★★**日付のある季節と無い季節が混ざると、県のページが古い季節を出す**（2026-08-21。大分）。
  `latestSeasonGames` は「日付のいちばん新しい試合の季節」を採るので、
  **日付の無い夏が、日付のある春に負ける。**
  ★**アダプタを足したら、県のページがどの季節を出すかも必ず見ること。**
  ★★**開催中も更新される紙には強い検算をそのままかけない。**
  「チーム数 − 試合数 = 1」を常に要求すると**大会期間中ずっと1試合も出せない。**
  **「決勝が読めたか」で強弱を分ける**（弱いほうは「前の回戦で負けた学校が
  次の回戦に出ていない」。これは途中でも成り立つ）。
  ★★**沖縄を「1〜N と並ぶ行が無い」として外していたのは誤りだった**（2026-08-21）。
  **1〜56 の行はきちんとあった**（滋賀と同じ「見つからない」と「無い」の取り違え）。
  ★**やぐら表の「試合番号の行」は1回戦の帯として通ってしまう**（沖縄・大分）。
  中点がスロットの境目に乗るため。**滋賀と同じ「組めてしまう」壊れ方。**
  落とし方は「**スロット行より上で、0 を除いた数字が 1 ずつ増える行**」。
  ★**大分を「営利目的の複製を禁止」として外していたのは誤りだった**（2026-08-20）。
  原文にその条項は無く、自動分類が「非営利」の語だけを拾っていた。
  ★**47連盟の「制限あり12件」は、他も原文を読み直す価値がある。**
  ★**岩手の出典（白球ペンギン.com）は免責事項に「無断転載の禁止」がある。**
  **記事の文章は1文字も取らず、末尾のスコア表の数値と校名だけ**を取っている。
  **この線を緩めないこと。公開前に一度問い合わせること**（サイト自身が連絡を求めている）。
  ★★**季節は県ごとに違う。**「アダプタがあるから他の季節も同じ紙」は**成り立たない**
  （2026-08-19 に静岡・宮崎・福井で確かめた）。
  静岡は**夏が左右2段組なのに春は京都型**、宮崎は**座標も年の出し方も検算材料も別**、
  福井は**春に日付が1つも無い**（秋にはある）。**季節ごとに測り直すこと。**
  ★★**同じ季節でも年で紙がまるごとずれる**（2026-08-21。宮崎の春季）。
  2025春と2026春は**左のスロット列が 123 と 105、右が 462 と 482**で、
  **範囲を決め打ちすると片方の年しか読めない。**
  ★**スロット番号の列を紙から探して、範囲はそこからの相対で取る。**
  ★★**決勝と3位決定戦の内側・外側は当てにならない**（宮崎の春季）。
  `finalAt: "innermost"` は「境目をはさむ組のうちいちばん内側」を決勝とするが、
  **2026春は3位決定戦のほうが内側**（決勝16 対 3決15。秋は決勝15 対 3決41で逆）、
  **2025春は同じ幅7で「先に見つかったほう」任せ**だった。
  ★**取り違えると画面に嘘の決勝スコアが出る**（実際に 1-6 が決勝として出た）。
  ★**幅ではなく紙の区画で決める** —— 中央の**3位決定戦のラベルの y**を
  `centerFloor` に渡して、**それより下を中央の走査から丸ごと外す。**
  ★**ラベルの行の「中央の断片」も落としてから組み立てる** ——
  2025春のラベルは `3`(x=282) と `位`(x=293) に割れており、
  **`3` が準決勝の帯（x=279）に3ポイント差で並んで「数字3個（必要2個）」になる。**
  ★★**3位決定戦の出場校は、準決勝の敗者と突き合わせられる**（宮崎の春季）。
  ラベルの下に**両校名が縦書きで刷ってある**ので、**枝とは別の場所から来る事実**として使える。
  ★**優勝校が刷られていない年でも「準決勝の相手が違う」を止められる**（石川の壊れ方）。
  ★**完全一致で比べること** —— 同じ紙の同じ書き方なので略し方は揃う。
  **部分一致で緩めると `都城` と `都城東` を取り違える**（2025春に両方いる）。
  ★★**日程表の「試合数」の合計が検算になる**（宮崎の春季）。
  球場ごと・日ごとの試合数が紙の下に刷ってあり、**合計＝組み立て＋3位決定戦**。
  ★**日ごとではなく合計を見るのは、雨天順延に強いから**（順延は日のあいだで
  動かすだけで合計を変えない）。★**球場名のある行だけ数える**
  （見出しの `日程` `月日` の行にも数字が並ぶ）。
  ★**この検算は、優勝校も刷ってある年（2025春）で両方が一致することを
  確かめてから、刷っていない年（2026春）に使った。**
  ★★**縦書きの注記の列は、日程表を落としてから作ること**（宮崎の春季）。
  `stripVerticalNotes` は「同じ x の文字をつなぐ」ので、切る前は
  `（2季ぶり14回目）6火22×○×` になり**形が合わず1件も落ちなかった**
  （沖縄で凡例が校名に吸われたのと同じ「下端が列に入る」罠）。
  ★★**紙のいちばん下の凡例が校名にくっつく**（2026-08-21。沖縄）。
  縦書きの校名は「同じ x の文字をつなぐ」ので、**連合・合同チームの注記が
  近くのスロットに丸ごと吸われる**（`＊宮古総工：宮古総実・宮古工業エナジック`）。
  ★**行ごと落とす**（`【` か `＊` で始まる断片しか無い行）。**文字で消さないこと**
  （千葉で「宣」を巻き込んだのと同じ轍）。
  ★**そのうえで凡例は捨てずに使う** —— `略称：校名・校名` を `expand` に渡して
  連合チームを展開する。展開しないと `isCombinedTeam` に当たらず**1校に結び付く。**
  ★★**年は「回数」から出さず、紙に刷ってある「会期」の元号から出す**（沖縄）。
  春季・秋季は選手権と別の系列で、**春の紙には九州地区大会の回数まで刷ってある。**
  会期は**枝とは別の場所から来る事実**なので、
  **「組み立てた試合がそこに収まるか」がそのまま検算になる。**
  ★**ただし「最終日の一致」は落とす条件にしないこと。** 会期は**予定として先に刷られる**ので、
  **雨天順延で決勝が1日ずれると、正しい大会を丸ごと捨てる。**
  ★**2026-08-16〜18 に 20県 → 27県。詳細はREADMEの
  「★★2026-08-18 に 兵庫」「★★2026-08-17 に 滋賀」
  「★★2026-08-16〜17 に 20県 → 25県」と、各アダプタ冒頭のコメント。** 要点だけ:
  - ★★**1つの大会が複数枚のPDFに分かれている県がある**（2026-08-18。兵庫は4枚）。
    **1枚では「チーム数−試合数=1」が成立しない。**
    ★**枚をまたぐ検算がいちばん強い** ——
    **次の紙の出場校が、前の紙の勝者と1対1で対応するか**を見る
    （兵庫は「5回戦の16校＝ブロックの優勝校16校」）。
    1ブロックでも読み違えれば必ずどれかが余る
  - ★★**紙ごとに略し方が違う**（兵庫。本体「市尼崎・西脇工」／5回戦「市立尼崎・西脇工業」）。
    枚をまたぐ検算で**完全一致を要求すると必ず落ちる。**
    **どちらかがもう一方の部分列なら同じ**とみなす
    （緩いのは1組の比べ方だけで、要求は「過不足なく1対1で対応する」）
  - ★★**校名と得点がスロット番号の同じ側にある表**は `assembleSlotBracket` の
    前提（校名と回戦が反対側）に合わない。**スロット番号の列を
    校名と得点のあいだへ動かしてから渡す**（スロット軸が y なら x を動かしても順番は変わらない）。
    ★**得点の列から20ポイント以上離すこと**（近いと1回戦の得点をスロット番号として拾う）
  - ★**1回戦が1試合しかないブロックがある**（兵庫の9チームのブロック）。
    既定は「2試合ぶん以上」なので**1回戦の帯が飛ばされる** → `minFirstRound`
  - ★**ブロックの大きさを決め打ちしない**（兵庫は9チームと10チームが混在）
  - ★★**「取れない」と分類した県は、根拠を1つずつ実データで確かめ直すこと。**
    2026-08-17 だけで**3件が誤りだった**（福井「一部しか公開していない」／
    和歌山「検算材料が無い」／**滋賀「スロット番号の行が無い」**）。
    滋賀は**行がちゃんとあった**のに、**全角で書かれていて、しかも
    「１５ １６ １７ １８」と1つの断片に潰れていた**ので当たらなかっただけ。
    ★**「無い」と書く前に、その1行を目で確かめること**（滋賀は5項目のうち4項目が誤り）
  - ★★**「数字が余って組めない」より「組めてしまう」ほうが危ない**（2026-08-17。滋賀）。
    滋賀は**縦書きのコールド「7回」**（数字の1行下に「回」）が落とせておらず、
    **その帯（8個）が1回戦（4試合）として読まれた。**
    コールドの数字は**試合の中心＝スロットの境目**に置かれるので、
    境目どうしの中点は `(n+0.5 + m+0.5)/2`＝**n+m が奇数ならまた境目に乗る**
    （1/2の確率で検査を通る。滋賀は4組すべて通った）。
    `stripVerticalInningMarks()` で落とす
  - ★**omyutech製SPAの県は「お知らせAPI」から連盟のPDFに辿れる**（静岡・山口・宮崎）。
    `other-api.omyutech.com/otherapi/rest/newsandtopic/list?userID=&language=&leagueId=<N>&type=N`
    と `.../detail?...&newsId=<M>`。`leagueId` は `main.*.chunk.js` にある県ごとの定数。
    ★**下の「ブラウザで開かないと分からない」は誤り**（2026-08-16 の記述）。
    ★**山形の「APIは追ってもサイト再ビルドで壊れる」も誤り** — 入口も `leagueId` も
    チャンクのハッシュが変わっても同じ。**山形もこの形に寄せられる**
  - ★★**茨城は取らないと判断した**（2026-08-17）。`/schedule` の中身は `webtoppage` API が
    返す `gameId` `team1Scores` … で、**一球速報の試合データベースそのもの**。
    **エンドポイントが `otherapi` 側であることは根拠にならない** —
    分けているのは**配信経路ではなく作った主体**
  - ★★**「結果を一部しか出していない」と判断した県は、画像とPDFを疑い直すこと。**
    福井は「優勝校の4試合だけ」としていたが**HTML本文しか見ていなかった**。
    同じページの画像 `…最終結果_page-0001.jpg`（**`_page-0001` は PDF変換の跡**）の
    元PDFに全26試合が入っていた。WordPress なら
    `/wp-json/wp/v2/media?mime_type=application/pdf` で添付を一覧できる
  - ★★**和歌山は準決勝の組み合わせを抽選で決める**（表に明記）。
    **枝をそのまま伸ばすと実在しない対戦ができる**（実際に作った）。
    甲子園の準々決勝以降と同じで**補わない**。
    ★★**ただし「補わない」と「出せない」は別だった**（2026-08-18）。
    **抽選の結果そのものが紙に縦書きで書いてある**ので、枝を伸ばすのではなく
    **そこを読めば出せる**（35試合すべて）。実際、枝の順（熊野vs智辯和歌山）と
    抽選後（熊野vs耐久）は**違っていた。**
    ★**検算は「その4校が準々決勝の勝者の並べ替えであること」。**
    ★**読めるようになったら `partial` を外すこと** ——
    付けたままだと**将来その読み取りが壊れて試合が消えても警告が出ない**
  - ★**`slot-bracket.mjs` に足した9つ。どれも既定を変えていない**
    （毎回**全県を再生成して既存県の生成物が1バイトも変わらない**ことを確認した）:
    `hitSpan`（スコアが連結線の両端に置かれる表。山口・宮崎・和歌山）／
    `maxGap`（離れた「回」がスコアを消すのを止める。宮崎）／
    `verify.teams`（出典が書いている出場チーム数と突き合わせる。宮崎・滋賀）／
    `×` の除去（サヨナラ `7×`。福井）／
    **`explodeNumberRuns()`**（潰れた数字の断片をほどく。滋賀）／
    **`pairedScores`**（`11-1` を左右2つの数として読む。滋賀）／
    **`stripVerticalInningMarks()`**（縦書きのコールド。滋賀）／
    **`labelReach`**（日付・球場がスコアの帯より**上**にある表。滋賀）／
    ★**`roundBandGap`**（回戦の帯をまとめる幅の**上限**。静岡の春季。
    **日付の帯がスコアの帯の11〜12ポイント下にある表**では、既定の
    「回戦の間隔の0.45倍」が広すぎて日付を巻き込み、数字がちょうど1.5倍になって必ず落ちる）
  - ★**断片の中の位置は「代表的な間隔 × 文字数」で見積もらないこと**（2026-08-17）。
    紙によって字送りが違い、実測で最大5ポイント（＝0.4スロット）ずれた。
    **`pdf-text.mjs` が返す `width`（断片の幅）を文字数で割る。**
    既存の読み手は `x` と `text` しか見ていないので、足すだけで済んだ
  - ★**詰まったら `BRACKET_DEBUG=1`。** 1回戦の帯の選ばれ方、各回戦の
    「中点／帯の数字と位置／一致数」、**組み立て前に落ちた理由**、
    **組み立てた試合の一覧**が出る（山口・和歌山・滋賀はこれで解けた）。
    ~~★**生成物には公立が絡む試合しか残らない**ので、
    **私立どうしの試合が合っているかはこの一覧でしか確かめられない**~~
    → ★**2026-08-21 に方針が変わり、生成物にも私立どうしの試合が入る**ので、
    **生成物を直接見て確かめられる**（下の「私立の戦績も引用する」）
  - ★★**「同じ県の別の季節」でも測り直すこと**（2026-08-17。山口の春季）。
    紙の形は夏とまったく同じなのに**4か所で落ちた**:
    ①**年の出し方**（春は「令和8年度」で回数が無く、`第N回 + 1918` が例外になる → `yearOf`）
    ②**シード記号と校名が1つの断片になっているスロットがある**
    （`① 下 関 国 際`。記号の列を外す `ranges` に巻き込まれて校名が消える →
    `splitLeadingMark()` で**断片を割ってから列で外す**。★記号を文字で消さない）
    ③**中央の縦書き「（３年ぶり８回目の優勝）」の `回` が103ポイント左のスコアを消す**
    （宮崎と同じ → `inningMarkGap`）
    ④**シード記号の x が季節で違う**（春 545.6／夏 556。`ranges` を季節ごとに持つ）
  - ★★**継続試合は日付を2つ持つ**（2026-08-17。山口の春季）。
    雨で中断した試合は翌日に再開されるので、紙に開始日と再開日の両方が載る。
    - **日付の個数の検算は「紙が『継続試合』と書いている個数だけ」超過を認める**
      （`continuationMark`）。**「1件くらいずれてもよい」にしないこと**
    - ★**再開日が枝ではなく「注記の列」に書かれている紙がある。**
      枝には開始日しか無く、`pickDate` では拾えない。
      **注記と枝の対応づけはしない**（座標の近さしか手掛かりが無く、
      間違えると別の試合に別の日が付く。**それは「日付が無い」より悪い**）。
      **中断した日の試合をまとめて日付なしにする**（`dropContinuationDates`）
  ★**出典のURLを追えない県では、ファイルIDを直に持ってよい**（2026-08-16。山形）。
  山形はSPAで、Driveのリンクが omyutech の告知APIからしか辿れない
  （★上記のとおりAPI自体は安定しているので、寄せ替えてよい）。
  ★**連盟が同じファイルを上書き更新している**ので、IDを固定しても大会中は自動で追随する
  （お知らせは6/25付なのにPDFには7/26の決勝まで入っていた）。
  **大会名はPDFから読むこと**（IDと一緒に書くと差し替え時に古い名前が出る）。
  ★**出典は連盟だけではない。** 埼玉・神奈川は個人運営の情報サイト、
  **愛知は県内のケーブルテレビ局14社の応援サイト**（`catvase.jp`）から取っている。
  連盟が出していない県はこの筋を探すこと。**出典表示は必ずそのサイトの名前で出す**
  （連盟の名前で出さない）。
  ★**出典を増やす前に README の「都道府県高野連サイトの規約調査」を読むこと。**
  47連盟を調査済みで、**12連盟は転載・複製を制限している**（北海道・青森・秋田・
  東京・鳥取は「データ」、埼玉・福島は「内容」「コンテンツ」を名指し、
  大分は営利目的の複製を禁止、岩手・宮城・島根・栃木は写真・記事の無断転載を禁止で
  うち3県は「リンクはトップページへ」）。**この12連盟を `ADAPTERS` に足さない。**
  ★**`data/federation-sites.json` の自動分類を鵜呑みにしない。** 福島・栃木は
  Copyright表記と同じ行にあったため分類から漏れていた（2026-08-13 に訂正）。
  足す前にその連盟のページを開いて「転載・無断・複製・営利」を自分で検索すること。
  ★★**2026-08-20 に方針を変えた。** 以下の「omyutech からは取らない」は**もう有効ではない。**
  新しい整理は「**連盟の公式サイト上で連盟名義で公開されている試合結果は、
  連盟の著作物として扱い、その数値（事実）を引用する**」（運営者判断）。
  これで**茨城・岡山・香川・高知・長崎の5県**を足した（`omyuAdapter`）。
  ★**前提を知らずに戻さないこと。** 塞いでいたのは**著作権ではなく利用規約の第20条**で、
  **著作権の面では元から問題になっていなかった**（スコアは事実）。
  判断材料と踏んだところは README の「★★2026-08-20 に方針を変えた」に全部ある。
  ★**文章は取らない。数値・校名・日付・球場・回戦だけ**にすること。
  ★~~一球速報（omyutech）については「取ってはいけないのは omyutech から取ること」だけ。~~
  8連盟（山形・茨城・静岡・山口・香川・高知・長崎・宮崎）はサイトの中身を
  `other-api.omyutech.com` から描いている（2026-08-14 判明）。
  **そこからは取らない**（連盟の規約ではなく業者側の規約で塞がれる。
  JavaScriptで描いていて取れない、ではなく取ってはいけない）。
  ★★**しかし「一球速報の県だから連盟からは取れない」は誤りだった**（2026-08-15）。
  **この分類にしていた岐阜・千葉・福井は3件とも、連盟が自分で結果を出していた**
  （岐阜＝日別のスコア表PDF11本、千葉＝やぐら表PDF、福井＝HTML。岐阜・千葉は実装済み）。
  ★**「サイトに omyutech がある」と「連盟から取れない」は無関係。**
  鹿児島・石川・千葉・岐阜は omyutech へのリンクを持ちながら連盟自身のPDFから取っている。
  ★**2026-08-16 に9県すべてを人がブラウザで開いて確かめた。**
  **殻は9県とも同じ omyutech の SPA だが、添付ファイルの置き場所は県ごとに違う**:
  **山形・静岡＝Google Drive**（omyutech と無関係。山形は実装済み）、
  **茨城・山口・宮崎＝`safe-api.omyutech.com` に連盟の文書**、
  岡山・香川・高知・長崎＝連盟の結果文書が見つからず。
  ~~★判定はブラウザで記事を開いて添付の行き先を見る。~~
  ★**2026-08-16 に自動化できた**（上記のお知らせAPI）。ブラウザは要らない。
  ★**運営者の判断で「連盟が作った文書なら omyutech のファイル置き場からでも取る」と決めた**
  （2026-08-16）。**スコアAPI（`baseballapi`）からは取らない**のは変わらない。
  詳細はREADMEの「★★旧『一球速報』9県を実際に開いて確かめた」。
  ★**2026-08-14 に47県すべての可否を確認した。HTMLでスコアを出している連盟は
  もう残っていない。** 残るは「PDFの14県」「Excelの新潟」「個人運営の情報サイト」。
  **連盟のサイトをもう一度見に行っても増えない**（READMEの「47都道府県の可否」）
  - ★**PDFの13県も全部開いた（2026-08-14）。スコア表だったのは愛媛だけ**で、
    残り12県は**トーナメント表**。組合せ表は点がどの対戦のものかが
    **線の位置でしか決まらない**（文字の並びに入っていない）。**推測で組み立てない。**
  - ★**トーナメント表は原則として出典にしない**（2026-08-14 に富山・石川で確認）。
    石川は格子状で組み立てられ、**検算（準々決勝4・準決勝2・決勝1）も通った**のに、
    **決勝の相手が違っていた**（事実は金沢3-4x遊学館、組み立ては金沢vs小松大谷）。
    許容幅を狭めれば決勝が組めず「決勝で負けた学校が決勝進出のまま」残る。
    **検算を通ってもなお間違える。**
  - ★**校名の欄と、その外の列を混ぜないこと**（2026-08-15。千葉）。
    千葉はシード記号（Ａ・Ｂ・Ｃ）が校名とは**別の列**に並ぶ
    （左 x=31 ／ 校名 37〜78、右 校名 513〜556 ／ 記号 x=561）。
    ★**記号を「文字で消す」作りにしないこと。** 右の x=560 に記号でない
    「宣」が1つ紛れており、`千葉東` が `千葉東宣` になっていた
    （**画面に実在しない校名が出るところだった**）。**列ごと範囲で外す**（`ranges`）。
    ★**全角ラテン文字を無条件に落とすのも駄目**（「光英ＶＥＲＩＴＡＳ」が壊れる）。
  - ★**同名の県立・市立がある県は対応表で受ける**（千葉は5組で最多。
    千葉・船橋・柏・松戸・銚子）。出典が「市立◯◯」と書き分けていて、
    **同じ表に両方が別のスロットとして載っている**なら、
    「市立が付かないほうが県立」は推測ではなく読み取り。
    **書き分けが無ければ結び付けないこと。**
  - ★**引き分けを「負け」と書かない**（2026-08-15。岐阜）。高校野球には
    **引き分け再試合**がある（市岐阜商 0-0 県岐阜商 が翌日 0-10 で再試合）。
    `won` は両方 false になるので、**「勝っていない＝負け」と読むと画面に事実と
    違うことが出る。** スコアが同じかで判定する（`RegionalDistrictCard` は △ と書く）。
    検算も**「引き分けを除いた決着した試合が チーム数−1」**にすること。
  - ★**同じ学校が回戦で書き分けられる出典がある**（2026-08-15。岐阜で5組:
    中津商/中津商業・多治見工/多治見工業・岐阜聖徳学園/岐阜聖徳など）。
    **畳まずに数えると「のべ出場校 − 試合数」が合わず検算が落ちる。**
  - ★**組み立てを考える前に、PDFの全ページを見ること**（2026-08-15。石川）。
    石川の `108nyagura-kekka-photo.pdf` は**1ページ目がやぐら表で、
    2〜5ページが試合ごとのイニングスコア表**だった。
    **スコア表があれば組み立ては要らない**（どの点がどの対戦かが紙に書いてある）。
    ★**石川を足したのは「トーナメント表を出典にする」決定の変更ではない。**
    **やぐら表は今も組み立てていない**（優勝校の検算にだけ使う）。
    スコア表型の検算は組み立て型より強い:
    ①各回の得点の和 == 印刷された合計（試合ごと）
    ②3回戦以降の出場校が全員「前の回戦の勝者」か・勝ったのに次にいない学校がいないか
    ——**②が、以前すり抜けた「構造は合うのに対戦相手が違う」を必ず捕まえる。**
    ★★**2026-08-21 にまた同じ轍を踏んでいたのが見つかった**（大分の支部予選）。
    「県北・久大以外の支部の紙が見つからない」としていたが、**同じPDFの2〜4ページ目**に
    別杵・大分・県南豊肥の3支部がそのまま入っていた。
    ★**アダプタが `parsed[0]` しか読んでいないなら、それは「1ページ目しか見ていない」**
    ということ。**「無い」と書く前に必ずページ数を数えること。**
  - ★**京都・広島・三重・鹿児島は例外として組み立てている**（鹿児島は 2026-08-15）。
    組み立ては `scripts/lib/slot-bracket.mjs`。**扱える表の条件がそこに書いてある。**
    要点は2つで、**どちらも石川は満たさない**:
    ①**1回戦のスコアがスロットの中心に置かれる**ので、シード（不戦）と対戦の
    区別が推測なしで付く ②**各回戦の数字の個数が試合数のちょうど2倍**なので、
    左から順に対応させられ位置の推測が要らない。
    ★**新しい県に流用する前に、この2つを実データで確かめること。**
    ★**検算は「枝とは別の場所に書いてある事実」と突き合わせる**
    （合計試合数・優勝校・日ごとの試合数・球場ごとの試合数）。
    **1つでも合わなければその大会を1試合も出さない。**
    ★**検算材料が無い表もある**（広島には優勝校も合計も無い）。その場合は
    構造の検算（スロットが欠けない・チーム数−試合数=1・日付の個数=試合数）で
    代用するが、**検知力は落ちる**ので外部との突き合わせを必ず1度行うこと
  - ★**鹿児島は「表の外」に検算材料があった**（2026-08-15）。連盟のトップページが
    「決勝戦 神村学園高等部 ９ ー ０ 鹿児島実業」と**文章で**書いている。
    **表の枝とは別の場所から来る事実**なので、石川で通ってしまった
    「構造は合うのに決勝の相手が違う」を止められる。**出典のページ本文も検算材料として探すこと。**
    校名は部分一致で比べる（表「神村学園」／文章「神村学園高等部」）が、
    **点数と勝敗は完全一致を要求する。**
  - ★**上下2段組・決勝が準決勝と同じ帯・日付に月が無い表も扱える**（鹿児島で実装）。
    `finalInCenter` / `parseLabel` / `expand` / `finalAt: "center"`。
    ★**日付の月を決め打ちしない**（鹿児島は表の開催期間の行から決めている）。
    ★**日付の帯は「スコアの帯に近い順」ではなく「前の回戦の帯より上のいちばん低い帯」で選ぶ。**
    準決勝と決勝はどちらも1試合なので個数では区別が付かず、近さで選ぶと入れ替わる
    （鹿児島は日付の位置が**上半分と下半分でも違った**ので、段数の決め打ちも効かない）
  - ★**校名が2行に組まれている表がある**（三重の連合チーム2件。2026-08-15 に修正）。
    **同じ行の文字はスロット軸の座標が等しい**ので、先にスロット軸でまとめてから
    `nameOrder` の向きで読む。**「・」は補わない**（どこが学校の切れ目かは字間でしか出ず、推測になる）
  - ★**トーナメント表は「1〜N と並ぶ行があるか」で一発で選別できる**（2026-08-14）。
    無い表（沖縄）は**勝者の校名を回戦ごとに書き直す形式**で、参加校数すら数えられない。
    **先に日付と検算材料の有無も見る。**
    ★**ただし「検算材料が無い」の判定は甘くなりやすい**（2026-08-17）。
    和歌山は日付が無いのは本当だったが、**お知らせの一覧に優勝校と準優勝校が
    別々の記事として載っていた。** 出典のページ本文まで探すこと。
    ★★**滋賀は「1〜N の行が無い」と分類していたが誤りで、2026-08-17 に実装した**
    （夏46・春45）。**行はあった** —— 全角で、しかも「１５ １６ １７ １８」と
    潰れていたので `/^\d+$/` に当たらなかっただけ。
    ★**「行が見つからない」と「行が無い」は別。**
    見つからないときは**全角・潰れ・2行に割れ**を疑うこと。
    詳細はREADMEの「見分け方」と「★★2026-08-17 に 滋賀」
  - ★**外部の情報源と食い違ったら、どちらが誤りかを「その後の試合」で確かめる。**
    広島で外部が「安芸府中 3-2 武田」と書きながら、同じ情報源が
    武田の2回戦・3回戦を載せていた（＝武田が勝っている）。**外部が誤りだった**
  - ★★**検索結果の要約を突き合わせの材料にしないこと**（2026-08-17。山口の春季）。
    要約は準決勝を「2-0」と書いていたが、**連盟の紙は `0` と `10`**
    （断片の幅も1桁/2桁で合っており、**4/27版と4/28版の2つの紙で同じ**）。
    **中断時点のスコアと混同していた**とみられる。
    **一次資料（連盟のPDF・お知らせ本文）まで降りること。**
    Wikipediaの要約で対戦相手が入れ替わったのと同じ轍
  - **Excelは `scripts/lib/xlsx-rows.mjs` で読む**（2026-08-14 導入。`exceljs`）。
    新潟がこれ。★**1行に試合が横に2つ並ぶ**ので、見出しの「校名」列を全部拾うこと
    （左だけ読んで試合がちょうど半分になり、検算で気づいた）。
    ★**「本大会」「甲子園」のシートを外す**（他県の学校が混ざる）
  - **PDFは `scripts/lib/pdf-text.mjs` で読む**（2026-08-14 導入。`pdfjs-dist`）。
    **devDependency。サイト側のコードから import しない**（35MBがバンドルに入る）。
    ★**座標つきで取り出して行に組み直す。** pdf.jsが返す並びは**描画順であって
    読む順ではない**ので、素直につなぐと表が壊れる。
    ★**1枚のPDFの失敗で1季節を落とさない**（例外を投げず null を返す）
  - **埼玉・神奈川は連盟ではなく個人運営の情報サイトが出典。** 出典表示はサイト名で出す
  - **甲子園（`live-results.ts`）とは別スクリプト・別ファイル。** 出典が県ごとに違い、
    1つにまとめると1県のサイト変更で全国の生成が止まる
  - ★★**取るときは私立の戦績も引用し、着目するところを公立にする**
    （2026-08-21 に運営者が決めた。**それまでは公立が絡む試合だけを生成物に残していた**）。
    **生成物（`src/lib/data/regional/<県>.ts`）には私立どうしの試合も入っている。**
    ★**絞り込みは表示側（`latestSeasonGames`）にある。**
    **県のページに私立どうしの試合を出すという意味ではない。**
    ★**片方だけ戻さないこと**（生成側だけ戻すと画面は変わらないが
    トーナメント表が作れなくなり、表示側だけ外すと画面に私立どうしの試合が並ぶ）。
    ★**なぜ必要だったか**: 落とすと**トーナメント表が作れない。**
    枝が欠けるだけでなく、**次の公立の試合に誰が上がってきたのかも辿れない**
    （方針変更前は全4,033試合に対し私立どうしが**0件**＝全部落ちていた）。
    ★**ベストNを数えるための `allGames` を別に持っていたのも同じ理由**で、
    「表示は公立だけ／数えるのは全部」を2本のデータでやっていた。
  - **トップは抜粋（`regional-pickup.ts`）しか読まない。** 県ごとの全試合は
    `src/lib/data/regional/<県>.ts`（1県約100KB）。47県ぶんを1つにすると6MBになる
  - ★★**進捗地図（`/regional`）は `regional-progress.ts`（4.4KB）だけを読む**
    （2026-08-22）。49地区ぶんを一度に描くので**県ごとのファイルは読めない。**
    ★**季節は「今日の日付」で決めない**（いちばん新しい試合の季節）。
    ★**大会の年を出してから「今季」かを判定する**（`yearOfTournament`）——
    **日付が1つも無い季節がある**ので（実測：夏7県・春3県・秋1県）、
    「新しい試合から120日」の窓が効かず、**前年の決勝を『終了』として出していた**（宮崎）。
    ★**1つの季節に複数の大会が入る県がある**（徳島の秋は5大会）。
    **選ぶのは「最後の試合がいちばん新しい大会」。試合数の多い大会ではない**
    —— 試合数で選ぶと**終わったブロック予選を「県の今季」として出す。**
  - ★★**組み合わせ（未実施の試合）は `upcoming` という別の入れ物に入れる**
    （2026-08-22。大分の秋が最初）。★**`games` に混ぜないこと** ——
    `games` は**スコアと勝敗を必ず持つ**形で、画面・トーナメント表・検算の
    すべてがそれを前提にしている。**足すだけなら既存に1バイトも影響しない。**
    ★**出すのは対戦相手が決まっている回戦だけ**（準決勝・決勝の枠を作って
    相手を空にしない）。★**結果が出た組は生成側で落とす** ——
    **その鍵に大会名を必ず入れること**（校名だけだと**別の大会の同じ顔合わせ**に当たる）。
  - ★★**大会名は年で変わることがある**（2026-08-22。山口）。
    `令和7年度山口県スポーツ大会高校野球競技(硬式)` が
    `令和8年度山口県新人高等学校野球大会` に改称され、**今年の大会を丸ごと
    拾えないまま去年の秋を「いまの秋」として出し続けていた。**
    ★**「秋が去年のままではないか」を毎季確かめること。**
    ★**開催中は「2枚そろわないと出さない」を緩める**（福島と同じ）——
    県決勝大会の紙は大会の後半にならないと出ないので、
    そのままだと**そのあいだ空白になる。** 落とすのは枚をまたぐ検算だけにして、
    **飛ばしたことはログに出す。**
  - ★★**トーナメント表は生成物から組み直す**（`src/lib/regional-bracket.ts`。2026-08-22）。
    **生成物は枝を持っていない**（スロット番号も勝者の行き先も無い）が、
    **全試合が揃っていれば「この回戦の学校は前の回戦のどの試合に勝って来たか」で枝が決まる。**
    ★**私立の戦績を残す方針変更があって初めて成り立つ**（上の項）。
    ★★**組めない大会は組まない**（実測95大会中57大会が成立）。落ちるのは
    **①いちばん深い回戦が2試合以上**（ブロック予選＝木ではない）
    **②決勝から辿り着けない試合がある**（出典に載っていない試合がある。いちばん効く検算）
    **③同じ試合が2つの試合へ繋がる**（`連合` のように**校名が一意でない**）。
    ★**1つでも当たれば null を返し、試合の一覧だけを出す。**
    「だいたい合っている表」を出さない（石川の「構造は合うのに相手が違う」と同じ轍）。
  - **県のページ（`/prefectures/<slug>`）はその県のファイルだけを動的 import で読む**
    （2026-08-13 実装）。索引 `src/lib/data/regional/index.ts` も生成物なので手で書かない。
    **静的 import にしないこと**（どの県のページにも全国ぶんが入る）。
    出すのはいちばん新しい季節の24試合まで。**取れている総数を画面に必ず書く**
  - ★**「いちばん新しい季節」を配列の先頭で決めないこと**（2026-08-17。滋賀）。
    `latestSeasonGames()` は日付のある試合があればそれで決めるが、
    **滋賀は季節を2つ持ち、どちらも日付が無い**（紙に日にちしか書かれていない）。
    先頭を採ると**アダプタの `seasons` に書いた順**で画面が変わるので、
    **季節の順（春→夏→秋）で決める。** ★秋が出たらここを見直すこと
  - **県のページの日付には年を付ける。** トップ（抜粋）は年を省いているが、
    県のページは前年の秋しか無い県があるので年が無いと見分けが付かない
  - ★★**県ごとに、組み立てた直後に書き出す**（2026-08-21。`writeDistrict`）。
    ★**最後にまとめて書かないこと。1県で落ちると実行まるごとが失われる。**
    2026-08-20 に実際に起きた —— 栃木の出典を `fetch`（undici）で取ると
    **Node の HTTP パーサが内部アサーションで落ち**（`assert(!this.paused)`）、
    ★**`try/catch` では拾えない**（イベントループから投げられる）ので
    **プロセスごと死に、それまでの35県ぶんが1つも書き出されなかった。**
    ★**出典側の回避策（栃木だけ `node:https` を使う）は原因の手当てにすぎず、
    どの県でも同じことが起きうる。** 取れたそばから書き出すのが本筋。
    ★抜粋（`regional-pickup.ts`）と索引（`loaders.ts`）は全県が要るので最後のまま。
    途中で死んでも**前の実行のものが残るだけ**で、県のページは壊れない
  - ★★**季節単位の歯止めがある**（2026-08-19 に入れた）。
    生成物を書き出す前に**いまのファイルを読み**、**季節ごとに「前は試合があったのに
    今回0」なら、その季節は前の内容を残す。** 入れる前は
    **日別ページ1枚の `fetch failed` で佐賀の夏32試合が丸ごと消えた**
    （県としては春34・秋34が取れていたので「取れている」と判断されていた）。
    ★**`kept` ではなく `seasonGames` が0のときに見ること**
    （`kept` は120日の窓で切るので、1試合でも取れていれば0にならない）。
    ★**「前より減った」で止めない**（出典が誤りを直して減ることはある）。**0のときだけ。**
    ★**それでも「中身が変わる」ほうは止まらない。** 再生成したら、
    自分が触った県以外の生成物が変わっていないか必ず見ること
  - **自動更新は甲子園と別のワークフロー**（`update-regional-results.yml`。
    2026-08-14）。**1日2回**（19時・22時）。甲子園と同じ3時間おきにしない —
    出典に個人運営のサイトが2つあり、1回で日別ページを数十枚取るため。
    ★**1試合も取れなかった県のファイルと、空の抜粋は書き換えない**
    （出典が変わったときに、その県の試合が消えたコミットを積まないため）
  - **抜粋の選定は生成時、シャッフルは表示時。** 生成時に混ぜるとCIが
    中身の無いコミットを積み続ける
  - **勝ち上がりは「4回戦突破」で出す。** ベストNは全試合が取れている前提が要り、
    取りこぼすと実際より小さく出る。トーナメント表は3県とも画像で読めなかった
  - ★**決勝まで終わった大会は勝ち上がり一覧に出さない**（終わった大会に勝ち上がりはいない）
  - **検算は準々決勝・準決勝・決勝の試合数が 4・2・1 か。** 校名では検算しない
    （出典が同じ学校を回戦ごとに書き分ける）。**全回戦に半分を要求しない**
    （シード校が2回戦・3回戦から登場する）
- **結果速報の枠は時期で中身が入れ替わる**（`src/lib/results-slot.ts`）。
  **今日の日付で判定しない。「最後の試合が新しいほうを出す」。**
  日付で切ると大会の谷間に何も出ない期間ができ、雨天順延にも追随できない。
- **出典の表示は実際の出所と一致させること。**
  手元に同じ記録があっても、転記した経路が別ならその経路が本当の出所。
  読者に対する出典表示の信頼性は規約の問題より優先する。
  `sources` の `url` は任意（運営者の記録・書籍にはURLが無い）。
- **都道府県セレクタの表示切り替えは画面幅ではなくコンテナ幅で判定する。**
  幅の狭いカラムに入れたときにマスが潰れて県名が読めなくなるため
  （`.prefecture-map` のコンテナクエリ）。
  **タイル地図の左上（1〜8列・1〜5行）は丸ごと空くので `heading` / `aside` で使う。**
  トップは見出し（1〜2行目）と凡例・数字・「学校名から探す」（3〜5行目、
  `PrefectureMapGuide`）を入れている。**見出しを地図の外に出さないこと** —
  地図の左上が下から始まって北北海道と上端が揃わず空きができる。
  ★**中身は割り当てた行ぶんの高さに収めること。** 行が `auto` なので、高いと
  同じ行の北海道・東北のマスまで伸びて高さが揃わなくなる。**足したら
  「入れる前と行の高さが変わらないこと」を実測して確かめる**（README参照）。
- 日本語Webフォントは読み込まない（スマホの表示速度優先）。システムフォントスタックを使う。

## ディレクトリ

- `src/app/` — ルーティング（App Router）
- `src/components/` — layout / news / schools / phenomenon / features / rankings / community / common / ads
- `src/lib/queries/` — DBアクセス層
- `src/lib/data/` — **スクリプトが生成するデータ。直接編集しない**
  （`twenty-first-century.ts` / `koshien-tournaments.ts` / `live-results.ts` /
  `regional-pickup.ts` / `regional/<県>.ts`）
- `data/federation-sites.json` — 47都道府県高野連サイトの規約調査の生データ
  （`scripts/survey-federations.mjs` が作る）
- `public/schools/` — **学校の校舎写真（生成物・3,059枚）。直接編集しない。**
  `scripts/build-school-images.mjs` が作る。**gitにコミットする**
- `src/lib/content/` — **手で書く編集コンテンツ**（`tournament-runs.ts`）。
  生成物と紛らわしいのでディレクトリを分けている
- `src/lib/constants.ts` — サイト定数・47都道府県マスタ・各種区分
- `supabase/migrations/` — スキーマ（SQLをGit管理）
